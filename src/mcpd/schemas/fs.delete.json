{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "fs.delete",
  "description": "Delete a file. ALWAYS returns requires_cow_approval (Tier 3) \u2014 deletion is destructive and never executes without explicit COW approval per INV-6.",
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096,
      "description": "Absolute path of the file to delete."
    }
  },
  "required": [
    "path"
  ],
  "additionalProperties": false,
  "x-icebreaker-trust": {
    "tier": 3,
    "reversible": false,
    "requires_hitl": true,
    "requires_cow": true
  }
}
