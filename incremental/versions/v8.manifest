# v8 — Polish + release
# Scope: wallpaper/branding + audit viewer + safe-mode verification + demo script
# Gate: GROUND_TRUTH.md § 3 V8.
#
# STUB ON PURPOSE (rules R2 + D-4): implement version_overlay() only when
# V7 is GREEN on the Status Board. Implementing ahead of the gate
# recreates the big-bang integration failure this project exists to kill.

VERSION_PACKAGES=""

version_overlay() {
    echo "FATAL: v8.manifest is not implemented yet." >&2
    echo "Rule R2: implement it only after V7 is GREEN in incremental/GROUND_TRUTH.md." >&2
    return 1
}
