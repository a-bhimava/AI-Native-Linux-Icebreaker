# ICEBREAKER INCREMENTAL REBUILD — GROUND TRUTH

> **This document is the single source of truth for the incremental rebuild.**
> If code, chat history, or memory disagrees with this file — this file wins.
> Update the Status Board and Failure Log on EVERY gate run. No exceptions.
>
> **Reflects code state through v6.15 arm64 (2026-08-03 audit).** For whitepaper-vs-reality drift see § 10 Whitepaper Departures Ledger (added 2026-08-03) and the audit doc at `docs/2026-08-03_whitepaper_vs_reality_audit.md`.

---

## 1. Status Board

| Version | Name | State | ISO | SHA-256 | Gate passed |
|---------|------|-------|-----|---------|-------------|
| V0 | Boots to GNOME | **GREEN 2026-07-03** (QEMU + UTM, commit `eb274f0`) | `ISO/incremental/v0.iso` | `fa688645…3fac` | 2026-07-03 |
| V1 | SSH + diagnostics | **GREEN 2026-07-03** (QEMU L1 + UTM, commit `50b2293`) | `ISO/incremental/v1.iso` | `f8c9bfff…89a8` | 2026-07-03 |
| V2 | Daemon (QB-unconfigured path) | **GREEN 2026-07-03** (smoke + QEMU L2 + live-guest turn.run/restart + UTM boot w/ SYSTEM HEALTHY, commit `8b4593b`) | `ISO/incremental/v2.iso` | `5cc74e1f…46a3` | 2026-07-03 |
| V3 | Dual-pane terminal | **GREEN 2026-07-03** (smoke + QEMU L3 incl. F-7 behavioral + UTM: TUI from icon, CoT stream, actionable QB error, visible daemon-down warning; commit `4e9c24e`) | `ISO/incremental/v3.iso` | `432c9757…aa6e` | 2026-07-03 |
| V4 | `#` trigger | **GREEN 2026-07-03** (21/21 gates incl. pty end-to-end + UTM: plain cmds OK, `#hello` routed to daemon, SYSTEM HEALTHY; commit `4633363`) | `ISO/incremental/v4.iso` | `38e5e764…dcaf` | 2026-07-03 |
| V5 | QB (Gemini) | **GREEN 2026-07-03** (25/25 QEMU gates on F-20-fixed ISO; UTM with-key: live Gemini parse → fs.list Tier 0 → full CoT pipeline to PB boundary; commit `6bed152`) | `ISO/incremental/v5.iso` | `783d44bc…5246` | 2026-07-03 |
| V6 | PB + mcpd execution | **GREEN 2026-07-04** — full end-to-end verified on UTM: `# list files in /home/icebreaker` returns Gemini→Qwen→mcpd→formatted natural-language listing of 18 files. F-22..F-30 all logged and fixed. Live-guest turn 2m55s under Rosetta emulation | `ISO/incremental/v6.iso` (source ready for rebuild) | pending rebuild | 2026-07-04 |
| V6.1 | V6 Stage 1 fixes baked in (F-27..F-30) | **GREEN 2026-07-04** — 29/29 gates + hash-verified download; carries F-27 PB target, F-28 MCPD_FS_READ_ROOTS + HOME on unit only, F-29 openat2 ENOSYS fallback, F-30 ProtectHome=tmpfs + BindReadOnlyPaths + readlink seccomp + mcpd_timeout=120s + PB prompt hardening | `ISO/incremental/v6.1.iso` | `e2d97d2c…24bf` | 2026-07-04 |
| V6.2 | V6B Stage 2: context-aware QB (cwd, recent_commands, active_window) | **TESTING** — 29/29 gates + hash-verified download; carries Stage 1 (F-27..F-30) + Stage 2 (ShellContext, XML preamble at both QB call sites, all 4 QB prompts redrafted with CONTEXT USAGE + examples, Terminal cwd tracking, ib_trigger IB_CWD/IB_RECENT export). UTM boot check pending. Live-guest pre-verified: `# list files here` resolves via cwd | `ISO/incremental/v6.2.iso` | `916777f4…55fc` | — |
| V6.3 | F-31 EROFS + F-32 `/` guard + Stage 3 (wmctrl + gate asserts) | **RED — SIGSYS in fs.write** — 29/29 automated gates PASS but UTM boot 2026-07-07: `# create Hello.txt` → mcpd killed by SIGSYS in `__GI_fsync()`. Root cause: seccomp allowlist missing `SYS_fsync`; the QEMU gate never exercised fs.write. Fix in V6.4 | `ISO/incremental/v6.3.iso` | `3c094c4c…48e5` | — |
| V6.4 | F-33 fsync + R8 harvest gate | **GREEN 2026-07-07** — F-33 fix verified in UTM (`fs.write "Hello.txt"` writes 14 bytes to `/home/icebreaker/`, fsync survives seccomp). Also surfaced F-34 (cosmetic ib-debug probe misdiagnosis) and F-35 (silent lookalike for `take me to downloads`). Both fixed in V6.5 | `ISO/incremental/v6.4.iso` | `174ef21f…67ed` | 2026-07-07 |
| V6.5 | F-35 intent coverage + F-34 ib-debug fix | **GREEN 2026-07-08 (via V6.51 rebuild)** — F-35/F-34 fixes intact; F-36 surfaced separately on non-AVX2 x86-64 hardware | `ISO/incremental/v6.5.iso` | `040d4952…207f` | 2026-07-08 |
| V6.51 | F-36 CPU-support scope decision + F-24 smoke-gate hardening | **TESTING** — Harvest gate ALL GREEN; corpus 9/9 offline; smoke gate ALL GREEN; QEMU gate PASS L6 (pbd active in 8s + /health=ok in 12s + INV-3). AVX2 min-CPU documented. UTM check pending: v6.5 test plan applies (F-35 UNSUPPORTED cards + F-33 fs.write + F-34 ib-debug clean) | `ISO/incremental/v6.51.iso` | `313d03d2…a12bb` | — |
| V6.6 | Multi-arch build system (amd64 + arm64) | **TESTING** — arm64 QEMU gate PASS 2026-07-08 (28/28 across L0–L6 under cross-arch TCG); F-37 (grub-efi-arm64-bin), F-38 (arm64 vmlinuz gzip decompress), F-39 (SOCK_MAX/L4_TIMEOUT under cross-arch), F-40 (per-arch mcpd install) all fixed. arm64 UTM Virtualize check pending. amd64 regression rebuild pending. Venv cache infra (v2.manifest): 55-min pip → 4-sec cache extract | amd64: pending · arm64: `ISO/incremental/v6.6-arm64.iso` `cb5304cd…0cac66` | — · — | 2026-07-08 (arm64 QEMU) |
| V6.61 | QB→PB rich envelope + system.unsupported crash fix | **RED** — UTM surfaced new bugs the automated tests didn't catch: F-48 (`step_state="unsupported"` fails CotEvent validation, port of F-42 was wrong), F-47b (intent_id override only landed on streaming path, non-streaming RPC path still leaked placeholder UUIDs), F-50 (F2 NL mode subsequent-turn regression + Control Center invisible-window due to GTK4 DRI3 default renderer). Also revealed venv-cache reseed can silently ship stale Python code (fixed structurally in V6.62 with F-51 integrity check). Superseded by V6.62 | arm64: `ISO/incremental/v6.61-arm64.iso` `f8ef6120…6dc98b` | — | — |
| V6.62 | UTM-surfaced regressions + venv-cache defense | **TESTING** — cache-hit rebuild (43 min) with fresh venv (hash `4e6f5cca…`). Fixes: F-48 (step_state="done" for unsupported), F-47b (extracted `_normalize_server_owned_fields` helper applied to both streaming + non-streaming paths), F-50 (defensive `nl_mode` cross-check in on_input_bar_submitted + idempotent `#` prepend in _run_nl_turn), Control Center GTK4 fix (`env GSK_RENDERER=cairo GDK_BACKEND=x11` in Exec line), Bug #2 F-45 HITL threading main-thread guard, Bug #7 F-46 search intent guardrail, stale test failures (audit outcome enum + pbd checksum). UTM verify pending | arm64: `ISO/incremental/v6.62-arm64.iso` `173a7606…12cb84` | — | — |
| V6.63 | F-49 configurable verifier retry + F-51 venv integrity check | **BUILDING** — verifier retry_mode enum (off/on_call_failed_only/on_any_rejection/skip_tier_01) with runtime dispatch in main.py, Control Center Behavior page for A/B testing modes, 19 offline tests parametrized over the modes. v2.manifest F-51 marker check aborts build if any recent-bug fix substring is missing from the installed venv — cache-reseed disasters cannot repeat silently | — | — | — |
| V6.64 | F-52 backend intent-schema relaxation + F-53 exception surfacing | **BUILDING** — Gemini backend response_schema was rejecting server-owned fields (`intent_id`, `schema_version`, `timestamp`) before the daemon-side normalize could regenerate them; `_relax_server_owned_for_backend()` strips their `required`+`format` for the backend copy while `validate()` still enforces post-normalize. Verifier BrainTruncationError surfaced through exception message instead of opaque "verifier call failed". Shipped default `qb.gemini.max_tokens` bumped 512→2048 as reactive workaround for the specific bug (v6.65 raises properly) | — | — | — |
| V6.65 | Pipeline robustness foundation + full Control Center Settings | **BUILDING** — Scope A: raised schema ceilings (max_tokens 32k→1M, timeouts 600s→7200s), bumped shipped defaults (qb.gemini.max_tokens 2048→8192, pb_max_tokens 256→1024, daemon.max_connections 1→4), added `x-icebreaker-trust` field to all 23 mcpd tool schemas (Phase 7 M7.1 will enforce via R13). Scope B: F-53 pattern applied to 12 exception sites via new `_log_exception` helper writing to SystemLogger; `verbose_errors` opt-in for full tracebacks. Scope C: five new Control Center pages — Models (backend/model picker with presets + Custom…), Limits (cost/turns/HITL), Errors (last 50 SystemLogger exceptions + verbose toggle), Tools (read-only mcpd tool inventory with tier/HITL/COW badges); Behavior page extended with fallback backend chain checkboxes + auto-cancel-on-cost toggle. Scope D: `qb.fallback_chain` config array + `FallbackChain` composition wrapper — transport/API errors fall through primary→fallback[0]→…, schema errors propagate (prompt issue). 16 new offline tests. Scope E: empty `mcp_allowlist.toml` ships; mcpd probes on startup and logs entry count (Phase 7 M7.2 wires the spawner). 1648 tests pass offline | — | — | — |
| V6.13_OC | OC edition (opencode + Gemini) v6.13 baseline — Fix M/N/P/Q/R stack | SHIPPED 2026-07-25 — iceui MCP wrapper (Fix M), oc_audit_bridge (Fix R), launcher (Fix P), daemon retention (Fix Q). Both arches. OC edition INV-1 gap surfaced but not closed (see § 10 rows A-1..A-9) | `ISO/incremental/v6.13_OC-arm64.iso` | `bc0c6fbd…` | 2026-07-25 |
| V6.14 | Fix M polish + F-101/F-102 iceui hardening (both editions) | SHIPPED 2026-07-31 — iceui self-test loosening, uinput udev, 12 gui.*/rpa.* tools reachable from OC edition | `ISO/incremental/v6.14_OC-arm64.iso` | `d2c931b4…` | 2026-07-31 |
| V6.15 | Fix V — full-blown vision-grounded UI automation (both editions) | SHIPPED 2026-08-03 — 24 iceui tools (12 v6.14 AT-SPI + 12 new vision), VisionGrounder + xdotool + HiDPI + trust store + annotated preview. F-103..F-108 landed. Smoke gate ALL GREEN | current: `9d2a176e…` · OC: `68eaaad2…` | 2026-08-03 |
| V6.16 | **Phase 7 convergence M7.0.1 + M7.0.2**: COW simulation dry-run + gated commit (both paths) + PB GBNF grammar wire-up + bounded retry with QB-consult rescue | **SHIPPED 2026-08-07 (arm64) + 2026-08-08 (amd64)** — arm64 `b6ec752f…`, amd64 `7b91db5f…`, both smoke-gate ALL GREEN incl. F-109 + F-110 assertions. Closes audit rows **C-1, B-2, B-3 (via F-109)** + **B-4, C-4 (via F-110)** — 5 of 26 departures. **Current-edition only** — OC edition bypasses the Controller pipeline this milestone shipped, so v6.16 machinery is dead code on OC ISOs. That gap is closed in v6.17 M7.6a-1 (see F-111) | current arm64: `b6ec752f…` · current amd64: `7b91db5f…` · OC: not built | — | 2026-08-07 (arm64 smoke-gate GREEN) |
| V6.17 | **Phase 7 convergence M7.6a-1**: OC edition submit_intent — closes the OC INV-1 gap so v6.16 M7.0.1 + M7.0.2 safety features finally reach OC users | **TESTING (2026-08-17)** — F-112 repair ISOs passed complete level-6 QEMU gates on both architectures. amd64 socket appeared in 6s; arm64 booted under TCG in 766s. Both passed daemon.status, turn.run, restart, PB health, INV-3, and the architecture-native in-guest seccomp harvest. **R1 UTM GUI boots on both repair ISOs remain required before V6.17 is green.** | local: `ISO/incremental/v6.17a_OC-amd64.iso` SHA `e51e66f…32a68` · `ISO/incremental/v6.17a_OC-arm64.iso` SHA `58ae8b56…14b5b` | — | 2026-08-17 both QEMU gates green |
| V6.18 | **Phase 7 convergence M7.0.3 + M7.1**, plus the **Frosted XFCE OC profile** | PLANNED — M7.0.3/R13 scope remains mandatory. The additive profile uses isolated `xfce-frosted` bases, LightDM/X11, verified MacTahoe/Plank assets, Frosted app defaults, and opt-in compositor effects. Build is blocked until V6.17's R1/R11 gates are recorded green. | — | — | — |
| V6.19 | **Phase 7 convergence M7.6a-2 + M7.6b**: vision parity via submit_intent + whitepaper §3/§4/§6 realignment + R14 backfill | PLANNED — closes audit rows A-9, B-1, B-5, C-17 | — | — | — |
| V6.20 | Phase 7 M7.2 (was V6.8): curated external MCP allowlist infrastructure | PENDING — inherits real COW + grammar-bound PB from v6.16 | — | — | — |
| V6.21 | Phase 7 M7.3 (was V6.9): Playwright MCP for browser | PENDING | — | — | — |
| V6.22 | Phase 7 M7.4 (was V6.10): MS Graph + Google Workspace MCP | PENDING | — | — | — |
| V6.23 | Phase 7 M7.5 (was V6.11): analytics.chart + analytics.summarize + fs.find | PENDING | — | — | — |
| V6.24 | Phase 7 M7.6 (was V6.12): E2E hardening + adversarial pen-test + p95 profile | PENDING | — | — | — |
| V6.25 | Phase 7 M7.7 (was V6.13): docs + GPG-signed ISO | PENDING | — | — | — |
| ~~V6.7~~ | ~~Phase 7 M7.1 (roadmap 2026-07-09 numbering)~~ | SUPERSEDED — see V6.16-V6.25 for the 2026-08-03 post-audit convergence sequence | — | — | — |
| ~~V6.8~~ | ~~Phase 7 M7.2 (old numbering)~~ | SUPERSEDED — see V6.20 | — | — | — |
| ~~V6.9~~ | ~~Phase 7 M7.3 (old numbering)~~ | SUPERSEDED — see V6.21 | — | — | — |
| ~~V6.10~~ | ~~Phase 7 M7.4 (old numbering)~~ | SUPERSEDED — see V6.22 | — | — | — |
| ~~V6.11~~ | ~~Phase 7 M7.5 (old numbering)~~ | SUPERSEDED — see V6.23 | — | — | — |
| ~~V6.12~~ | ~~Phase 7 M7.6 (old numbering)~~ | SUPERSEDED — see V6.24 | — | — | — |
| ~~V6.13~~ | ~~Phase 7 M7.7 (old numbering)~~ | SUPERSEDED — see V6.25 → v1.0 | — | — | — |
| v1.0 | v1.0 GPG-signed release ISO | PENDING — Phase 7 exit | — | — | — |
| v1.1+ | Phase 8 milestones (M8.1-M8.5): Autonomous Agent Loops | PENDING — post-v1.0 | — | — | — |

States: `RED` (not passed) · `BUILDING` · `TESTING` · `GREEN <date>` (gate passed on fresh ISO, both QEMU + UTM)

---

## 2. The Rules (non-negotiable)

- **R1 — Gate on real boots.** A version is DONE only when its gate checklist passes on a **freshly built ISO**, booted in BOTH QEMU (headless, on the build VM) and UTM (GUI, on the Mac). Chroot tests and unit tests do not count as a gate pass.
- **R2 — One version at a time.** Never write code for Vn+1 while Vn is RED. Manifests for future versions stay as unimplemented stubs until their turn.
- **R3 — Archive every GREEN ISO.** Copy to `ISO/incremental/vN.iso` + `.sha256` on the Mac. If Vn+1 breaks, we boot Vn and diff.
- **R4 — Debug live, rebuild to verify.** Inside a version, iterate with `update/ib-update.sh` against the booted VM (~2 min per cycle). Rebuild the ISO only to run the gate. Never debug by rebuilding ISOs.
- **R5 — Log every failure.** Every failure gets a dated row in § 7 Failure Log: symptom → root cause → fix → version. Read the log before debugging anything — most failures are repeats.
- **R6 — Silent failure is a bug.** Any component that can fail must say *why* on screen or in a log. A blank window, a swallowed exception, or `|| true` on a critical path is itself a defect to fix, independent of the underlying error.
- **R7 — Commit before building.** Builds use rsync from the Mac; uncommitted code CAN reach the VM, but a gate pass only counts if the built tree matches a pushed commit (record the SHA in the Status Board row).
- **R8 — No ISO without a harvest-gate pass (F-33 rule).** For any V ≥ 6 build, `incremental/build/mcpd-harvest.sh` MUST exit 0 before `build-iso.sh` extracts the base chroot. This runs mcpd on the VM with `MCPD_SECCOMP_LOG_ONLY=1` AND under the real allowlist, exercises every documented tool against a scratch `$HOME`, and refuses to proceed if any syscall is missing from the allowlist or mcpd dies with SIGSYS. The write path's fsync gap (F-33) would have been caught pre-build if this gate had existed. **Any new mcpd tool category or expanded write scope requires re-running this gate.**
- **R9 — Every user-visible intent maps to a real tool OR to `system.unsupported` (F-35 rule).** Silent lookalikes are worse than errors. QB emits `system.unsupported` when no listed action fits; the Controller enforces `_SUPPORTED_ACTIONS` and rewrites any phantom action to `system.unsupported` before dispatch. The golden intent corpus at `dual-brain/controller/tests/corpus/intent_corpus.json` is the regression floor — `build-iso.sh` runs it offline before ISO packaging, and it must pass. **Every new intent class we support (or refuse) needs a corpus row.**
- **R10 — Document the CPU support window; don't chase indefinite backward compatibility (F-36 rule, revised 2026-07-08).** After three failed rebuild attempts to support pre-Haswell x86-64, the pragmatic answer is: **AVX2 required (Haswell 2013+ / x86-64-v3 baseline)** and the ISO first-boot must surface that requirement clearly on unsupported CPUs. That covers 13 years of hardware and both primary test targets (Apple Silicon UTM via Rosetta 2 + GCP VM native). If we later choose to widen the window, ship per-CPU variants with a runtime dispatcher — do NOT try to compile one binary that works everywhere with hand-tuned flags. That path burns engineering time faster than it produces working binaries.
- **R11 — V ≥ 6.6 ships both amd64 and arm64 (multi-arch rule).** A V ≥ 6.6 is GREEN only when **both** arches pass their QEMU gate on the GCP build VM AND both boot in UTM (Emulate mode for amd64, Virtualize mode for arm64 on Apple Silicon). The `config/archs/${ARCH}.conf` files are the single source of arch-specific truth; every arch-aware build script sources them. Adding a third arch = new conf file + entry in `Makefile`'s `ARCHES` var + install cross-toolchain, done. V0–V6.51 are amd64-only historical artifacts; retroactive arm64 builds for pre-V6.6 are out of scope.
- **R12 — External MCP servers ship through a curated allowlist (Phase 7 M7.2 rule).** From V6.8 forward, third-party MCP servers (Playwright, Chrome DevTools, MS Graph, Google Workspace, etc.) are consumable only if they appear in `cx-distro/distro/mcp_allowlist.toml` with `{name, git_url, commit_sha, sha256, tier_ceiling, egress_hosts}`. `mcpd` spawns each as an unprivileged subprocess with scoped Landlock + seccomp + `nftables` egress allowlist + Tier ceiling enforcement. CI gate `external-mcp-integrity.sh` verifies SHA + Git SHA per entry before any ISO builds. Rationale: the Anthropic MCP registry has ~9,600 servers as of May 2026 — many are excellent, most are supply-chain risk. Curated allowlist + pinning + sandbox is the honest way to consume the ecosystem.
- **R13 — Every mcpd tool declares `x-icebreaker-trust` in its schema (Phase 7 M7.1 rule).** From V6.7 forward, `src/mcpd/schemas/*.json` and any external allowlisted MCP tool schema must include `x-icebreaker-trust: {tier: 0|1|2|3, reversible: bool, requires_hitl: bool, requires_cow: bool}`. `mcpd` refuses to start with tools that omit it; corpus tests fail on tool references without the field. This is the schema-embedded form of graduated determinism — no more grepping `main.py`'s `_SUPPORTED_ACTIONS` to know a tool's risk profile.
- **R14 — Every F-xx fix carries a named regression lock (F-4x/F-5x rule, 2026-07-11).** R9 covers *routing* regressions via `intent_corpus.json`. R14 covers *behavioral* regressions. Every entry in the Failure Log is not "closed" until its row carries a `regression lock: <file>[::test]` annotation naming a corpus row, a test module, or a specific test function that fails when the fix regresses. Adding a new bug fix without an accompanying regression lock is a review-blocker. The `regression lock` annotation lives on the row itself so `git grep F-43` finds both the failure story AND the test that protects against its return. Rationale: fixes without named locks silently rot when the surrounding code refactors — a UTM boot two months later re-surfaces the exact same failure and the "we fixed this once" claim in `git log` no longer maps to any live assertion.
- **R15 — Every catalogued mcpd tool is exercised in `mcpd-harvest.sh` (F-55 rule, 2026-07-11).** Every tool in `_SUPPORTED_ACTIONS` that dispatches to mcpd MUST have a corresponding `LOG_ONLY` exercise in `incremental/build/mcpd-harvest.sh`. A tool that has never been exercised under seccomp `LOG_ONLY` is a latent F-33 / F-55 waiting to fire post-ship — the syscall gap only surfaces when a user actually invokes that tool. R15 complements R8 (no ISO without a harvest-gate pass) by defining WHAT the harvest gate must exercise. Ship both `*at` and `*at2` variants for every path-check syscall (per containerd PR #4481) — arm64 kernels don't implement legacy `access(2)` at all, and glibc 2.33+ prefers `faccessat2` when the kernel supports it. When Phase 7 M7.2 lands external MCP servers, R15 extends: every allowlisted external tool that reaches mcpd's sandbox gets a harvest exercise before its allowlist entry can merge.
- **R16 — Pipeline migrations audit every invariant on the old path (F-32-recur / F-82 / F-83 rule, 2026-07-20 / 2026-07-21).** Any change that redirects the primary request flow (a config flag flip like Task #173 `agent_graph.enabled=True`, or a hard cut-over from module A to module B) MUST audit every invariant enforced by the old path and either port it to the new path OR write a regression test that fails on the new path without the port. No exceptions for "small config flag" changes when the flag redirects the primary flow. **Extension 1 (F-82 addendum, 2026-07-21):** every INV-* invariant needs an active regression test on both paths of any dual-path config flag, not just the default path — INV-8 was silently violated for ~4 days on shipped v6.11 because no test exercised `agent_graph.enabled=True AND audit populates`. The offline test suite passed on both paths only because it never asked "did the log grow?" — a question that trivially detects the failure but only if you wrote it. **Extension 2 (F-84/F-85/F-86/F-87 addendum, 2026-07-21 later):** every user-facing outcome path must have a live golden-query test in Stage E of the release plan, not just a unit test. The four v6.12 defects F-84 through F-87 all had passing unit tests but no one ran the actual `# ...` shell command through a real Gemini call until Stage E — that's when the wire-shape bugs surfaced (nav.cd not resolving next turn, verifier rejecting every tier ≥ 2 turn, F-35 rendering as red error, shell $PWD not syncing). Golden queries are cheap; they belong in every release plan going forward. R16 exists because Task #173 was framed as a config flip when it was in fact a pipeline replacement; the failure mode is well-defined enough to prevent by discipline, and any future dual-path flag under our review gets an explicit R16 gate on the PR checklist.
- **R17 — Every whitepaper claim shipping differently is an active row in § 10 (2026-08-03 rule).** The 2026-08-03 audit (`docs/2026-08-03_whitepaper_vs_reality_audit.md`) surfaced 26 places where `AI_Native_OS_Whitepaper.md` claims one thing and the shipping code does another. R5 (log every failure) handled *bugs*; R14 handled *regression locks*; R17 handles *spec-vs-reality drift*. **Every whitepaper claim that ships differently must be an active row in § 10 Whitepaper Departures Ledger**, either resolved (→ F-xx that closes it) or explicitly deferred (→ roadmap milestone with a rationale). Any PR that changes an INV-\* invariant, edits a whitepaper section referenced in § 10, or introduces a departure from `AI_Native_OS_Whitepaper.md` must touch § 10 in the same commit. New whitepaper departures without a row are a review-blocker. Rationale: without R17, the F-41 rich envelope and the COW stub and the OC-edition INV-1 gap each drifted silently for weeks or months; the audit found them only after a user asked "why isn't this dual-brain anymore?" — the exact failure mode BP-13 exists to kill, applied at the architecture level. § 10 seeded on 2026-08-03 with the 26 rows from the audit; every future audit + every future INV-touching PR extends it.

---

## 3. The Version Ladder

Each version isolates exactly ONE integration seam. When a version breaks, the cause is confined to what that version added.

### V0 — Boots to GNOME desktop
- **Adds:** Ubuntu noble (debootstrap) + kernel + live-boot + GNOME desktop suite + GDM autologin (`icebreaker`/`icebreaker`) + virtio modules. **ZERO Icebreaker code.**
- **Seam proven:** the ISO build machinery itself (debootstrap → squashfs → hybrid BIOS/EFI boot).
- **Build:** `sudo bash incremental/build/build-base.sh` (once), then `sudo bash incremental/build/build-iso.sh 0`
- **Gate:**
  - [ ] QEMU: `incremental/tests/qemu-gate.sh <iso> 0` passes (ssh reachable, `graphical.target` active)
  - [ ] UTM: boots to GNOME desktop without interaction; GNOME Terminal opens; keyboard works
  - [ ] `cat /etc/icebreaker-version` prints `v0`

### V1 — SSH access + diagnostics
- **Adds:** sshd enabled + `ib-debug` (from `dual-brain/scripts/ib_debug.py`) at `/usr/local/bin/ib-debug` + `/etc/icebreaker-version`.
- **Seam proven:** remote access + self-diagnosis. **Unlocks the 2-minute live-update loop for every later version.**
- **Gate:**
  - [ ] QEMU gate passes at level 1 (ssh in, `ib-debug snapshot --no-color` runs without traceback)
  - [ ] UTM: `ssh icebreaker@<utm-ip>` from the Mac works; `ib-debug snapshot` runs
  - [ ] `update/ib-update.sh <utm-ip> --dry-run` connects and reports target paths

### V2 — Controller daemon (unconfigured brains) + mcpd
- **Adds:** Python venv **built inside the chroot** at `/opt/icebreaker/venv` + `controller` package + package data (schemas/, prompts/, grammars/, catalogue.toml — PKG-1) + `icebreaker-controller.service` (+ sysusers.d/tmpfiles.d) + socket at `/run/icebreaker/controller.sock` (PKG-4 perms) + **mcpd binary** (daemon spawns it unconditionally; dormant until V6) + Mac ssh pubkey in authorized_keys. QB = gemini **without a key** → tested `_UnconfiguredBackend` path; PB absent → same. `ICEBREAKER_SOCKET_TIMEOUT=0` so ExecStartPre doesn't stall (V6 reverts). See D-7 for why there is no echo backend.
- **Seam proven:** pip packaging, data files, systemd unit + sysusers/tmpfiles, socket creation/perms/auth, mcpd spawn+handshake — the historically worst seam, with zero AI complexity.
- **Gate:**
  - [ ] `ib-debug snapshot`: controller service active, socket ping (daemon.status) OK
  - [ ] socket perms `0660 root:icebreaker-users` (PKG-4)
  - [ ] `turn.run "hello"` over the socket returns a **graceful structured error** naming GEMINI_API_KEY (R6) — and the service stays active (no crash-loop)
  - [ ] `systemctl restart icebreaker-controller` → active again within ~10 s (no wait-for-sockets stall)
  - [ ] passwordless `ib-update.sh <ip> --dry-run` from the Mac succeeds

### V3 — Dual-pane terminal
- **Adds:** `terminal` package (+ styles.tcss) + `icebreaker-terminal.desktop` + autostart + `ib-wait-sock` helper. **Includes fix:** replace silent `except Exception` in `dual-brain/terminal/__main__.py:26-32` with a 20 s retry loop + visible `startup_warning` (plumbing already exists in `terminal/app.py:130-139`).
- **Gate:**
  - [ ] UTM: desktop icon opens split-pane TUI; F2 NL mode; typed text echoes back via daemon
  - [ ] `sudo systemctl stop icebreaker-controller` → TUI launched fresh shows explicit daemon-unreachable warning (never blank/silent)
  - [ ] Autostart: TUI appears on login without racing the daemon

### V4 — `#` trigger in default terminal
- **Adds:** `shell/ib_trigger.bash` → `/usr/share/icebreaker/shell/` + sourced in `/etc/skel/.bashrc` AND explicitly appended to `/home/icebreaker/.bashrc` (skel is copied at useradd time, which happens in the base — see Failure Log F-6).
- **Gate:**
  - [ ] UTM: `# hello world` in GNOME Terminal prints the daemon echo response
  - [ ] Plain commands (`ls`, `git status`) still work normally; `#comment` in scripts unaffected

### V5 — QB (Gemini) live
- **Adds:** Gemini backend config in `controller.toml` + API-key provisioning flow (env file `/etc/icebreaker/locations.env`, documented key setup on first boot).
- **Gate:**
  - [ ] With key installed: `# list the files in my home directory` returns a real parsed intent (no execution yet — PB absent)
  - [ ] Without key: explicit "QB not configured: set GEMINI_API_KEY" message (R6), daemon stays healthy

### V6 — PB + mcpd execution
- **Adds:** `run7_cot_q4km.gguf` embedded (INV-7 checksums) + llama-server + `icebreaker-pbd.service` + `start-pbd` + mcpd binary + schemas + HITL approval flow. **Includes fix:** actionable "PB model missing — run `controller.model_registry install --id qwen-2.5-coder-1.5b-instruct-q4_k_m`" message from `_UnconfiguredBackend`.
- **Gate:**
  - [ ] End-to-end: NL request → dry-run report → human approve → command executes
  - [ ] Adversarial prompt (`# delete everything on this system`) → refusal
  - [ ] `ib-debug snapshot`: pbd active, model checksum OK, mcpd has no listeners (`ss -tlnp | grep mcpd` empty — INV-3)

### V7 — Chatbot GUI
- **Adds:** `gui` package + `icebreaker-chatbot.desktop` (+ autostart). GTK deps already in base (asserted by smoke gate from V7 on). **Includes fixes:** 3 s connect timeout + threaded connect + "Daemon unreachable — Retry" banner in `dual-brain/gui/daemon_client.py` / `gui/app.py`; guard `_load_widget_classes()` import (`gui/app.py:78`).
- **Gate:**
  - [ ] UTM: chatbot icon opens window, message streams a response
  - [ ] Stop daemon → banner + Retry button (never frozen/blank — R6)

### V8 — Polish + release
- **Adds:** wallpaper/branding, audit viewer, safe-mode boot entry verification, README/demo script.
- **Gate:** full demo script passes end-to-end on UTM; all previous gates re-run green on the final ISO.

---

## 4. Directory Layout

```
incremental/
├── GROUND_TRUTH.md            # this file
├── build/
│   ├── packages-desktop.txt   # canonical apt package list — hash keys the base cache
│   ├── build-base.sh          # ONE-TIME: debootstrap+packages → .build/base-<hash>.tar.zst  (~60 min)
│   ├── build-iso.sh           # per version: base + manifest overlay + smoke gate + ISO      (~15 min)
│   └── smoke-gate.sh          # in-chroot assertions, level-gated by version; build ABORTS on failure
├── versions/
│   └── v0.manifest … v8.manifest   # bash-sourceable: VERSION_PACKAGES + version_overlay()
├── update/
│   └── ib-update.sh           # push code to a BOOTED system over ssh, restart, ib-debug     (~2 min)
├── tests/
│   └── qemu-gate.sh           # headless boot of ISO + ssh assertions, level-gated
└── .build/                    # (VM only, gitignored) base tarballs, chroots, ISOs
```

**Code is referenced, not forked.** Manifests copy from `dual-brain/`, `src/mcpd/`, `shell/`, `cx-distro/distro/` at build time. There is no second copy of application code to drift.

---

## 5. Build-machine runbook

| Item | Value |
|------|-------|
| Build VM | `icebreaker-build-vm` · zone `us-central1-a` · project `project-ef281c18-2a28-4139-a89` · account `team.projectsyard@gmail.com` |
| Start VM | `gcloud compute instances start icebreaker-build-vm --zone=us-central1-a --project=project-ef281c18-2a28-4139-a89` |
| Repo on VM | `$HOME/Icebreaker/` — git clone, pulled per build (`git fetch origin && git checkout <branch> && git pull --ff-only`) |
| Sync | branch push + pull. From Mac: `git push origin <branch>`. On VM: `cd ~/Icebreaker && git fetch && git checkout <branch> && git pull --ff-only` |
| Build pattern | Always inside tmux: `tmux new -d -s build '<cmd> 2>&1 \| tee /tmp/build.log'` — survives SSH drops |
| cargo gotcha | cargo lives at `$HOME/.cargo/bin/cargo`; `sudo` strips PATH. Use `sudo env PATH=$HOME/.cargo/bin:$PATH bash …` (only matters when a version rebuilds mcpd) |
| Models on VM | `$HOME/models/*.gguf` + `$HOME/Icebreaker/models/checksums.sha256` |
| Download ISO | `gcloud compute scp icebreaker-build-vm:~/Icebreaker/incremental/.build/out/vN-arm64.iso ~/Documents/Icebreaker/ISO/incremental/ --zone=us-central1-a --project=project-ef281c18-2a28-4139-a89` |
| STOP the VM when done | `gcloud compute instances stop …` — it bills while running |

## 6. The two loops

**Inner loop (debugging a version) — ~2 min:**
```
edit code on Mac
  → update/ib-update.sh <booted-vm-or-utm-ip>     # rsync code into venv, restart services
  → ib-debug snapshot on target                    # green?
  → repeat
```

**Outer loop (passing a gate) — ~15 min + boot tests:**
```
commit + push → rsync to build VM
  → build-iso.sh N        (reuses cached base; smoke gate runs in chroot — build aborts if red)
  → tests/qemu-gate.sh    (headless boot assertions on VM)
  → scp ISO to Mac → boot in UTM → run gate checklist by hand
  → update Status Board (state, ISO path, sha256, commit SHA, date) → archive ISO (R3)
```

---

## 7. Failure Log (append-only — READ THIS BEFORE DEBUGGING)

Seeded from two months of prior failures. Every new failure gets a row.

**Scope note (added 2026-08-03):** § 7 is for *runtime failures* (a shipped bug, a UTM crash, a smoke-gate red). For *whitepaper-vs-reality drift* (a claim the whitepaper makes that the code ships differently) see **§ 10 Whitepaper Departures Ledger**, governed by R17. Do not file spec drift here.

| ID | Date | Symptom | Root cause | Fix / rule |
|----|------|---------|-----------|------------|
| F-1 | 2026-06 | Blank terminal on icon click | `from terminal.app import …` OUTSIDE try/except in `controller/__main__.py`; `.desktop` fallback `exec bash` swallowed the error | Imports inside try/except; never `exec bash` fallback (R6) |
| F-2 | 2026-06 | Terminal icon dead on vm profile | `.desktop` hardcoded `gnome-terminal`, not installed on XFCE profile | `Terminal=true` — let the DE choose its terminal |
| F-3 | 2026-06 | `RuntimeError: Daemon already running` | `icebreaker --terminal` started a 2nd daemon while systemd ran one | Terminal connects with `--sock`; never self-starts a daemon |
| F-4 | 2026-06 | TUI crash on NL turn | `App.run_in_thread()` doesn't exist in Textual | `asyncio.to_thread()` |
| F-5 | 2026-06 | Controller crash-loop at boot | `LlamaCppLocalBackend.__init__` health-probes llama-server; raises when pbd.sock absent | `_build_pb_safe()` → `_UnconfiguredBackend`; daemon must start degraded, not die |
| F-6 | 2026-06 | `#` trigger absent for icebreaker user | `useradd` copies skel BEFORE artifact overlay; skel edits never reached existing home | Patch `/home/icebreaker/.bashrc` explicitly AFTER overlay |
| F-7 | 2026-07 | Terminal silently degrades to shell-only | bare `except Exception: daemon_client = None` in `terminal/__main__.py:26-32` | V3 fix: retry 20 s + visible startup_warning (R6) |
| F-8 | 2026-07 | Chatbot window frozen ~30 s | `GtkDaemonClient.connect()` has no timeout, blocks GTK main loop | V7 fix: 3 s timeout + threaded connect + Retry banner |
| F-9 | 2026-07 | Apps randomly dead after login | autostart `.desktop` races daemon socket with blind `sleep 5` | V3 fix: `ib-wait-sock` helper loops until socket accepts |
| F-10 | 2026-07 | `ModuleNotFoundError: gi` possible | GTK system packages only installed in one build stage; `--skip-to` could omit | GTK deps in canonical `packages-desktop.txt` (in base, always present); smoke gate asserts import |
| F-11 | 2026-07 | `cargo: command not found` under sudo on VM | sudo strips user PATH | See runbook § 5 |
| F-12 | 2026-07 | Build "succeeded" but ISO broken | `cmd \| tee log` reports tee's exit code, not the build's | `set -o pipefail` in ALL build scripts |
| F-13 | 2026-07-03 | V0 QEMU gate failed on `graphical.target not active` while gdm WAS active | Gate checked the target once, the instant sshd came up; under TCG the target is still activating for minutes after | qemu-gate.sh polls graphical.target with a grace period (120 s KVM / 600 s TCG). Rule: boot-time assertions must poll, never single-shot |
| F-16 | 2026-07-03 | V2 QEMU gate failed on `restart took 20s (≤15s)` — 10/11 checks green | Restart limit ignored TCG's ~5x Python-startup penalty (boot timeout was already accelerator-aware, restart limit wasn't) | Gate: 15 s KVM / 45 s TCG. Rule: every timing assertion in a gate must scale with the accelerator. A real wait-for-sockets stall reads 60 s+ either way |
| F-15 | 2026-07-03 | V2 build: smoke gate all green, then `xorriso: Image size exceeds free space` | VM disk 98% full — old monolithic chroot (15 G), untarred base chroot (5 G), already-archived ISOs (4.7 G) | Cleaned; build-base.sh now deletes its chroot after tarring; build-iso.sh fails fast if <12 G free. VM ISOs are deletable once archived on the Mac (R3) |
| F-14 | 2026-07-03 | UTM boot: GRUB `attempt to read or write outside of disk 'cd0' / you need to load the kernel first` | ISO booted while still downloading from the build VM — truncated file | Rule: **verify sha256 against the build hash BEFORE booting** (`shasum -a 256 ISO/incremental/vN.iso`). The scp task prints it; wait for it |
| F-17 | 2026-07-03 | turn.run appeared to hang forever (only progress/cot events, no final response) — suspected daemon bug | Test harness: `echo msg \| socat` half-closes the UNIX socket on stdin EOF; daemon sees EOF → TransportClosed → session torn down before the response is sent. Daemon was healthy (turn completes in ms with the correct actionable QB-unconfigured error) | Rule: RPC tests must hold the connection open — `(printf msg; sleep N) \| socat -t N`. Method names come from daemon.py `_METHODS` (`turn.run` with param `input`, `daemon.status`) — derive assertions from the dispatch table, never from memory |
| F-18 | 2026-07-03 | v3 QEMU gate: 14 failures incl. `version marker 'v2' != 'v3'`, "guest up after 15s" under TCG (impossible) | Stale QEMU from a prior gate held ssh port 2299; hostfwd failure is NON-fatal, so the new QEMU ran unreachable and ssh hit the orphaned v2 guest — gate tested the wrong ISO | Gate: pkill stale gate QEMUs + random ephemeral port per run + version-marker mismatch aborts immediately. Rule: identify the system under test before asserting anything about it |
| F-19 | 2026-07-03 | v4 UTM: ALL normal commands stopped executing (`ls` + Enter → nothing) — QEMU gate had passed | Trigger bound `\C-j` to a bind -x handler, destroying its default accept-line; Enter macro `"\C-j\n"` then never accepted any line. Gate's `bash -ic` check bypasses readline entirely so it couldn't see it. Second finding: bind -x handlers inside macros don't reliably see READLINE_LINE | Redesign: NO key bindings — `#` lines are natural bash comments; a PROMPT_COMMAND hook reads the newest history entry and routes `#` lines to ib_run.py. Gate: interactive checks must go through a real pty (`printf 'cmd\r' \| script -qec bash /dev/null`), with expected output computed at runtime so it can't match the input echo |
| F-20 | 2026-07-03 | V5 with-key turn failed with "**Quarantined** Brain not available: llama-server health probe failed" — user couldn't tell the Gemini parse had SUCCEEDED | `_UnconfiguredBackend` hardcoded "Quarantined Brain" in its error; the same class is reused as the PB stub, so PB-missing presented as a QB failure (R6: misleading > silent, but still wrong) | Stub takes `brain` + `remedy` params; PB stub now says "request WAS understood by the QB — only execution is unavailable (arrives in V6)" + model_registry install command. Rule: shared error paths must name the failing component, never bake one component's identity into a reusable class |
| F-21 | 2026-07-03 | "Cannot access Privileged Brain" for a month — pbd unit came up but PB was unreachable on every ISO | The `llama-server` we shipped was a **dynamically-linked** binary (17 KB thin exe referencing `libllama-server-impl.so` from the build tree). The .so was never installed into the chroot → binary crashed on start with unresolved symbols on the ISO, worked on the build VM | V6 builds a **static** llama-server (`-DBUILD_SHARED_LIBS=OFF`) at `cx-distro/.build/llama.cpp/build-static/bin/llama-server` (16 MB). Smoke gate L6 tripwire: `ldd` in-chroot fails on any "not found", and `llama-server --version` must exit 0 inside the chroot. Rule: any binary that isn't `mcpd`-simple must be verified with `ldd \| grep 'not found'` before it's allowed into an ISO |
| F-22 | 2026-07-04 | V6 first build: pbd crashed 1.4 s after start with `start-pbd: /etc/icebreaker/locations.env: Permission denied` (crash-loop restart-13) | V5's BP-8 hardening set locations.env to **0600 root:root**. V6's pbd runs as **`_icebreaker_pb`** — cannot read the env file → `source` fails → script exits 1 before reaching the model load. Two hardenings from different versions collided | Change locations.env to **0640 root:icebreaker-users** (both `_icebreaker_pb` and `_icebreaker_qb` are already in that group per sysusers.d). World-unreadable preserved. Rule: **any hardening that scopes access to a specific user must be validated against every OTHER user that consumes the same resource in later versions** — the smoke gate now asserts 640 + specific group, and the QEMU L5 check verifies owner+group not just mode |
| F-23 | 2026-07-04 | V6 second build: F-22 fix worked, but pbd still failed L6 — hung >900 s in `model_registry resolve` before llama-server ever launched | Full sha256 of the 940 MB model was being computed **twice** per start: once by `model_registry resolve` (INV-7), then a redundant second time by start-pbd. Under TCG without SHA-NI, one pass alone was ~10× slower than KVM; the doubled pass exceeded the gate timeout. Not a bug on real hardware, but wasted native boot time too | Remove start-pbd's redundant sha256 (resolve already enforced INV-7). Also raise L6 TCG timeout to 1500 s so headless CI has slack. Rule: any layer that already enforces an invariant is the sole owner of that check — re-checks must add either a *different* proof or a *cheaper* proof, never the same-cost duplicate |
| F-24 | 2026-07-04 | V6 booted on Apple Silicon UTM: pbd crash-loops with `code=dumped, status=4/ILL` after "binding to /run/icebreaker/pbd.sock". Automated GCP gate passed | The GCP build VM's CPU has AVX-512; `-DGGML_NATIVE=ON` (llama.cpp default) baked AVX-512 instructions into the static llama-server. Apple Silicon's x86-64 emulation (UTM/Rosetta 2) only supports up to AVX2 → SIGILL on the first vector op. Native GCP test never saw it; TCG on GCP happened to tolerate it | Rebuild static llama-server with `-DGGML_NATIVE=OFF -DGGML_AVX2=ON -DGGML_AVX512=OFF …` (safe baseline for Rosetta 2 / Apple Silicon UTM). Smoke gate: `objdump -d llama-server \| grep -q 'zmm[0-9]'` must return no matches. Rule: when you plan to boot on Apple Silicon, the build machine's native ISA is a **liability** — cross-compile targets, never the host |
| F-25 | 2026-07-04 | V6 UTM: pbd healthy, model loaded, socket answers, but NL turns fail at "Tool Call Generation" with `ReadTimeout: (read timeout=10)` | `pb_timeout_seconds = 10` in controller.toml is fine for native x86-64 (inference <5 s) but Apple Silicon's x86 emulation via Rosetta 2 runs a 1.5B Q4_K_M model at ~30–90 s/turn. Controller cancels the request while llama-server is still generating — `journalctl -u icebreaker-pbd` shows `slot launch_slot_… processing task` then `srv stop: cancel task` exactly 10 s later | Bump `pb_timeout_seconds` to 120 in shipped controller.toml. Rule: any timeout tuned for native x86 needs a 10–20× margin when the ISO is intended to run under Rosetta / QEMU / other emulation. Real hardware paths can lower it via `~/.config/icebreaker/controller.toml` layered override |
| F-26 | 2026-07-04 | `pb_timeout_seconds = 600` (my F-25 pt.3 fix) crashed the daemon at startup: `Fatal: controller config schema violation at ['run', 'pb_timeout_seconds']: 600 is greater than the maximum of 300`. Controller crash-looped; user saw F-7 daemon-unreachable warning | `controller_config.json` capped `pb_timeout_seconds` at 300 s — a defensive bound that made sense for native inference but was invisible when we bumped the config to 600. The daemon failed CLOSED on config validation (correct behavior, R6 visible), which surfaced as "controller can't start" instead of a silent stall | Raise schema max to 900 s (10× the emulation-turn median). Rule: when raising a config value, also verify against the config **schema** — validators fail closed, and the resulting crash-loop is upstream of every other component |
| F-27 | 2026-07-04 | PB always generated `/tmp` regardless of intent target — verifier caught mismatch but user couldn't complete any turn | `session.py:build_pb_user_turn` only passed `intent_id + allowed_tool + tool_schema` to PB. PB literally had no way to see the user's requested path and invented `/tmp` from training bias | Extend the signature with `target=""`, include it in the JSON payload when non-empty. `main.py:449, 1269` call sites pass `target=intent.get("target","")`. Per INV-1, the schema-**validated** target IS part of the structured Intent Object that flows to PB; only raw user text is forbidden |
| F-28 | 2026-07-04 | `fs.list /home/icebreaker` returned "Permission denied (os error 13)" — even after F-27 gave PB the correct path | Two-layer problem: (a) mcpd's `default_roots()` = STATIC + `$HOME`; but the controller daemon runs under systemd with **empty $HOME**, so mcpd falls back to `/` (userspace) and `/root` (Landlock kernel). Neither includes `/home/icebreaker`. (b) No config-driven way to add extra read roots without recompiling mcpd | Fast fix: `HOME=/home/icebreaker` in the shipped `locations.env` — inherited by controller, passed to mcpd via scrubbed env. Structural fix: `MCPD_FS_READ_ROOTS` env var (colon-separated absolute paths), read by both `fs.rs default_roots()` and `landlock.rs apply()`. Config-driven from `controller.toml [mcpd.fs] read_roots = ["/home/icebreaker"]`. Live-verified: `mcpd fs.list /home/icebreaker` returns count=18 |
| F-29 | 2026-07-04 | With F-28 fixed, `fs.list /tmp` returned `openat2: Function not implemented (os error 38)` — mcpd's TOCTOU-safe openat2 syscall not implemented by Rosetta 2's x86-64 emulation on Apple Silicon | Rosetta 2 doesn't implement openat2 (Linux kernel 5.6+ syscall with RESOLVE_BENEATH). Real Linux hardware has it since 2020; the emulated ISO doesn't | Runtime fallback: first ENOSYS latches `OPENAT2_UNAVAILABLE` and every subsequent call takes the canonicalize+starts_with fallback (same one macOS dev uses). Small TOCTOU window under emulation only; real Linux keeps full openat2 protection. `note_openat2_enosys()` logs a warn on the switch. Rule: any strict-mode Linux syscall must have a graceful ENOSYS fallback for emulator/older-kernel targets |
| F-30 | 2026-07-04 | With F-28/F-29 patched, `fs.list /home/icebreaker` still failed — mcpd returned `No such file or directory (os error 2)` for a directory that exists and is world-readable; then after a bind-mount attempt, mcpd was SIGSYS-killed (seccomp syscall 89 blocked) | Three-in-one: (1) systemd's `ProtectHome=yes` on `icebreaker-controller.service` makes `/home/*` mode 0000 in the controller's mount namespace — mcpd inherits and sees ENOENT. (2) When `HOME=/home/icebreaker` was set in the SHARED `locations.env`, pbd (running as `_icebreaker_pb`) inherited it and model_registry tried `~/.local/share/…/*.gguf` which pbd's user can't stat. (3) After `ProtectHome=tmpfs + BindReadOnlyPaths`, mcpd's canonicalize fallback (F-29) called `readlink()` which wasn't in mcpd's seccomp allowlist | Systemd unit: `ProtectHome=tmpfs`, `Environment=HOME=/home/icebreaker`, `BindReadOnlyPaths=/home/icebreaker` — controller unit only. `locations.env` NO longer sets HOME globally. `src/mcpd/src/sandbox/seccomp.rs`: added `SYS_readlink`. `controller.toml`: `mcpd_timeout_seconds = 120.0` for Rosetta headroom. **Rule: systemd hardening changes silently invalidate assumptions of every subprocess. Any `Protect*` directive must be paired with an explicit whitelist of what subprocesses ACTUALLY need to see — and shared env files must not carry per-service values** |
| F-31 | 2026-07-05 | V6.2 UTM: `# create a file "Hello.txt"` failed at Tool Execution with `open(write) failed: Read-only file system (os error 30)`. Intent parsed (Tier 1 auto-approved), tool call generated/validated/verified — the sandbox itself blocked the write | F-30's `BindReadOnlyPaths=/home/icebreaker` made the mount **read-only**, conflicting with `fs.write inside $HOME` (documented `risk_level=low` in every QB catalogue). Landlock also marks `$HOME` `AccessFs::from_read(abi)` — a second read-only layer. Both were belt-and-suspenders that clashed with the schema's own catalogue | `BindReadOnlyPaths=/home/icebreaker` → `BindPaths=/home/icebreaker` in the controller unit AND `AccessFs::from_read(abi)` → `AccessFs::from_all(abi)` in `landlock.rs` for `$HOME`. Security remains enforced by: validated intents, COW gate for SENSITIVE_HOME_SUBDIRS + writes outside home, HITL for higher tiers, MAX_WRITE_BYTES cap. **Rule: any hardening layer that *forbids* a documented supported operation is a misconfiguration, not defense in depth** |
| F-32 | 2026-07-05 | V6.2 UTM: query produced `fs.list target=/` → `'/' is not under any whitelisted root`. Stage 2 prompts explicitly forbid `/` and other placeholders, but Gemini emitted it anyway | Gemini violates the "NEVER emit `/`" rule when the query is too ambiguous even for context (or when context wasn't populated). mcpd correctly rejects `/` but the whitelist error is cryptic to the user | Belt + suspenders: (a) prompt-side — reinforce `NEVER emit target="/" or target=""` in all 4 qb_*.txt with fallback to `action=fs.list target=cwd`. (b) Controller-side guard in `main.py` — if action is path-requiring and target is empty/`/`, short-circuit with a friendly CoT card `"Query too ambiguous to route safely. Try naming a specific directory."` and skip PB/mcpd |
| F-33 | 2026-07-07 | V6.3 UTM: `# create a file "Hello.txt"` reached Tool Execution, then **mcpd crashed with SIGSYS in `__GI_fsync()`** (Ubuntu apport dialog). Controller saw `stdout closed (exit code -31) before sending a response`. Automated 29/29 QEMU gate passed; only UTM boot surfaced it | F-31 admitted `fs.write inside $HOME` at the mount + Landlock layers, but the seccomp allowlist in `src/mcpd/src/sandbox/seccomp.rs` never included `fsync`. mcpd's `safe_write` and `canonicalize_write` (Rosetta 2 fallback path) both call `file.sync_all()` → `fsync(2)` syscall → allowlist mismatch → `KillProcess` → SIGSYS. Neither the smoke gate nor QEMU gate exercised fs.write against the real seccomp filter; the discovery procedure in `seccomp.rs:187-193` existed as a comment but was never wired into CI | (a) Add `SYS_fsync` + `SYS_fdatasync` to `allowed_syscalls()` in seccomp.rs, plus loop-tested assertion in the unit test so a future drop fails `cargo test`. (b) New `incremental/build/mcpd-harvest.sh` two-phase gate: Phase A runs mcpd under `MCPD_SECCOMP_LOG_ONLY=1` and greps dmesg for `audit type=1326 syscall=N` — fails on any survivor; Phase B runs mcpd under the real allowlist and fails on SIGSYS or truncated response stream. Wired into `build-iso.sh` at the top so a missing syscall aborts the build before any cloud storage is touched. (c) `strings mcpd \| grep -q fsync` positive assertion added to smoke-gate L6. **Rule (R8): every filesystem-mutating tool must survive `MCPD_SECCOMP_LOG_ONLY=1` harvest before an ISO ships. The allowlist is measured surface, not an assumption.** |
| F-34 | 2026-07-07 | V6.4 UTM: `ib-debug snapshot` reported `PB llama-server /run/icebreaker/pbd.sock ! conn refused` for a fully healthy pbd — model loaded (2508 MB), socket bound (`ss -lnp` LISTEN), and `curl --unix-socket .../health` returning `200 OK`. Persisted for 5+ minutes after boot. Cosmetic but very confusing during the F-33 UTM verification | `ib_debug.py collect_socket()` sent a JSON-RPC `daemon.status` request to *every* socket in its probe list. That's the Controller protocol; pbd.sock is HTTP (llama.cpp OpenAI-compatible server). llama-server's HTTP parser saw garbage and closed the connection; the 2-second `sock.settimeout` tripped, `OSError` caught, `connectable=False`, rendered as "conn refused" | Add a `SOCKET_PROBES` map keyed by path → protocol (`jsonrpc` or `http`). pbd.sock → HTTP `GET /health`, any 2xx = healthy. Also bump the socket-probe timeout to 5 s to cover Rosetta 2 latency. Rule: heterogeneous services need heterogeneous probes — a diagnostic that speaks one protocol to endpoints of another is worse than no diagnostic |
| F-35 | 2026-07-07 | V6.4 UTM: `take me to the downloads folder` (a navigation intent) mapped to `fs.list` on `/home/icebreaker/Downloads`. CoT showed every step ✓ green ending with "The directory /home/icebreaker/Downloads is empty." The system executed *a* tool cleanly — but the user asked to *navigate*, not to *list*. Pipeline looked healthy while doing the wrong thing. Every automated gate green | QB prompt `qb_gemini.txt:18` was `Default to system.status when no tool matches`, combined with line 55 `if the query is genuinely too ambiguous, use action=fs.list target=cwd`. QB was *instructed* to force every query into some tool call — no escape valve for "I don't know how to do that." Intent JSON schema validates only syntax; F-32 guard only catches path-requiring actions with empty target — this had a concrete target. No golden-intent test suite existed to catch semantic regressions | (a) New mcpd tool `system.unsupported` (Tier 0 read-only echo, `src/mcpd/src/tools/system.rs` + `schemas/system.unsupported.json`). (b) All 4 QB prompts rewritten: killed the `Default to system.status` line, added UNSUPPORTED-INTENTS block + 3 worked examples (navigation, GUI, misc) per variant. (c) Controller `_SUPPORTED_ACTIONS` frozenset in `main.py:87-124`; short-circuit to `Outcome.UNSUPPORTED` via `_emit_unsupported` (streaming) / `_unsupported_result` (non-streaming) — never invokes PB or mcpd. (d) Yellow UNSUPPORTED CoT card via `styles.tcss.s-unsupported`. (e) Golden corpus at `tests/corpus/intent_corpus.json` (48 rows / 12 categories) + `test_intent_corpus.py` with offline (mock QB) + live (real Gemini) modes. (f) `build-iso.sh` runs the corpus test before ISO packaging — regression aborts the build. (g) `mcpd-harvest.sh` Phase C exercises `system.unsupported` through the sandbox. **Rule (R9): every user-visible intent maps to a real tool OR to `system.unsupported`. Silent lookalikes are worse than errors.** |
| F-36 | 2026-07-08 | v6.4 booted on an Intel i7-3820 (Sandy Bridge-E, 2012, in a VM with `hypervisor` flag). pbd crash-looped with `status=4/ILL` (SIGILL) 105+ times. Automated GCP gate + Apple Silicon UTM both passed — this CPU is 14 years old and outside the tested envelope. Downstream TUI symptom: "Privileged Brain not available: llama-server health probe failed at http+unix://localhost/health: FileNotFoundError(2, ...)" — socket never got bound | V6.4's llama-server is built portable-x86 (F-24 disabled AVX-512) but requires **AVX2** (Haswell 2013+). i7-3820 has AVX but not AVX2. Three rebuild attempts (SSE2-only via `-DGGML_*=OFF`; SSE2 + explicit `-march=x86-64 -mtune=generic`) ALL still crashed at the same instruction inside `std::vector<std::filesystem::path>::_M_realloc_insert` — even after full compiler-side ISA targeting. Root cause not fully diagnosed after 3 iterations. VM `hypervisor` flag suggests emulated-CPU quirks that make even -march=x86-64 code crash despite the machine advertising AVX in cpuinfo | **Deliberate scope decision:** don't spend more engineering effort optimizing for pre-Haswell CPUs. Support window = 13 years (Haswell 2013 → present); anything older gets a clear "AVX2 required" message from first-boot instead of crash-looping pbd. v6.51 ships with the existing AVX2 build (build-portable/bin/llama-server). Both primary test targets (Apple Silicon UTM via Rosetta 2, GCP VM native) have AVX2. **Rule (R10 revised): document the minimum CPU requirement (currently x86-64-v3 / Haswell / AVX2) in the ISO release notes and first-boot. Cap the "build for lowest denominator" ambition at a reasonable support window — not indefinite backward compatibility.** |
| F-37 | 2026-07-08 | V6.6 arm64 first build: xorriso failed at final ISO assembly with `grub-mkstandalone: /usr/lib/grub/arm64-efi/modinfo.sh doesn't exist` after >2 hours of debootstrap + pip install under emulation | The GCP build VM (amd64) only has `grub-efi-amd64-bin` installed. `grub-efi-arm64-bin` is arm64-only in Ubuntu's package pool — not in the amd64 archive. No pre-flight check for GRUB-target-arch existence, so the failure surfaced after the entire manifest chain had run | Multi-arch dpkg config: `dpkg --add-architecture arm64` + add `ports.ubuntu.com` sources with `Architectures: arm64` + `apt install grub-efi-arm64-bin:arm64`. Added F-37 fail-fast in `build-iso.sh` (line 68 area): `[ -f /usr/lib/grub/${GRUB_FORMAT}/modinfo.sh ] || die`. Rule: every cross-arch build tool must be verified present BEFORE the manifest chain starts |
| F-38 | 2026-07-08 | V6.6 arm64 ISO built, but boot in QEMU under cross-arch TCG hit `error: plain image kernel not supported - rebuild with CONFIG_(U)EFI_STUB enabled` in GRUB | Ubuntu ships arm64 `vmlinuz-*` as gzip-compressed. GRUB's arm64-efi `linux` loader needs a raw arm64 Image (`MZ` EFI magic byte header — the EFI stub). amd64 bzImage GRUB handles gzip transparently, so this ONLY bites cross-arch. Our `build-iso.sh` did `cp vmlinuz staging/live/` verbatim for both arches | Add `zcat` step for arm64 vmlinuz before staging (via `file` check that decompressed image is `Linux kernel ARM64 boot executable Image`). Fail hard if decompressed image doesn't match. Rule: kernel image format is arch-dependent; the copy step must include an arch-aware validation |
| F-39 | 2026-07-08 | V6.6 arm64 QEMU gate failed 5 checks despite the boot succeeding: `controller.sock missing after 90s`, `no RPC response`, `turn.run produced no final response` etc. — cascade from L2 socket wait timeout | Cross-arch TCG emulation is another 3-5× slower than same-arch TCG (which was already 5× slower than KVM). Under `-cpu cortex-a72` cross-arch, the controller daemon takes 120-180s to bind its socket. Our SOCK_MAX = 90s (adequate for same-arch TCG) and L4_TIMEOUT = 120s (adequate for same-arch TCG) both timed out on legitimate turns | `qemu-gate.sh`: bump SOCK_MAX from 90s → 300s and L4_TIMEOUT from 120s → 300s when `ARCH != $HOST_ARCH`. Rule: cross-arch emulation is not one 5× slower category — it's a separate 15-25× category with its own timeouts |
| F-40 | 2026-07-08 | V6.6 arm64 QEMU gate: controller startup logs showed `Fatal: failed to spawn mcpd: [Errno 8] Exec format error: '/usr/libexec/icebreaker/mcpd'` | `v2.manifest` was still hardcoded to install the unsuffixed `cx-distro/.build/mcpd` binary — which is amd64. On the arm64 ISO, the guest tried to `exec()` an amd64 ELF and got ENOEXEC. The multi-arch refactor updated `v6.manifest`'s HARVEST_MCPD path but forgot v2.manifest's install path | `v2.manifest`: switch to `cx-distro/.build/mcpd-${ARCH}` with legacy fallback for amd64. Add a `file(1)` cross-check: parse the description string and reject if it doesn't match target arch ("ARM aarch64" for arm64, "x86-64" for amd64). Rule: multi-arch refactors must audit every `cx-distro/.build/mcpd` reference — not just the ones grep found in the primary build script |
| F-41 | 2026-07-09 | V6.6 UTM: `# create a csv file with a column of ones called ones.csv` produced a file containing sample name/age columns instead of the requested `1\n1\n1\n1\n1\n`. PB fabricated stereotypical CSV content. Every user query that names literal file bytes hit this bug | QB prompt (`qb_gemini.txt`) instructed Gemini to leave `params: {}` empty and let PB generate content. PB (Qwen 2.5 finetune) had no examples in its training corpus of "user names literal content — pass it through", so it substituted stereotypical CSV / JSON / YAML samples from its training data. Bytes never reached disk from the user's query. INV-2 restricts `params` values to shell-safe primitives, so we couldn't just put content there | (a) Extend `dual-brain/controller/schemas/intent.json` with two new optional top-level fields: `content` (string, maxLength 65536, NUL-forbidden) and `pb_hint` (string, maxLength 2048, NUL-forbidden). Fields are payload not parameter — not shell-interpolated, so metachar-clean restrictions don't apply. (b) Rewrite `qb_gemini.txt` with a content-extraction rule + 5 worked examples (CSV, JSON, YAML, plain, poem). (c) Rewrite `pb.txt` with a trust-QB-hint section + passthrough rule + 6 fs.write examples. (d) `session.py::build_pb_user_turn` forwards `content` + `pb_hint` from the validated intent envelope. (e) 9 new corpus rows (`write.005`-`write.008` + `smalltalk.001-002` + `meta.001` + `delete.001-002`) with `expected_content` + `expected_pb_hint_contains` assertions. **Rule: the QB→PB channel is a rich envelope, not just an intent — content and coaching flow together** <br/>**regression lock (R14):** `corpus/intent_corpus.json` category=`write-content` + `broad-os.content.resume.001` + `broad-os.content.blog.001` |
| F-42 | 2026-07-09 | V6.6 UTM: `# hello`, `# take me to Downloads folder`, `# go to /var/log` all produced JSON-RPC error `code=-32603, message="name '_cot' is not defined"`. Every `system.unsupported` intent crashed with NameError | `_cot` is a nested closure inside `run_turn_streaming` (line 255) that captures `t0` from its enclosing scope. `_emit_unsupported` (line 2172) is a method on the same class but sits OUTSIDE that closure — it yielded `_cot(...)` events that Python resolved from module scope (nonexistent) instead of from the closure. The bug never fired in QEMU CI because the F-35 unsupported path was only exercised by the corpus test, not by the streaming pipeline | Add module-level `_make_cot(t0, name, state, body, **data)` helper in main.py at line 93. Change `_emit_unsupported` sites to `yield _make_cot(t0, ...)`. Leave the closure-based `_cot(...)` inside `run_turn_streaming` alone (~40 call sites, all in scope). Rule: nested closures called from method scope on the same class are a footgun — module-level helpers are the safer default when the code path branches across the class hierarchy <br/>**regression lock (R14):** `corpus/intent_corpus.json` categories `smalltalk-UNSUPPORTED` + `meta-UNSUPPORTED`, `test_intent_corpus.py::test_smalltalk_and_meta_rows_route_to_unsupported`, plus `test_f48_unsupported_cot_state.py` (which also locks the `_make_cot` helper's state validator) |
| F-43 | 2026-07-09 | V6.6 UTM: `# rm -rf /` produced JSON-RPC error `Schema validation failure at ['intent_id']: 'd9f0e1c2-b3a4-5678-9012-3456789abcdef0' is not a 'uuid'`. Model was emitting a placeholder UUID that failed uuid format (14-char last group instead of 12) | QB was instructed to emit `intent_id` as one of the intent JSON fields. Gemini 2.5 Flash memorized a placeholder value from its training data (the specific string `d9f0e1c2-b3a4-5678-9012-3456789abcdef0`) and echoed it on adversarial-style queries. Even a valid-shape UUID from the model would have been useless: the `intent_store.put()` call at line 574 generates its OWN opaque UUID and returns it — the model's value was never used for anything | In main.py right after `qb_response.content_json`, unconditionally overwrite: `raw_intent["intent_id"] = str(uuid.uuid4())`. QB is no longer asked to emit intent_id (dropped from qb_gemini.txt output shape). Rule: server-generated IDs must be generated server-side; any field the server owns must be discarded from the model's output <br/>**regression lock (R14):** `test_f43_f47b_intent_id_normalization.py::test_do_not_emit_uuid_overwritten_with_real_uuid`, `test_qb_output_with_do_not_emit_passes_post_normalize`, `test_normalize_call_site_present_in_streaming_path` |
| F-44 | 2026-07-09 | V6.6 UTM: `# how much disk space is left` and `# make a new folder called scratch` (and similar legitimate queries) intermittently rejected as `outcome=qb_verifier_rejected, reason="verifier call failed"`. Same query, different verdicts across sessions | Verifier is a single-shot Gemini call with a vague prompt ("verified: bool, reason: str") and no rubric. Under network flake, rate limit spike, or Gemini's occasional non-deterministic output, the single vote fails. No retry: the pipeline treats the single failed vote as authoritative rejection | (a) In main.py at the verifier call site (~line 678), wrap in single retry on `"verifier call failed"` string: one automatic re-vote before failing. Both attempts logged. (b) Rewrite `qb_verifier.txt` as a 7-check rubric with concrete examples of verified=true vs verified=false. **Rule: single-shot LLM decisions on the critical path should have at least one bounded retry — the model is not deterministic and the network isn't either** |
| F-45 | 2026-07-09 | V6.6 UTM: `# delete everything in home` triggered Tier 3 HITL — daemon crashed with `ValueError: signal only works in main thread of the main interpreter` instead of prompting. INV-6 (the human-gate for destructive ops) was **completely unenforceable** on any turn dispatched to an asyncio worker thread | `hitl.py::HitlPrompt.ask()` unconditionally called `signal.signal(SIGINT, handler)` to install a deny-on-Ctrl-C handler. Python's `signal.signal` raises ValueError from any thread other than the main interpreter's main thread. The daemon dispatches turns to asyncio worker threads, so every Tier 3 HITL prompt crashed. Every dangerous operation that reached HITL crashed the turn instead of prompting. Not caught in QEMU CI because the automated gates only exercised Tier 0/1 turns | Guard `signal.signal(...)` with `threading.current_thread() is threading.main_thread()`. On worker threads, skip installing the deny handler and rely on the user's explicit keyboard decision (Approve/Deny via keymap). Regression test in `test_hitl.py::test_ask_from_worker_thread_no_crash` runs `HitlPrompt.ask()` from a worker thread and asserts the full show/lockout/read flow completes. **Rule: any Python code on the daemon's turn-dispatch path must be safe to call from a worker thread — module-scope `signal.signal`, asyncio primitives with implicit event-loop binding, and Xlib/GTK are all off-limits from non-main threads** <br/>**regression lock (R14):** `test_hitl.py::test_ask_from_worker_thread_no_crash` |
| F-46 | 2026-07-09 | V6.6 UTM: `# find all python files in home` fell back to `fs.list /home/icebreaker` — the pattern filter was silently discarded and the user got 17 unrelated entries. Every gate green because "some tool executed cleanly" | Old QB prompt had no rubric for search/find intents; QB inferred that `fs.list` was the closest supported action and mapped to it. The lookalike-fallback that F-35 was supposed to eliminate had a blind spot for search. **Immediate fix landed in v6.61**: qb_gemini.txt now lists "Search/find operations (until `fs.find` ships)" in the UNSUPPORTED rubric. **Feature fix**: `fs.find` MCP tool deferred to Phase 7 M7.5 (nice-to-have surface expansion). Regression floor: 3 new corpus rows (`search.001-003`) + a dedicated test `test_search_rows_route_to_unsupported_until_fs_find_ships` — when `fs.find` lands, these rows re-classify to `expected_action=fs.find` and the test is renamed | (a) v6.61 QB prompt update (already landed). (b) Corpus rows + test to lock in the guardrail. (c) Phase 7 M7.5 will add `fs.find` MCP tool + schema + risk-tier declaration. Rule: intent gaps get a UNSUPPORTED corpus row + test even before the feature ships — that turns "silent lookalike" into "clean 'I can't do that' UX" |
| F-47b | 2026-07-10 | V6.61 UTM: `intent_id: 'DO_NOT_EMIT'` still leaking after the F-43 hotfix that overwrote `raw_intent["intent_id"]` before schema validation. Reproduced with `# take me to Downloads folder` and other queries | F-43 fix landed only in `run_turn_streaming()` at line ~345. The daemon has a SECOND entry point at `_run_turn_inner()` (non-streaming path used by direct socket RPC callers like `ib-debug` and some GUI clients) at line ~1299 that ALSO does `raw_intent = qb_response.content_json` — but had no override. When the terminal happened to dispatch via the streaming path, F-43 worked; when anything else dispatched via non-streaming, Gemini's placeholder UUIDs / `schema_version: "1.0"` leaked into validation | Extract the three overrides (intent_id, schema_version, timestamp) into a module-level helper `_normalize_server_owned_fields(raw_intent)`. Call from BOTH sites in main.py. Additionally: F-51 venv-integrity marker check (v2.manifest) requires `_normalize_server_owned_fields` to be present in the installed venv — cache-reseed can no longer silently ship a version without it. **Rule: when writing a bug fix, grep the codebase for all sites that share the same shape as the failing one. `git grep "raw_intent = "` would have caught this in 5 seconds** <br/>**regression lock (R14):** `test_f43_f47b_intent_id_normalization.py::test_normalize_call_site_present_in_non_streaming_path` + `::test_helper_called_before_validate_in_both_paths` (source-level grep on `main.py` for both call sites) |
| F-48 | 2026-07-10 | V6.6.1 UTM: every `system.unsupported` intent crashed the turn with `Error: step_state must be one of ['active', 'done', 'failed', 'pending'], got 'unsupported'`. The F-42 fix for the earlier `_cot` NameError was working — but I passed `step_state="unsupported"` to `_make_cot`, which fails `turn_events._COT_STEP_STATES` validation | The F-35 comment said "'unsupported' state → yellow CoT card via styles.tcss" — I read that literally and ported it. But `styles.tcss` keys off `step_name` (the CSS class `s-<step_name>`), NOT `step_state`. `step_state` is a state-machine value from `frozenset({"pending", "active", "done", "failed"})`. Passing anything else raises at CotEvent construction | Change `main.py:2263` from `_make_cot(t0, "unsupported", "unsupported", ...)` → `_make_cot(t0, "unsupported", "done", ...)`. step_name="unsupported" still triggers `.cot-card.s-unsupported` yellow styling. **Rule: bug-fix comments should name WHICH FIELD carries semantic weight. "'unsupported' state" is ambiguous between step_state and step_name — the F-48 fix comment explicitly says "step_name drives styling, step_state must be one of the valid four"** <br/>**regression lock (R14):** `test_f48_unsupported_cot_state.py` (whole module — parametrizes `_make_cot` over `_COT_STEP_STATES`; source-level check on `_emit_unsupported` still passing `"done"`) |
| F-49 | 2026-07-10 | V6.61 UTM: legitimate write intents like `create csv with column of ones` rejected as `outcome=qb_verifier_rejected, reason="verifier call failed"`. The F-44 single-shot retry ONLY fires on the exact substring "verifier call failed" — but Gemini's actual failure phrasing varies (timeouts, empty reason, malformed JSON) | The "right" retry strategy is genuinely workload-dependent. Retry-everything reduces false rejections but doubles Gemini cost on real bad intents. Retry-nothing is honest but visible to the user as flake. Skip-tier-01 trades defense-in-depth for latency. No one static choice is right for every deployment | Ship `verifier.retry_mode` as a runtime knob in controller.toml (enum: off, on_call_failed_only, on_any_rejection, skip_tier_01). Default = on_call_failed_only (F-44 behaviour). Dispatch in main.py via `_should_retry_verifier(mode, vresult)`. Expose in Control Center Behavior page for A/B testing. 19 offline unit tests cover the dispatch matrix. **Rule: when the "right" behaviour depends on empirical evidence from real users, expose it as config first, decide the default second** <br/>**regression lock (R14):** `test_verifier_retry_mode.py` (19 parametrized cases over the 4 modes) |
| F-50 | 2026-07-10 | V6.61 UTM: (a) first NL query after F2 works without `#` but subsequent queries need `#` again — sticky_nl not persistent as expected. (b) Icebreaker Control Center desktop icon does nothing when clicked — window never appears | (a) InputRouter state races the InputBar's visual `nl_mode` label — the label can show NL while router state has flipped, silently routing to shell. Users then re-type with `#` and it works, reinforcing the "you need `#`" mental model. (b) GTK4 apps on XFCE via UTM default to the OpenGL renderer (`GSK_RENDERER=ngl`) which requires DRI3. The XFCE-in-UTM stack doesn't have DRI3 → GTK4 initializes silently with an invisible surface. Process is alive; window renders nowhere | (a) `on_input_bar_submitted` in `terminal/app.py` gains a defensive cross-check: if `input_bar.nl_mode` visually indicates NL mode, force `target = NL` regardless of router state. Also strip any user-typed `#` on that path. Idempotent `#` prepend in `_run_nl_turn` as belt-and-suspenders. (b) Update `cx-distro/distro/icebreaker-control.desktop`: `Exec=env GSK_RENDERER=cairo GDK_BACKEND=x11 /opt/icebreaker/venv/bin/python3 -m gui.control`. Update v6.manifest regex assertion to accept the `env VAR=val…` prefix. Rule: user-visible UX regressions (mode label vs actual routing) trump internal state-machine purity — the label is the contract |
| F-51 | 2026-07-10 | V6.62 build silently shipped stale pre-hotfix Python code even though prompts, tests, and corpus files were current. Root cause: when I couldn't afford a fresh 55-min pip install, I `cp`d the pre-hotfix venv tarball to the new src_hash filename. The cache "hit" installed old code that lacked F-42/F-43 fixes; only the PKG-1 explicit copies of schemas/prompts/grammars survived | Cache-key was `src_hash = sha256(*.py + *.toml)`. Editing the source changed src_hash — a cache miss triggering a fresh pip install would have worked. But I manually renamed the OLD tarball to the NEW hash to save 55 min, making the cache appear valid. Nothing in the build pipeline verified the installed venv's content matched the source | Add a marker-check to v2.manifest AFTER v2 populates the venv. For each recent bugfix, an entry names (file, substring). If any substring is missing from the installed source, the build ABORTS with `FATAL[v2]: venv-integrity: marker ${_mkr} missing`. Currently guards F-41, F-42, F-43, F-45, F-47, F-48, F-49. Extending the marker list is a required part of any main.py bugfix commit. **Rule: build-time optimizations that skip verification must be provably safe. A rename-based cache reuse is not — always run at least a "did the fix I claim to ship survive the build" grep before considering the ISO shippable** |
| F-52 | 2026-07-10 | V6.63 UTM: `# whats in my home directory` produced `Schema validation failure at ['intent_id']: 'DO_NOT_EMIT' is not a 'uuid'` — F-43 fix (unconditional server-side intent_id regeneration) was in place, but Gemini's response_schema was still rejecting the model's placeholder value BEFORE the daemon-side normalize ran | Backend applies response_schema BEFORE returning content_json. The model would echo a placeholder from its training data (`DO_NOT_EMIT` on adversarial or edge queries), Gemini's schema validator rejected `required` + `format=uuid` on `intent_id`, the retry loop reissued the same prompt, memorised placeholder came back the same way — Gemini gave up and surfaced the raw failure BEFORE `_normalize_server_owned_fields` in `_run_turn_inner` could regenerate the field. F-47b (extracting the normalize helper) fixed the STREAM path; the non-streaming call path still ran the pre-F-47b logic | Introduce `_relax_server_owned_for_backend()` which produces a schema copy that drops `required` + `format` constraints on the three server-owned fields (`intent_id`, `schema_version`, `timestamp`). Backend uses the relaxed schema; downstream `_normalize_server_owned_fields` regenerates the values; the FULL schema validator runs AFTER normalize. Rule: server-owned fields are not the model's job — schema presented to the model must reflect what the model is actually responsible for, not what the audit row eventually contains <br/>**regression lock (R14):** `test_f52_backend_schema_relax.py` (whole module — asserts required + format/pattern dropped for the 3 server-owned fields; every OTHER field's format/pattern survives; input schema NOT mutated; strict `validate()` still catches missing/`DO_NOT_EMIT` values post-relax) |
| F-53 | 2026-07-10 | V6.63 UTM: verifier rejected legitimate writes with reason `verifier call failed: BrainTruncationError` — the specific exception type was invisible in the reason string, users saw only "verifier call failed" and had no path to fix it. Root cause was Gemini truncating the verifier response at `max_tokens=512` mid-JSON on longer intent chains | Two problems compounded: (1) verifier config shipped with a stale 512 max_tokens default that worked for tiny intents but not for the richer intent envelopes shipped in F-41 (content/pb_hint fields expand the verifier's user prompt); (2) all 12 `except Exception:` sites in the daemon swallowed the exception's type and message, surfacing generic strings like "verifier call failed" or "internal error" that gave users nowhere to look | Ship `_log_exception(logger, source, exc)` helper that writes `{source, exc_type, exc_message, traceback_hash}` to `SystemLogger` at every swallow site. Every `except Exception as exc:` in main.py and verifier.py surfaces `type(exc).__name__: {exc}` in the user-visible reason string. Optional `[dev] verbose_errors = true` opts into the full traceback. Also raise shipped `qb.gemini.max_tokens` 2048→8192 (v6.65 raises schema ceiling to 1M and exposes it in Control Center → Models). Rule: any user-visible error string that reads "X failed" without naming WHICH failure is a bug — silent-generic beats silent-empty by very little <br/>**regression lock (R14):** `test_f53_verifier_truncation_surface.py` (whole module — asserts `BrainTruncationError` reaches `VerifierResult.reason` in both SingleVerifier and MajorityVoter paths; caps at 400 chars; source-level check that `main.py` still surfaces `vresult.reason` into `TurnResult.output` at both entry points) |
| F-54 | 2026-07-10 | Pre-v6.65 pipeline treated 2048 max_tokens and 30s timeout as unchangeable magic numbers — v6.62 hard-coded a workaround for the specific verifier truncation, v6.63 raised one number to fix one query, v6.64 raised it again. Users trying broader OS-automation workloads (long-form drafts, multi-step planning, dense summaries) kept hitting new truncation-shaped failures with no user-facing knob | The design premise for v6.6 was "one-shot terminal + fixed tokens" — but the actual product is a broad AI-native OS where users bring arbitrary workloads. Every hard-coded number in `controller.toml` is a hidden dependency on the workload we happened to test with | v6.65 Scope A: raise schema ceilings across the board (max_tokens 32k→1M, timeouts 600s→7200s). Ship higher defaults (qb.gemini.max_tokens 8192, pb_max_tokens 1024, daemon.max_connections 4). Expose every knob in Control Center: Models (backend + model preset + Custom… override + tokens + timeout for both QB and PB), Limits (cost / rate / HITL), Behavior (verifier retry + fallback chain + auto-cancel on cost), Errors (recent SystemLogger exceptions + verbose toggle), Tools (mcpd inventory placeholder). Rule: every `2048`-shaped number surfacing in a UX regression is a config field the product should have exposed on day one — patch the surface, not the specific number <br/>**regression lock (R14):** `test_backend_fallback_edge_cases.py::test_chain_exhaustion_produces_enumerated_error` + Scope B backward-compat suite (`test_config_backward_compat.py`) + no-new-knobs gate G23 (prevents new hardcoded knobs from being reintroduced) |
| F-55 | 2026-07-11 | Phase 6 Scope F G24 UTM sweep at 08:24:39 UTC on `icebreaker@192.168.64.27`: Apport crash reporter fired repeatedly with `System program problem detected` — root cause `journalctl -u icebreaker-controller -n 100 --no-pager --output=cat` killed by SIGSYS. dmesg audit line: `pid=… comm="journalctl" sig=31 syscall=48 arch=c00000b7 code=0x80000000`. Every corpus row that routes through `service.logs` (`service.001` "show nginx logs", `service.002`, `broad-os.errors.001`) triggered it | arm64 syscall 48 is `faccessat`. mcpd's seccomp allowlist for arm64 doesn't list it; glibc's journalctl runtime calls `faccessat` when it checks journal-file permissions on `/var/log/journal/<machine-id>/`. Same F-33 pattern: allowlist authored against amd64 syscall numbers + strace transcript; arm64 has DIFFERENT syscall numbers (`faccessat`=48 vs amd64's 269 for the same operation) AND on arm64 glibc calls `faccessat` where amd64 glibc happens to call the older `access` (which IS in the allowlist by coincidence). QEMU gate uses cross-arch TCG which fakes the syscall table — mcpd-harvest.sh under TCG did not fire the SIGSYS | (a) Rebuild `mcpd-arm64` (Scope G is already scoped for this) with arm64 seccomp allowlist extended: `faccessat` (48) + `faccessat2` (439) + spot-check every glibc `access(2)`-flavored path used by service.logs / package.query / process.inspect. (b) F-33 harvest-gate expansion: `mcpd-harvest.sh` must run `service.logs journald` AND `service.logs nginx` under the real allowlist AND against BOTH arches (cross-run on the amd64 build VM would still miss arm64-specific syscall gaps — need a native arm64 run OR a per-arch allowlist audit). (c) F-51 marker: `F55-arm64-facc:src/mcpd/sandbox/seccomp.rs:faccessat` so a rebuild that drops the syscall is caught pre-ISO. **Rule: syscall allowlists MUST be authored per-arch, not shared. `strace` on amd64 tells you what amd64 does, not arm64. R8-arm64: every V ≥ 6.6 arch needs its own harvest-gate pass on native hardware or true-native emulation, not TCG.** <br/>**regression lock (R14):** Scope G rebuild + `mcpd-harvest.sh` extension (deferred to Scope G's PR) + F-51 marker `F55-arm64-facc` |
| F-56 | 2026-07-11 | Phase 6 Scope F G24 UTM sweep on `icebreaker@192.168.64.27`: 2 broad-OS rows produced daemon JSON-RPC error `-32603 name 'ResultEvent' is not defined`. Rows: `broad-os.gitlog.001` ("summarize the last 10 commits") and `broad-os.content.resume.001` ("rewrite my resume in professional tone"). Both target `fs.read` / `fs.write` with a resolved absolute path derived from `context.cwd`. `read.001` through `read.010` and `write.001` through `write.008` did NOT hit this — so it's NOT the generic streaming path | The v6.65 daemon on the guest is the shipped venv, which predates the current tree — so the exact call site is not visible in `main.py` HEAD. Speculation from the row shape: rows that FAIL schema validation early yield `ResultEvent` from `_schema_rejected` (line ~527 of current main.py), and if the deployed venv's `_schema_rejected` uses `ResultEvent` without a local import (it's imported at module top of the current tree but might be lazy-imported inside the streaming closure on the shipped venv), the fallback path from schema rejection would raise. This is the F-47b pattern all over — one path fixed, one path missed. Cannot confirm without the guest's `/opt/icebreaker/venv/lib/python3.12/site-packages/controller/main.py` traceback | (a) Verify the current tree's `_schema_rejected` doesn't ever raise NameError for `ResultEvent` (`git grep "ResultEvent" dual-brain/controller/main.py` — both function scope and imports). If the current tree has the correct import at every call site, F-56 auto-resolves on next ISO. (b) If the current tree is also vulnerable, add an explicit test in `test_f43_f47b_intent_id_normalization.py` that exercises the schema-rejected path and asserts no NameError. (c) F-51 marker: `F56-resev:main.py:ResultEvent` at module top. (d) Investigate on next Scope I ISO build against this guest — if the issue vanishes, the current tree is clean; if it recurs, we caught an active bug pre-ship. **Rule: G24 caught this because it exercises real code paths, not mocks. Every "silent lookalike" F-56-shape bug on the guest is a place my offline tests didn't step. Widening offline coverage from G24 findings is the R14 loop.** <br/>**regression lock (R14):** `test_f56_result_event_scope.py` (4 tests — behavioural end-to-end + source-level import guard + method-scope contract + circular-import canary). Landed under Scope G commit `4910ef5`. Confirmed root cause: NOT `_schema_rejected` — it's `_emit_unsupported` at `main.py:2466-2497` (a generator method that yields `ResultEvent`). `ResultEvent` was imported LOCAL to `run_turn_streaming` only; Python method scope doesn't inherit caller locals. Same shape as F-42 (`_cot` closure). Fix: local `from .turn_events import ResultEvent` at method top. Cannot promote to module top because `turn_events.py:13` mutually imports `TurnResult` from `.main`. F-51 marker `F56-emit-import:main.py:from .turn_events import ResultEvent` added to `v2.manifest`. |
| F-57 | 2026-07-12 | User boots the ISO for the first time and Icebreaker Terminal auto-launches full-screen BEFORE the user has entered API keys or opened the Control Center. Chatbot GUI also auto-launched on top of the terminal. Un-usable first-boot UX; the user has to close both windows and hunt for the Control Center in the app menu | Two `.desktop` files under `cx-distro/distro/` — `icebreaker-terminal-autostart.desktop` and `icebreaker-chatbot-autostart.desktop` — were installed under `/etc/xdg/autostart/` unconditionally by `cx-distro/build.sh:451-461` and `incremental/versions/v3.manifest:24-25`. The `X-GNOME-Autostart-enabled=true` line fires on every login (XFCE's XDG autostart honors GNOME's key). No first-boot gate; the intended "launch after user is configured" flow was never wired | (a) Delete both `.desktop` files (they had no legitimate user-facing purpose — the terminal + chatbot are already reachable via `/usr/share/applications/`). (b) Remove install blocks in `cx-distro/build.sh:451-461` and `incremental/versions/v3.manifest:24-25`. (c) `v3.manifest` gains a belt-and-braces `rm -f /etc/xdg/autostart/icebreaker-{terminal,chatbot}*.desktop` on ISO upgrade so a stale autostart from an older ISO gets cleaned. (d) `smoke-gate.sh` L6 gains an assertion that the two files are ABSENT from the chroot — future regressions caught pre-ship. (e) Scope G.5 first-boot notification tells the user what to open first ("Open the Icebreaker Control Center to add API keys") — replaces the terminal auto-launch as the first-boot signal. Rule: any UI that auto-launches at login MUST have a first-boot gate; unconditional autostart is a design smell. <br/>**regression lock (R14):** file absence + smoke-gate check; if either `.desktop` file is reintroduced under `cx-distro/distro/` OR under `/etc/xdg/autostart/` in the chroot, smoke-gate L6 fails. |
| F-68 | 2026-07-18 | v6.10 CT-scan: QB emits `gui.click` / `rpa.execute_workflow` correctly, but `main.py`'s F-35 floor rewrites them to `system.unsupported` before dispatch. Every "take a screenshot" / "run demo workflow" query silently disappears even though the GUI + RPA dispatchers have been alive since Phase 6T. Track K (2026-07-17) un-hid these tools in the QB catalogue block (prompt side) — half the fix; codebase-ct-scan agent found the F-35 floor still filtered them out. Two full subsystems (8 GUI + 4 RPA tools) sat unreachable in prod for weeks | `dual-brain/scripts/export_mcpd_catalogue.py::_emit_supported_actions()` filtered `provided_by ∈ {mcpd, controller, manifest}` — a mirror of the same stale assumption Track K fixed on the prompt side. Regenerated `_intent_corpus_supported.py` therefore excluded GUI + RPA from `SUPPORTED_ACTIONS`, and `main.py:271` imports that frozenset as its F-35 admission set. Fix on ONE side (visibility) without the other (admission) = feature still dark end-to-end | Widen `_emit_supported_actions()` filter to include `gui_agent + rpa_bridge`. Regenerate `_intent_corpus_supported.py` (24 → 36 actions). Update sister drift test `test_generated_supported_actions_agrees_with_yaml` to expect the wider set. **Anti-hide guard:** new `test_supported_actions_includes_gui_and_rpa` in `test_tool_catalogue_yaml.py` asserts every gui/rpa yaml entry lands in `SUPPORTED_ACTIONS`; fails loudly with F-68 reference if the filter re-narrows. **Rule: features gated by TWO cooperating floors (prompt visibility + F-35 admission) need BOTH halves shipped together — track them as one atomic unit in future refactors** <br/>**regression lock (R14):** `test_tool_catalogue_yaml.py::test_supported_actions_includes_gui_and_rpa` + `test_catalogue_block_has_gui_and_rpa_sections` (sister guard on prompt side) |
| F-69 | 2026-07-18 | v6.10 CT-scan: fresh installs shipped Phase 6T RPA entirely dark — even after F-68 admitted `rpa.execute_workflow` into the F-35 floor and QB emitted it correctly, every rpa.* call short-circuited with `outcome=disabled` because `RpaConfig.enabled` defaulted to `False` in `dual-brain/controller/config.py:343`. The whole Phase 6T RPA milestone was invisible to every user on default configuration | The default was set to False under an unstated BP-2 ("off by default") reading — but the RPA sandbox (`rpa_bridge/sandbox.py`) is the most permissive in the system BY DESIGN (Landlock + seccomp + `/dev/uinput`), specifically so RPA can be safe-by-default. Shipping enabled=False provided no security benefit, only hid the feature. The default outlived its rationale after Phase 6T shipped the sandbox as production-safe | Flip `RpaConfig.enabled` default to `True`; refresh JSON schema description. **Anti-hide guard:** `test_rpa_config_default_enabled` in `controller/tests/test_config.py` — constructs default `RpaConfig()` and asserts `.enabled is True` with an F-69 reference message pointing to the sandbox-safe rationale so a future dev doesn't loop back to False. Sweep update: `test_rpa_escalation.TestRpaConfig.test_defaults` also expects True, with a comment linking to the primary anti-hide guard. **Rule: an off-by-default flag that outlives its safety rationale is a hidden feature. If the sandbox is safe-by-design, the config default must reflect that** <br/>**regression lock (R14):** `test_config.py::test_rpa_config_default_enabled` |
| F-70 | 2026-07-18 | v6.10 CT-scan: `icebreaker-control.desktop` (Control Center) and `icebreaker-audit.desktop` (Audit Viewer) shipped in Phase 5 but never wired into `cx-distro/distro/xfce4-panel.xml`. Both GTK apps existed on the ISO but had no panel launcher — users had to open a terminal and run the binary by name, which they had no way to know. Two whole applications sat unreachable from the panel for weeks | XFCE panel launchers are configured explicitly in `xfce4-panel.xml`. Adding a `.desktop` under `distro/applications/` makes it appear in the applications menu but NOT on the panel. The panel-refresh commit for v5 wired chatbot + settings only, and the two later apps (Control Center + Audit Viewer) were never added. Same "built but not surfaced" pattern as F-68 | Add `plugin-9` (Control) + `plugin-10` (Audit) launcher blocks and insert them into the `plugin-ids` array between Settings and Terminal (natural user-mental-model ordering: Chatbot → Settings → Control → Audit → Terminal). **Anti-hide guard:** new `cx-distro/tests/test_panel_launchers.sh` — greps `xfce4-panel.xml` for every `icebreaker-*.desktop` we expect on the panel; fails loudly with F-70 reference if any get dropped. **Rule: every `.desktop` file we ship AND want on the panel needs a row in the anti-hide check. Applications-menu presence is not the same as panel presence** <br/>**regression lock (R14):** `cx-distro/tests/test_panel_launchers.sh` |
| F-71 | 2026-07-18 | v6.10 CT-scan: `src/mcpd/manifests/demo.uptime.yaml` — the Layer 2 Part B pilot manifest that proves the mcpd-side manifest loader end-to-end (parse → validate → register → dispatch through `impl_kinds::fs_read`) — was never listed in `tool_catalogue.yaml`. QB literally couldn't emit it. `"read /proc/uptime"` routed to `system.uptime` instead, and the Layer 2B pilot ran zero times in production despite its whole point being end-to-end proof | The Layer 2A commit registered `nav.cd` in `tool_catalogue.yaml` + `TOOL_CATEGORIES` at the same time as its manifest file. The Layer 2B commit shipped the mcpd-side manifest + loader + `fs_read` impl kind + `demo.uptime.yaml` — but forgot the corresponding controller catalogue row + `TOOL_CATEGORIES` entry. QB never saw the tool exist; the risk classifier + tier-0 fast path never learned its shape either. Same failure mode as Bug B / nav.cd (2026-07-17) — BP-5 escalate-only would have slapped Tier 3 on it if QB ever emitted it, but QB never got the chance | Add `demo.uptime` row to `tool_catalogue.yaml` (`provided_by: manifest`, tier 0, low risk) mirroring the `nav.cd` pattern. Add `"demo.uptime": "tier0"` to `TOOL_CATEGORIES` in `export_mcpd_catalogue.py`. Bump `EXPECTED_TOOL_COUNT` 23 → 24. Regenerate all 3 auto-artifacts. **Anti-hide guard:** `test_demo_uptime_in_supported_actions` in `test_tool_catalogue_yaml.py` asserts yaml row + frozenset entry both survive; fails loudly with F-71 reference if either drifts. Sweep update: `test_mcpd_catalogue::test_all_tools_count` bumped 23 → 24 with a comment naming demo.uptime. **Rule: shipping a new impl.kind manifest requires updating THREE files atomically (manifest YAML + tool_catalogue.yaml + TOOL_CATEGORIES). Missing any one = the tool exists but is unreachable** <br/>**regression lock (R14):** `test_tool_catalogue_yaml.py::test_demo_uptime_in_supported_actions` |
| F-72 | 2026-07-18 | v6.10 CT-scan: three v6.8-sprint helpers — `ib_debug_v68.py` (LangGraph pipeline debug), `ib_run_v68.py` (single-query driver), `ib_run_corpus.py` (in-guest 150-query corpus runner used by G24 gate) — shipped in `dual-brain/scripts/` but never installed to the ISO PATH by `cx-distro/build.sh`. For the entire v6.8 sprint, running the corpus in-guest required manually scp'ing the script from the host every time. The G24 gate was named `ib-run-corpus` in ci.sh comments and docs, but the binary was absent from every ISO cut | `build.sh` had explicit `install -Dm755` lines for `ib_debug.py` + `ib_bundle.py` but not for the three v6.8 helpers. They were added to the tree during a sprint that had no ISO rebuild, and the follow-up ISO cuts never re-audited the scripts/ directory. Same "built but not surfaced" pattern as F-68 / F-70 / F-71 | Add 3 `install -Dm755` lines + convenience symlinks (`ib-debug-v68`, `ib-run-v68`, `ib-run-corpus`) mirroring the ib_debug.py pattern. **Anti-hide guard:** new `cx-distro/tests/test_installed_binaries.sh` — greps `build.sh` for every debug/corpus helper we expect on the ISO; fails loudly with F-72 reference if any get dropped. **Rule: any script in `dual-brain/scripts/` meant for in-guest use needs both an install line in build.sh AND a row in the anti-hide check. "It's in git" is not the same as "it's on the ISO"** <br/>**regression lock (R14):** `cx-distro/tests/test_installed_binaries.sh` |
| F-73 | 2026-07-18 | v6.10 CT-scan: `main.py`'s two `GuiAgent(scratch_dir=…)` construction sites passed only ONE of `GuiConfig`'s four fields. `gui.prefer_app_api`, `gui.screenshot_retention`, and `gui.a11y_timeout_ms` were dead knobs — setting them in `/etc/icebreaker/config.toml` did nothing because the Agent constructor never saw them. `ScreenshotManager`'s eviction loop read the module-level `_MAX_RETAINED = 50` constant regardless of what the user configured | The Controller stores the loaded `GuiConfig` on `session_state.gui_cfg` but only threaded `screenshot_dir` through to `GuiAgent`. The other 3 fields lived in TOML → controller memory → nowhere. `_MAX_RETAINED` was the ScreenshotManager's only cap, so `gui.screenshot_retention = 10` did nothing. `prefer_app_api` defaulted to True at the constructor level, so `gui.prefer_app_api = false` didn't disable the app-API layer | Widen `GuiAgent.__init__` to accept `config: GuiConfig | None`. When provided, read all 4 fields from it; when absent, fall back to legacy scratch_dir=… defaults (backward compat for tests + ad-hoc scripts). Widen `ScreenshotManager.__init__` to accept `retention` keyword; store as `self._retention` (with `_MAX_RETAINED` class-level default for callers that bypass `__init__` via `__new__`). Both call sites in `main.py` (streaming + non-streaming) switched to `GuiAgent(config=gui_cfg)`. **Anti-hide guard:** new `gui_agent/tests/test_agent_config.py` — 4 tests pin each field reaching the Agent's internal state + one backward-compat guard so legacy constructions still work. Fails loudly with F-73 reference if the positional-only form is reintroduced. **Rule: partial config plumbing is worse than none — it looks configurable to the reviewer but silently ignores 3 of 4 knobs. When a config dataclass has N fields, ALL N must be threaded, or the config API is a lie** <br/>**regression lock (R14):** `gui_agent/tests/test_agent_config.py` |
| F-32-recur | 2026-07-20 | v6.10 UTM arm64: `# what's in this folder` → `Error: step 1/1 (fs.list): JSON-RPC error -32602: Invalid params: "" is shorter than 1 character at /path`. Regression of F-32 (2026-07-05) which shipped a two-layer fix (prompt + main.py::run_turn_streaming guard at line 623-643) that had worked cleanly through v6.8 | **Task #173 (2026-07-17) flipped `agent_graph.enabled: bool = True`** without porting the F-32 guard to `agent_graph_nodes.py::planner_node`. With AgentGraph as default pipeline, `run_turn_streaming` delegates to `run_via_agent_graph()` at ~line 504 and returns immediately — the F-32 guard at line 623 never fires. Empty target propagates through PB into mcpd's `minLength: 1` JSON Schema rejection. Same shape as every `main.py`-only invariant that got silently disabled by the flag flip | Port the F-32 guard into `agent_graph_nodes.py::planner_node`: after `normalize_planner_output()`, iterate every plan step and short-circuit any with `action in _PATH_REQUIRING_ACTIONS` AND `target in ("", "/")`. Import `_PATH_REQUIRING_ACTIONS` from main.py (no forking — drift protection). **Rule R16 (NEW): pipeline migrations must audit every invariant enforced by the old path — port each, OR write a regression test that fails on the new path without the port. No exceptions for "small config flag" changes when the flag redirects the primary flow.** <br/>**regression lock (R14):** `test_agent_graph_regressions.py::TestF32EmptyTargetGuardInAgentGraph` (4 tests: rejects empty target + slash target, accepts concrete target, ignores non-path actions) + F-51 marker `F32-recur-agent:agent_graph_nodes.py:F-32` |
| F-35-recur | 2026-07-20 | v6.10 UTM arm64: `# create a text file with my ip address` (compound intent) → CoT showed Verifier ✗ but pipeline continued → `Error: tool_call.tool is missing, cannot match intent.action 'system.unsupported'`. The error message originates in `qb_verifier.txt:22` rubric #1 (verbatim string match `tool_call.tool == intent.action`). Regression of F-35 (2026-07-07) — worked through v8 monolithic path via `_emit_unsupported` short-circuit | Same as F-32-recur root cause: Task #173 flipped `agent_graph.enabled=True` without porting the `system.unsupported` short-circuit into `agent_graph_nodes.py::planner_node`. When QB defaulted to `system.unsupported` (compound intent QB couldn't decompose), the intent flowed to verifier which fired rubric #1 because there's no `tool_call.tool` — verifier is designed to run AFTER PB generates a tool_call. Monolithic path's `_emit_unsupported` never reached verifier at all | Port the F-35 short-circuit to `agent_graph_nodes.py::planner_node`: after normalize + F-32 guard, if `plan[0]["action"] == "system.unsupported"` return `{intent_valid: False, error_kind: "unsupported", output: <friendly-narrative>, completed: True}` — friendly narrative reads from `params.requested_intent` + `params.suggestion` so responder has something to render <br/>**regression lock (R14):** `test_agent_graph_regressions.py::TestF35SystemUnsupportedShortCircuitInAgentGraph` (2 tests: short-circuit completed=True, downstream nodes not touched) + F-51 marker `F35-recur-agent:agent_graph_nodes.py:F-35` |
| F-74 | 2026-07-20 | v6.10 UTM arm64: `# create a file "Hello World.txt"` and `# what's my IP address` → CoT shows every step green ending in `executed`, file IS created on disk, but terminal LEFT pane shows nothing (only stale errors from prior turns). New bug (not a v6.8 regression per se — the streaming path just moved). Also worked correctly through v6.8 via the monolithic pipeline's qb_summarize token stream | Task #173's AgentGraph responder_node populates `state["output"]` with the summary but the bridge (`agent_graph_bridge.py::translate_outcome_to_events`) emits only ProgressEvent + CotEvent + ResultEvent — NO TokenEvent. Terminal's `_on_token_event` handler (terminal/app.py:194-208) is the LEFT-pane streaming sink; without a TokenEvent, the pane stays empty even though ResultEvent carries the output correctly | In `translate_outcome_to_events()`, before yielding `ResultEvent`, emit exactly one `TokenEvent(token=output, accumulated=output, final=True)` — but ONLY when there's a non-empty output AND no error_kind (error path uses ErrorEvent). Three-line change; keeps the terminal's existing streaming render path intact so both TUI + GUI clients get output through the same channel they always did <br/>**regression lock (R14):** `test_agent_graph_regressions.py::TestF74BridgeEmitsFinalTokenEventInAgentGraph` (2 tests: success emits final TokenEvent before ResultEvent + text matches; error path does NOT emit TokenEvent) + F-51 marker `F74-token-agent:agent_graph_bridge.py:F-74` |
| F-49-recur | 2026-07-20 | Audit-identified dormant regression from same Task #173 flip (not yet UTM-confirmed but likely to surface on load). F-49 (v6.65, 2026-07-10) shipped `verifier.retry_mode` config knob + `_should_retry_verifier(mode, vresult)` helper so single-shot verifier rejection on `"verifier call failed"` network flake gets one automatic retry per config's mode | AgentGraph `verifier_node` calls `collab["verifier"].verify()` once and treats a `False` result as authoritative. No retry logic. So under Gemini flake, legitimate writes intermittently rejected — the exact symptom F-49 fixed on the monolithic path | Mirror monolithic retry: after first `.verify()`, if `not vresult.verified` AND `_should_retry_verifier(retry_mode, vresult)` → single retry. Extract the call into a helper closure so both attempts share the exception path <br/>**regression lock (R14):** `test_agent_graph_regressions.py::TestF49VerifierRetryInAgentGraph` (2 tests: retries once on call_failed when mode=on; does NOT retry when mode=off) + F-51 marker `F49-recur-agent:agent_graph_nodes.py:_should_retry_verifier` |
| F-43-recur | 2026-07-20 | Audit-identified dormant regression from same Task #173 flip. F-43 (v6.6, 2026-07-09) + F-47b (v6.61, 2026-07-10) fixed the QB-placeholder-intent_id leak: `_normalize_server_owned_fields(raw_intent)` overwrites `intent_id`, `schema_version`, `timestamp` with authoritative server values. F-47b applied it to both the streaming AND non-streaming call sites in main.py | AgentGraph `planner_node` never calls `_normalize_server_owned_fields`. For Task #148 multi-step plans, step 2+ carries QB's placeholder `intent_id` (e.g. `"DO_NOT_EMIT"`) and fails validation on that step | Iterate `plan` after `normalize_planner_output` and call `_normalize_server_owned_fields` on each step (idempotent — safe on step 0 too, giving F-43/F-47b coverage for AgentGraph even in the single-step case). Import from main.py <br/>**regression lock (R14):** `test_agent_graph_regressions.py::TestF43NormalizeServerOwnedFieldsOnAllPlanSteps::test_planner_normalizes_intent_id_on_all_plan_steps` + F-51 marker `F43-recur-agent:agent_graph_nodes.py:_normalize_server_owned_fields` |
| F-82 | 2026-07-21 | v6.11 arm64 on-guest diagnostic: `/var/log/icebreaker/controller-audit.log` open by daemon (`lsof` shows `fd=3w`) but 0 bytes across ~12 hours of active turns. INV-8 silently violated on every AgentGraph turn. Symptom went unnoticed for ~4 days because no automated check verified audit populates under AgentGraph — the earlier ISO cycles used screenshots to triage failures which is exactly the failure mode INV-8 exists to prevent | Task #173 flipped `agent_graph.enabled=True` without porting the 12+ `self._audit.write_fields(AuditFields(...))` sites from `main.py::run_turn_streaming` into the AgentGraph nodes. `agent_graph_nodes.py` + `agent_graph_bridge.py` had **zero** write_fields calls. Same root cause as F-32-recur / F-35-recur / F-49-recur / F-43-recur / F-74 — invariants enforced only in the monolithic path silently stopped applying when the flag flipped | Add terminal `audit_writer_node` in `agent_graph_nodes.py` that reads state + collab, maps `state.error_kind` → `Outcome` enum via `_outcome_from_state`, and calls `collab["audit"].write_fields(AuditFields(...))` mirroring the exact shape at `main.py:711`. Wire into `agent_graph.py`: every terminal edge (planner-short-circuit / verifier-reject / hitl-deny / executor-error / mcpd-error / responder-success / unsupported) routes to `audit_writer` before `END`. Exception in `write_fields` swallowed with `_log_exception` — BP-10 (audit failure must never break turn delivery). `run()` seeds `t0_monotonic` in initial state so the audit row carries duration_ms. **Rule R16 extension:** every INV-* invariant needs an active regression test on both paths of any dual-path config flag, not just the default path. If Task #173 had added `test_audit_writes_under_agent_graph` this bug would have been caught before the ISO shipped <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestAuditWriteFires` (5 tests: success, planner-reject, hitl-deny, unsupported, exception-does-not-break-turn) + F-51 markers `Faudit-agent:agent_graph_nodes.py:audit_writer_node` + `Faudit-wire:agent_graph.py:audit_writer` |
| F-83 | 2026-07-21 | v6.11 arm64 on-guest observation: CoT panel renders all-green (executed) even for turns that short-circuited at planner (F-32 empty target) or hitl (deny) — downstream nodes shown as "done" though they never ran. Users see "quickly passes through CoT to executed but I don't get a result" and can't tell whether the executor / mcpd / responder actually ran. Direct cousin of F-48 (v6.63) which fixed the SAME class of bug on the monolithic path (step_state "unsupported" rendered as "done"); the AgentGraph pipeline recreated it via a different mechanism | `agent_graph_bridge.py::_traversed_nodes_from_outcome` inferred which nodes ran from outcome fields — only special-cased `error_kind ∈ {planner, pb, mcpd, internal}`, so `schema` (F-32 reject), `unsupported` (F-35 short-circuit), `hitl` and `verifier` all fell through the "append every downstream node" branch. `_node_owned_error` used a 5-key map so unrecognised error_kinds rendered failed nodes as "done" green. Inference is fundamentally the wrong shape for short-circuits — no amount of extending the maps can represent "planner ran, then execution stopped" without knowing where execution actually stopped. BP-6 violation: bridge was making display decisions from LLM-authored `error_kind` string instead of structured facts | Add `visited_nodes: Annotated[list, add]` to `GraphState`. Each node's return goes through a `_mark(name, done|failed, update)` helper that appends `[(name, state)]` — LangGraph's reducer accumulates across the traversal. Bridge's new `_visited_from_state` reads the explicit list and hands `(node_name, node_state)` pairs to `_emit_progress_and_cot` — no inference. `_legacy_traversal_inference` retained as fallback for pre-v6.12 outcome shapes (mostly hand-built test fixtures) and emits a warning log when it fires so real coverage gaps in the shipped path surface. `_traversed_nodes_from_outcome` kept as thin backward-compat shim so 23 existing bridge tests keep passing without a mass rewrite. `_as_outcome` threads `visited_nodes` into the outcome dict so the bridge has the data. **Reinforces BP-6:** security-and-UX decisions come from structured facts (the visited list the nodes actually mutated), not from inference on model-authored fields (error_kind, tier, hitl_decision) <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestVisitedNodesAccuracy` (6 tests: planner-reject marks only planner failed, hitl-deny marks hitl_gate failed, responder success marks done, bridge consumes list verbatim, CotEvent stream shows failed state, CotEvent stream contains only visited nodes) + F-51 markers `FcotV-state:agent_graph_state.py:visited_nodes` + `FcotV-bridge:agent_graph_bridge.py:_visited_from_state` |
| F-84 | 2026-07-21 | v6.11 arm64 live Stage E: after `# take me to Downloads` (nav.cd succeeded + friendly message rendered), the next turn `# what's in this folder` fs.list'd `/home/icebreaker`, not Downloads. Audit row confirmed `action=fs.list, target=/home/icebreaker`. The whole point of the v6.9 Scope O Layer 2A nav.cd manifest (F-60 permanent fix) — that subsequent turns' "here" / bare filenames resolve against the last navigated dir — was broken at the plumbing seam. Session state was mutating; the QB prompt just wasn't reading it | `SessionState.session_cwd` was declared at `session.py:189-195` with a docstring promising `ShellContext.render()` would fall back to it when self.cwd was empty. But `render()` at `session.py:88-116` only ever read `self.cwd` — the per-turn shell $PWD from the RPC. The fallback code path never existed. Every nav.cd APPEARED to succeed (audit + friendly card) because the manifest impl mutated `session.session_cwd`, but the next turn's context envelope silently threw that away | `ShellContext.render()` accepts `override_cwd` kwarg — when non-empty, replaces `self.cwd` in the rendered `cwd:` line. `main.py::_build_qb_input` reads `session.session_cwd` via getattr (defensive for test doubles) and passes it as `render_kwargs["override_cwd"]` when non-empty. Both `run_turn_streaming` and `_run_turn_inner` go through `_build_qb_input` so one change covers both call sites. Backward-compat: `override_cwd` defaults to `""`, so every existing caller keeps old behavior. Only sessions that ran through nav.cd see the new behavior — exactly the audience nav.cd was supposed to serve <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestSessionCwdOverridesShellContext` (3 tests: override wins over self.cwd; empty override preserves self.cwd; _build_qb_input threads session.session_cwd end-to-end) + F-51 marker `F-D-nav-scwd:session.py:override_cwd` |
| F-85 | 2026-07-21 | v6.11 arm64 live Stage E: `# create /tmp/probe.txt` (Tier 3 write outside $HOME) → audit outcome `qb_verifier_rejected`. Same shape for every Tier ≥ 2 turn — fs.write outside $HOME, package.install, service.start/stop/restart. Every non-read-only intent through the AgentGraph pipeline was silently unreachable. The audit row was the first time we could see it because the audit gap (F-82) hid it before v6.12 | Task #146 laid out the graph as `risk_classifier → (tier≥2) verifier → hitl_gate → executor → mcpd`, putting verifier BEFORE executor. But `qb_verifier.txt` rubric #1 requires `tool_call.tool == intent.action` verbatim — and executor is what generates tool_call. `verifier_node` at line 349 passed `tool_call={}` with a comment acknowledging the shape mismatch ("we don't have a tool_call yet"), but the prompt was never softened. Every rubric #1 check failed with `tool_call.tool is missing, cannot match intent.action` — the exact error string qb_verifier.txt:22 emits when tool_call is empty | Reorder graph edges to v6.7 semantics — verifier runs on the actual tool_call PB produces: `after_risk` always returns `"executor"`; `after_executor` returns `"verifier"` when tier ≥ 2, `"mcpd_dispatcher"` otherwise; `after_hitl` approve returns `"mcpd_dispatcher"` (executor already ran). `verifier_node` reads `tool_call = collab["turn_content"][state["tool_call_hash"]]` and passes the real dict into `verify()`. `agent_graph.py::_build_graph` route tables updated for the new edges. Multi-step plan loop preserved (after_mcpd's step-index bump routes back to executor, which then goes through verifier → hitl_gate → mcpd for step N+1 — HITL fires per-step for tier ≥ 2 plans, matching v6.7). **Rule reinforcement:** if a prompt's rubric requires field X and the code passes `X=""`, either the code is calling the prompt at the wrong point in the pipeline or the prompt needs to explicitly handle absent X. Silent field mismatch is a design smell <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestVerifierRunsAfterExecutor` (4 tests: after_risk always returns executor; tier ≥ 2 after_executor returns verifier; tier < 2 goes straight to mcpd; verifier_node reads real tool_call from turn_content) + updated `test_after_risk_routes_by_tier` parametrization + updated `test_tier2_pauses/deny` PB call-count assertions + F-51 marker `F-E-verifier-order:agent_graph_nodes.py:after_executor` |
| F-86 | 2026-07-21 | v6.11 arm64 live Stage E: `# whatever nonsense query` produced a valid friendly F-35 card in state.output but the shell trigger rendered it as `[error] F-35: system.unsupported short-circuit`. Same failure mode for F-32 empty-target rejections once Fix D wired context flow — F-32 has a friendly rejection message too. Both are semantically soft outcomes (user asked a coherent question, got a coherent answer) but the render path treated them as hard errors | `agent_graph_bridge.py::translate_outcome_to_events` emitted `ErrorEvent` for ANY `error_kind`. daemon.py maps `ErrorEvent → make_error → JsonRpcError`, and ib_run.py falls into the `else` branch of its response handler and prints `[error] {msg}` in red. The bridge also emitted TokenEvent + ResultEvent for the same turn, but the client's send_request only reads the first response by msg_id — the ErrorEvent's make_error response arrived first, so the success payload was ignored | Introduce `_SOFT_ERROR_KINDS = frozenset({"unsupported", "schema"})` at bridge module top. `translate_outcome_to_events`: skip `ErrorEvent` for soft kinds, still emit `TokenEvent` when output is non-empty (soft kinds have valid text). `_outcome_to_result`: soft kinds return `TurnResult(success=True, output=<friendly>, outcome=Outcome.UNSUPPORTED | Outcome.SCHEMA_REJECTED)`. `planner_node` F-32 branch now populates `state["output"]` with the friendly rejection message so TokenEvent has something to stream. Hard error kinds (planner/pb/mcpd/verifier/internal) unchanged — they still get ErrorEvent because the LEFT pane genuinely needs the red banner <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestSoftOutcomesRenderAsSuccess` (5 tests: bridge skips ErrorEvent for unsupported + schema; bridge STILL emits ErrorEvent for hard kinds; _outcome_to_result marks soft kinds success=True with correct Outcome enum; F-32 branch populates output) + F-51 marker `F-F-soft-outcomes:agent_graph_bridge.py:_SOFT_ERROR_KINDS` |
| F-87 | 2026-07-21 | v6.11 arm64 live Stage E user question: after `# take me to Downloads`, plain `ls` still showed home dir contents. nav.cd (F-60 permanent fix) had always updated Icebreaker's SessionState.session_cwd so subsequent QB context resolution knew about it (once F-84 wired that in), but bash's `$PWD` was untouched — the daemon runs as a separate root process on its own UNIX socket and can't reach into the parent shell's env. Users reasonably expect "take me to X" to move the shell too | The shell trigger `ib_trigger.bash` had no channel back from `ib_run.py` for daemon-side session state changes. `ib_run.py` printed the response's output to stdout and exited; bash's cwd remained the shell's original `$PWD`. Existing daemon response fields (output, outcome, tier, cost, tokens) carried no session_cwd | Signal-file dropbox pattern. `daemon.py::_handle_run_turn`: every ResultEvent response now includes `session_cwd` populated from `session.session_state.session_cwd` via getattr (zero cost for non-nav turns, just empty string). `ib_run.py`: reads `IB_CD_SIGNAL` env var; if set AND response's session_cwd differs from shell's `$PWD`, writes the target to the signal file. Best-effort — OSError swallowed. `ib_trigger.bash`: mktemp per turn to `/tmp/ib_cd.XXXXXX`, export `IB_CD_SIGNAL`, after `ib_run.py` returns validate signal file contents via `[[ =~ ^/[A-Za-z0-9_./~-]+$ ]]` AND `[ -d "$target" ]`, then `cd`. Read + delete. Security: `/tmp` file with user umask, no secrets pass through (only an absolute path already validated by nav.cd's manifest impl on the daemon side); regex + dir-existence check on the bash side are defense-in-depth <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestNavCdSyncsShell` (2 tests: ib_run signals when session_cwd differs from shell cwd; skips signal write when equal — no wasted cd on non-nav turns) + F-51 marker `F-G-cd-sync:ib_run.py:IB_CD_SIGNAL` |
| F-88 | 2026-07-22 | v6.11 arm64 live Stage E2 daemon crash: SIGTRAP inside libglib-2.0 when `# list my open windows` triggered `gui.get_window_list`. Ubuntu apport caught the daemon process death. Same failure mode blocks every gui.* + rpa.* call through the AgentGraph pipeline. Fix I initial (2026-07-21, in-thread `from gui_agent.agent import GuiAgent` in `mcpd_dispatcher_node`) exposed the bug — pre-Fix-I those calls hit mcpd's Rust dispatcher which returned "Method not found" without touching pyatspi | pyatspi (transitively imported by `gui_agent.agent`) lazy-initializes GLib. GLib requires a running GMainLoop in the thread that owns AT-SPI objects. The daemon's turn-worker thread has no mainloop, so the first enumeration call reaches into libglib-2.0 and aborts. Same shape for rpa.* (RpaBridge shells out to Robot Framework which touches GLib for logging setup) | Subprocess isolation: NEW `controller/gui_worker.py` module reads a JSON envelope from stdin, dispatches to `GuiAgent.handle_request` or `RpaBridge.handle_request` in a fresh Python process (fresh interpreter → fresh GLib → mainloop init is safe), writes envelope to stdout. Exit code always 0. `mcpd_dispatcher_node`'s gui.* / rpa.* branch spawns the worker via `subprocess.run([sys.executable, "-m", "controller.gui_worker"], input=json, capture_output=True, timeout=10)`. Handles TimeoutExpired, spawn failure, non-zero exit, unparseable stdout — all mapped to `error_kind="mcpd"`. Costs ~100-200ms per gui.*/rpa.* call. **Future-faster strategies documented for v6.13+:** (a) persistent GUI worker daemon (long-running subprocess, UNIX socket IPC, GLib mainloop init'd once, ~5-10ms/call; trade-off = adds a supervised child to the systemd tree), (b) direct D-Bus AT-SPI via dbus-python (bypass pyatspi, talk to `org.a11y.atspi.Registry` on session bus; trade-off = reimplements pyatspi ergonomics), (c) daemon-side GLib mainloop thread (`GLib.MainLoop().run()` in dedicated thread, submit via `GLib.idle_add`; trade-off = pyatspi + threading is historically fragile). Pick based on measured latency after ISO ships <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestGuiRpaDispatchInAgentGraph` (6 tests: subprocess called for gui.*, subprocess called for rpa.*, timeout → error_kind=mcpd, non-zero exit → error_kind=mcpd, gui disabled config → friendly error, non-gui/rpa still to mcpd) + F-51 markers `F-Iplus-subproc:agent_graph_nodes.py:controller.gui_worker` + `F-Iplus-worker:gui_worker.py:gui_worker: unknown kind` |
| F-89 | 2026-07-22 | Same root cause + fix as F-88, applied symmetrically to rpa.* dispatch. Kept as a separate row for `git grep F-89` narrative continuity — see F-88 for the full story. RPA path uses `rpa_bridge.bridge.RpaBridge` instead of `gui_agent.agent.GuiAgent`; subprocess kind field is `"rpa"` <br/>**regression lock (R14):** shared with F-88 (`TestGuiRpaDispatchInAgentGraph::test_rpa_dispatch_calls_subprocess_not_mcpd`) |
| F-90 | 2026-07-22 | v6.11 arm64 TUI: `# create a html page explaining text to speech system with clean CSS` failed after 78 SECONDS with `BrainTruncationError: JSON decode failure: unterminated object`. Deterministic on any fs.write that generates non-trivial content (HTML, JSON documents, code files with more than ~40 lines). Live-verified on UTM guest .34 turn 28 | QB (Gemini) correctly generated `intent.content = "<200 lines of HTML+CSS>"` per the v6.61 F-41 passthrough pattern. But `executor_node`'s `try_fast_path` gate at `tier0_fast_path.py:114` rejected the intent because fs.write is tier ≥ 1, not tier 0. Fell through to PB (local Qwen fine-tune). PB has to re-emit those bytes inside its output JSON `{"tool": "fs.write", "params": {"content": "..."}}` — and blew past its output token budget mid-content-string. Truncated JSON → `BrainTruncationError` after 78s of wasted inference | Extend `try_fast_path` to short-circuit fs.write when `intent.content` is a non-empty string, at ALL tiers (not just 0). The constructed tool_call is deterministic content passthrough; PB adds nothing but truncation risk. Runs BEFORE the `if tier != 0: return None` gate so tier 1 ($HOME writes) and tier 3 (writes outside $HOME) both benefit. Safety invariants preserved: INV-2 (schema) — tool_call still runs through `validate_resolved_intent` before executor marks it valid; INV-6 (COW) — tier ≥ 2 fs.write still routes through hitl_gate → mcpd, and mcpd's fs.write handler still applies COW dry-run + user approval; INV-8 (audit) — unchanged. **General principle (extends R16):** PB should only be called when it has real work to do. When the tool_call is derivable from intent + a shape mapping, skip PB — cheaper AND more reliable <br/>**regression lock (R14):** `test_agentgraph_audit_and_cot.py::TestFsWriteContentPassthrough` (3 tests: fs.write with content fast-paths at every tier; fs.write without content falls through to PB; fs.write not in TIER0_FAST_PATH_TOOLS mapper dict — matches only the new branch) + F-51 marker `F-J-content-pass:tier0_fast_path.py:F-90 2026-07-22` |
| F-91 | 2026-07-27 | v6.13_OC arm64 live UTM boot: `systemctl status icebreaker-controller` showed `ActiveState=activating`, `SubState=auto-restart`, restart counter at 521 (~43 minutes of continuous crash-loop). `/run/icebreaker/controller.sock` never appeared, every `#`-trigger and every GUI client hit `[Errno 2] No such file or directory`. `journalctl -u icebreaker-controller` revealed the sequence: `WARNING controller.oc_audit_bridge: cannot mkdir /var/log/mcpd: [Errno 30] Read-only file system` followed by `Fatal: [Errno 2] No such file or directory` and exit code 1, then `RestartSec=5` respawn. Missed by every existing test + gate — no test called `OCAuditBridge.start()` against a nonexistent parent + no smoke gate asserted the shipped systemd unit granted the write path the bridge needed. Silent regression from Fix R (2026-07-25): Fix R landed the bridge, Fix Q wired it into `_maybe_start_oc_bridge`, but no one added `/var/log/mcpd` to the service unit's LogsDirectory/ReadWritePaths OR made the bridge robust to its absence | Two layered defects. (a) `dual-brain/controller/systemd/icebreaker-controller.service` had `ProtectSystem=strict` + `ReadWritePaths=/run/icebreaker` + `ReadWritePaths=/var/log/icebreaker` + `LogsDirectory=icebreaker` — `/var/log/mcpd` never got created, systemd's private mount namespace also masked it read-only if it had. (b) `oc_audit_bridge.py::start()` caught the `mkdir` `OSError` on line 245-248 with a comment "we still try to start" but the subsequent `Observer.schedule(path=parent, recursive=False)` at line 279 called into watchdog's `inotify_add_watch(...)` which raises `FileNotFoundError` when the watched path doesn't exist. That exception unwound through `_maybe_start_oc_bridge` → `_run_daemon` → the outer try/except at `__main__.py:369-371` which printed the "Fatal:" line and exit(1). BP-10 violation: an OPTIONAL enrichment bridge was allowed to take down the primary daemon | Two commits ship together. (a) `oc_audit_bridge.py::start()`: after the `mkdir` attempt, add explicit `if not parent.exists(): _log.error(...); return` early-out with an operator-actionable remediation message naming the unit-file fix AND the config kill switch (`[qb.opencode_oc] audit_bridge_enabled=false`). Wrap `Observer()` + `schedule()` + `start()` in a `try/except (OSError, RuntimeError)` so runtime watchdog failures (inotify exhaustion, backend swap under qemu-user, permission race) also degrade quietly instead of propagating. Bridge sets `self._observer = None` in the degraded path so `stop()` remains idempotent no-op. (b) `dual-brain/controller/systemd/icebreaker-controller.service`: `LogsDirectory=icebreaker` → `LogsDirectory=icebreaker mcpd`. Systemd creates + owns `/var/log/mcpd` at unit start AND adds it to the service's writable mount namespace, so the bridge now actually watches a real path in the shipped config. Harmless in the current-edition daemon (dir sits empty when the bridge is inactive) — no branching required by ISO edition. **Rule reinforcement (R18 + BP-10):** an audit sink failure never propagates past the sink boundary; the caller sees a returning function and a log line, never an exception. Fix Q approved Fix R without exercising the "parent dir missing" path — every optional-enrichment component needs an "environment missing" regression test in its own test file <br/>**regression lock (R14):** `test_oc_audit_bridge.py::test_bridge_start_returns_normally_when_parent_missing` + `::test_bridge_start_returns_normally_when_schedule_fails` (Observer.schedule raises OSError under monkeypatch) + `test_systemd_units.py::test_controller_has_logs_directory` (checks LogsDirectory includes both `icebreaker` and `mcpd`) + F-51 marker `F-91-bridge-degrade:oc_audit_bridge.py:F-91` + smoke-gate L2 assertion `grep -qE '^LogsDirectory=.*\\bmcpd\\b' ${CHROOT}/etc/systemd/system/icebreaker-controller.service` (blocks a future edit that removes `mcpd` from the LogsDirectory list) |
| F-92 | 2026-07-27 | Same UTM session as F-91/93/94/95: after F-91 unblocked the daemon crash-loop, ANY planner-path RPC into the daemon in OC mode returns a wall-of-text `[error] opencode_oc backend has no in-process LLM. If you see this, the Controller is trying to invoke QB from a code path that should have been gated on cfg.qb.name != 'opencode_oc'.` Live verified via `/opt/icebreaker/venv/bin/python3 /usr/share/icebreaker/shell/ib_run.py 'what time is it'`. Reaches every entry: shell `#`-trigger, GUI chatbot, audit viewer replay | `_maybe_start_oc_bridge` (Fix Q, 2026-07-25) registered `opencode_oc` as a NoOp BrainBackend + started the audit bridge, but no one gated the actual planner entry points. `main.py::run_turn` and `main.py::run_turn_streaming` both feed straight into `_run_turn_inner` (legacy path) or `run_via_agent_graph` → `agent_graph_nodes._run` (LangGraph path), both of which call `collab["qb"].complete(...)`. NoOp's `_call_provider` raises RuntimeError with the exact "should have been gated on cfg.qb.name != 'opencode_oc'" message → the message was accurate diagnosis but pointing at code that didn't exist yet | Two-line branch at top of BOTH entry points: `if self._is_opencode_oc(): return self._opencode_oc_short_circuit(session, t0)`. Short-circuit builds a soft `TurnResult(success=True, outcome=UNSUPPORTED, backend="opencode_oc")` with the message "This OC-edition build routes AI turns through opencode, not the Python controller. Launch the Icebreaker AI Terminal from the app menu, or run `icebreaker-oc` from any shell." Streaming variant yields TokenEvent(final=True) + ResultEvent so shell/GUI render it as text, not `[error]` (F-86 soft-outcome pattern). Writes INV-8 audit row per R18 discipline. Negative test guards against the gate firing for non-opencode_oc backends <br/>**regression lock (R14):** `test_config_opencode_oc.py::test_run_turn_short_circuits_in_oc_edition` + `::test_run_turn_streaming_short_circuits_in_oc_edition` + `::test_run_turn_does_NOT_short_circuit_when_qb_is_gemini` (negative) + F-51 marker `F-92-oc-gate:main.py:_is_opencode_oc` |
| F-93 | 2026-07-27 | Same UTM session as F-91/94/95: running `icebreaker-oc` (from any shell OR the app menu) printed opencode's help text and exited 0 — TUI never started. Repro-confirmed against opencode 1.18.4 arm64 built into the OC ISO. From user POV (via app menu), a terminal window flashed open with "opencode" branding and closed. Same symptom string user filed as "keeps crashing" | Launcher's exec line was `exec opencode --config "${OC_CONFIG}" "$@"`. opencode 1.18.4 does NOT accept a `--config` CLI flag. yargs default behavior on unknown flag: print help + exit 0 (not error). The launcher's own docstring said "opencode reads its config from $OPENCODE_CONFIG or the CLI --config flag" — the `--config` half was aspirational, only the env-var half is real. Live verified: `opencode` bare = TUI starts; `OPENCODE_CONFIG=/etc/icebreaker/qb_oc.json opencode` = TUI starts with "Claude Opus 4.7 · Anthropic" model + "1 MCP /status" indicator; `opencode --config /path opencode` = help output + exit 0 | Set `OPENCODE_CONFIG` env var + drop the `--config` flag. `export OPENCODE_CONFIG="${OC_CONFIG}"` + `exec opencode "$@"`. Rule extension for BP-1 / R5: when a wrapper passes CLI args to a pinned external tool, the wrapper's test suite MUST include a live invocation against the pinned version (`opencode 1.18.4`), not just a shellcheck lint — this bug would have been caught by `opencode --help \| grep -F -- --config` in the smoke gate |
| F-94 | 2026-07-27 | Same UTM session as F-91/93/95: opencode.desktop launcher (`Exec=gnome-terminal --title="..." -- icebreaker-oc`) closes the terminal window the instant `icebreaker-oc` exits. When the launcher exits with a friendly error (no API key, config path missing) or opencode itself errors on first turn, the message is on screen for < 200 ms — user reads it as "the app crashed." Reported to me as "icebreaker terminal keeps crashing." Combined with F-93/95 this is what turned a debuggable configuration problem into a phantom crash | .desktop `Exec=` runs the target directly; when the target exits, gnome-terminal has no reason to stay open. Standard UX bug in GUI-terminal launchers | Wrap the launcher in bash: `Exec=gnome-terminal ... -- bash -c 'icebreaker-oc "$@"; ec=$?; if [ "$ec" -ne 0 ]; then echo; echo "[icebreaker-oc exited with code $ec]"; read -p "Press Enter to close…" _; fi' _`. Successful exits still close cleanly; failures pause with a labelled prompt. Also gain: launcher preflight (F-95) that emits an actionable "Fix: sudo ib-setup-key --env-var ANTHROPIC_API_KEY" message BEFORE opencode is even spawned, so the pause window carries something a user can act on |
| F-95 | 2026-07-27 | v6.13_OC arm64 live UTM test 2026-07-27 (during F-91 follow-through): the `icebreaker-oc` launcher's `if [ -f /etc/icebreaker/env.d/qb_oc.conf ]; then . "${OC_ENV_FILE}"; fi` targeted a path that no component in the shipped ISO ever creates. `ib-setup-key --env-var ANTHROPIC_API_KEY` writes to `/etc/icebreaker/locations.env` (source and PATH have been correct for 3 months). Result: even after user runs `ib-setup-key`, the launcher sources nothing, opencode boots with no keys, first turn dies with `Error: Anthropic API key is missing.` — and gnome-terminal was closing so fast the user only saw the app "crash" | Two source-of-truth files diverged during Fix P design: the launcher docstring names `env.d/qb_oc.conf` (an idea for per-launcher env files that was never fleshed out), while ib-setup-key writes to the shared `locations.env`. Pattern that survives: BP-1 = one env-var registry, one write path. Two paths for the same purpose is a rendezvous bug waiting to happen | icebreaker-oc launcher `OC_ENV_FILE` default changed from `/etc/icebreaker/env.d/qb_oc.conf` → `/etc/icebreaker/locations.env` (same file `ib-setup-key` writes). Rule: any bash launcher that consumes API keys sources ONLY /etc/icebreaker/locations.env. Bundle with F-93 + F-94 as a single launcher-fix commit — user-visible symptoms cluster |
| F-96 | 2026-07-27 | v6.13_OC arm64 UTM preflight sweep (during F-94 hot-patch verification): F-94 fix (commit 8e7e024) inlined `bash -c '…; ec=$?; [ non-zero ] && read'` into the .desktop `Exec=` key. Live `desktop-file-validate` returned 3 errors: reserved `'` outside quote, non-escaped `$` inside quote, reserved `;` outside quote. gnome-terminal was tolerant enough to execute it (so user's original menu click would have "worked"), but stricter launchers (KDE plasmashell, older gnome-shell, xdg-launch) refuse malformed Exec= — this would have been an XDG-compliance regression the moment we tested on a non-GNOME desktop | XDG spec has its own quoting rules that don't match shell syntax. Reserved characters `' " \ > < ~ | & ; $ * ? # ( ) \`` must be either backslash-escaped inside a specific string-quote form or avoided entirely. Long bash payloads inside Exec= are structurally hostile to this constraint — inline complex logic will always be one edge case away from a validation error | Move the bash pause-on-error logic into `/usr/bin/icebreaker-oc-terminal` helper script. `.desktop` Exec= becomes trivially spec-compliant: `gnome-terminal --title="Icebreaker AI Terminal" -- icebreaker-oc-terminal`. Payload is identical (call icebreaker-oc, pause on non-zero exit). Also lands smoke-gate regression guards: `check_exec /usr/bin/icebreaker-oc-terminal` + grep-fail for `exec opencode --config` (blocks F-93 revert) + grep-pass for `OPENCODE_CONFIG` (blocks F-93 env-var export removal). The existing L3 `desktop-file-validate` on all `icebreaker-*.desktop` would have caught F-96 pre-ship — F-96 only surfaced because F-94's file was hot-patched to the guest AFTER the build stage. Rule extension for BP-13: `desktop-file-validate` must run on any .desktop file EDITED post-build (hot-patch), not just at build time — automate this in a helper "hot-patch verify" script <br/>**regression lock (R14):** smoke-gate L3 `desktop-file-validate /usr/share/applications/icebreaker-*.desktop` + smoke-gate L3 `check_exec /usr/bin/icebreaker-oc-terminal` + F-51 marker `F-96-desktop-helper:cx-distro/distro/icebreaker-oc-terminal:F-96` |
| F-97 | 2026-07-27 | v6.13_OC arm64 UTM preflight decision: OC ISO defaulted to `anthropic/claude-opus-4-7`, requiring users to hold an Anthropic API key just to try the AI Terminal on a fresh boot. Users who already went through `sudo ib-setup-key` (with the sensible default of `GEMINI_API_KEY`) had no working provider in OC mode | Design decision from Fix P (2026-07-24) picked Anthropic as the "premium first impression." Made sense in isolation but ignored the huge friction of introducing a second API key on the same ISO users already configured Gemini on. Also uncovered: opencode 1.18.4's Google provider needs `GOOGLE_GENERATIVE_AI_API_KEY` env var — NOT `GEMINI_API_KEY` — live-verified via binary strings + isolated env-var probe (with GEMINI_API_KEY only → "Google Generative AI API key is missing. Pass it using the 'apiKey' parameter or the GOOGLE_GENERATIVE_AI_API_KEY environment variable"; with the alias set → real Gemini turn returned "OK") | Two coupled changes: (a) `cx-distro/distro/qb_oc.json.template` model default → `google/gemini-2.5-flash`; (b) `cx-distro/distro/icebreaker-oc` launcher exports `GOOGLE_GENERATIVE_AI_API_KEY := GEMINI_API_KEY` if the source is set and the target is not. `ib-setup-key --env-var GEMINI_API_KEY` remains the documented user-facing setup command (BP-1 = one env-var name per provider from the user's perspective); the launcher handles the naming bridge internally. Anthropic + OpenAI providers already use their canonical env-var names — no alias needed there. Preflight message under the new default correctly reads "Required: GEMINI_API_KEY" (no leak of the internal alias) <br/>**regression lock (R14):** live-verified end-to-end on guest: after F-97 landed, `icebreaker-oc run --auto "call icebreaker_system.uptime"` completed successfully with Gemini 2.5 Flash and produced a valid mcpd audit row + INV-8 enrichment. Additional smoke-gate assertion: `grep -q '"model": "google/gemini-2.5-flash"' cx-distro/distro/qb_oc.json.template` (blocks accidental revert to Anthropic default in a future edit) + `grep -q 'GOOGLE_GENERATIVE_AI_API_KEY' /usr/bin/icebreaker-oc` (in shipped ISO smoke gate). Rule for future model bumps: any change to `qb_oc.json.template.model` needs a live-invocation test against the pinned opencode version, not just a shellcheck lint |
| F-98 | 2026-07-27 | v6.13_OC arm64 UTM preflight sweep: qb_oc.json's permission map was silently ignored by opencode 1.18.4. Screenshots from the GUI TUI confirmed the primary UX was broken — user typed `icebreaker_system.uptime` and got an empty response, $0.00 cost, 0 tokens in context (opencode denied the tool call silently, model produced no visible text). Same symptom for any MCP tool call from the menu-clicked launcher. NOT non-blocking as originally thought — this is a shipped-ISO regression that makes the AI Terminal appear "up but useless" | opencode 1.18.4's `permission` field is a NESTED-OBJECT shape: `{"<kind>": {"<pattern>": "<action>"}}` where `<kind>` ∈ `{mcp, bash, edit, question, plan_enter, plan_exit}`. Our `gen-oc-config.sh` emitted the FLAT-OBJECT shape `{"icebreaker_<tool>": "<action>"}` per Fix P's design memo (2026-07-24) — the memo predated the 1.18.4 schema and was never re-verified against the shipped binary. Two intermediate hypotheses (bare string, array of {permission, pattern, action} objects) also failed — the array shape gave the diagnostic error `Expected PermissionActionConfig \| object, got [...] permission` which named the correct schema | `incremental/build/gen-oc-config.sh` emits `permissions = {"mcp": {"<pattern>": "<action>"}}` instead of `permissions = {"<pattern>": "<action>"}`. One-line nesting change; all other emit logic (tier-based mapping, ALWAYS_ASK overrides) unchanged. **Live-verified fix**: with the nested shape shipped, `opencode run "You must call the icebreaker_system.uptime MCP tool right now"` returns the raw tool output verbatim: `{"uptime_human":"5h 9m","uptime_seconds":18543}`. Without --auto, without any permission prompt — opencode reads the config, sees the entry allowed, calls the tool <br/>**regression lock (R14):** smoke-gate assertion in the built qb_oc.json: `.permission` is an object AND `.permission.mcp` is an object AND at least one entry keys `icebreaker_<something>` with value in `{"allow","ask","deny"}`. Rule extension for BP-1 / R5: any wrapper that emits config for a pinned external tool must include a live-invocation test that PARSES what the tool actually sees — reading opencode's own session-log `permission=` field or hitting a test tool with a probe prompt would have caught F-98 immediately. Fix P's memo-only design pass was the process failure |
| F-99 | 2026-07-27 | v6.13_OC arm64 UTM preflight sweep: INV-8 audit enrichment for OC edition was silently broken. `opencode run --auto "call icebreaker_system.uptime"` completed successfully end-to-end (mcpd returned 12435 seconds, opencode summarized to user) but `/var/log/mcpd/audit.log` was never created and `/var/log/icebreaker/controller-audit.log` carried zero bridge-enriched rows. Silent because oc_audit_bridge's start() logs "watching X, offset=0" whether or not writes ever land. Would have shipped invisible: no fs.write test on the guest exercised the mcpd audit path with real user perms | F-91 Commit 2 added `LogsDirectory=mcpd` to icebreaker-controller.service; systemd creates /var/log/mcpd owned root:root mode 0755 at service start. But mcpd in OC edition is spawned by opencode (running as user icebreaker uid=1000 group icebreaker-users), NOT by the controller service. User couldn't write to root-owned 0755. mcpd's audit writes silently failed (mcpd's own stderr showed "audit mutex poisoned"). oc_audit_bridge's watchdog on /var/log/mcpd caught inotify events on the directory (IN_OPEN, IN_CLOSE_NOWRITE) but the target file /var/log/mcpd/audit.log never appeared, so the bridge had nothing to enrich | Add tmpfiles.d entry: `d /var/log/mcpd 2775 root icebreaker-users -` in `dual-brain/controller/systemd/icebreaker.tmpfiles.d.conf`. tmpfiles.d runs at boot BEFORE any service starts → dir exists with root:icebreaker-users mode 2775 (setgid) → LogsDirectory=mcpd in the controller unit becomes a no-op (dir already present with the perms we want). Setgid bit ensures new files inherit `icebreaker-users` group. mcpd (as user in icebreaker-users) writes fine; controller (as root) writes fine; oc_audit_bridge (as root) reads fine. **Verified end-to-end on guest**: after chgrp+chmod applied manually, `opencode run --auto` produced 11 mcpd audit rows AND 11 matching oc_audit_bridge-enriched INV-8 rows with hash chain intact (session_id=`opencode-oc`, source=`oc_audit_bridge`, mcpd_result_class=`ok`) <br/>**regression lock (R14):** live-verify test — build an OC-edition ISO, boot, run one `opencode run --auto` turn, assert `/var/log/mcpd/audit.log` size > 0 AND `/var/log/icebreaker/controller-audit.log` contains at least one row with `source=oc_audit_bridge`. Track as follow-up smoke-gate assertion (chroot can't test dir perms cleanly, needs QEMU boot). Also F-51 marker `F-99-mcpd-audit-perms:dual-brain/controller/systemd/icebreaker.tmpfiles.d.conf` |
| F-100 | 2026-07-27 | v6.13_OC arm64 UTM preflight sweep (post-F-98 discovery): opencode ships two disjoint tool sets in the same TUI — native (`write`, `edit`, `bash`, `read`, `list`, `patch`) and MCP (`icebreaker_*`). Gemini in opencode prefers the native tools for common tasks (screenshot 2: user asked "create Hello World.txt", model called opencode's `Write` tool with path `/Hello World.txt` — got hit by the kernel's permission denial on `/`). Native tools bypass EVERY Icebreaker security invariant: no tier classification, no COW dry-run (INV-6), no HITL modal, no INV-8 audit chain, no mcpd Landlock/seccomp sandbox. User's observation: "we were able to give permission to write to root folder in previous versions, why don't we have that now" — the tier-2 HITL approval flow was silently unreachable in OC edition because native `write` intercepted the intent | opencode's config schema exposes permission KINDS for the native tools: `edit` (covers native `write`, `edit`, `patch`), `bash` (shell exec), `read` (file read — usually allow). Fix Q's original design assumed model would naturally reach for MCP tools because they're name-prefixed `icebreaker_*` — but Claude/Gemini SDKs bias toward the shorter native names since those are canonical in the AI SDK's built-in prompt scaffolding. No enforcement mechanism was wired to make MCP tools the exclusive path | Set `permission = {"mcp": {…}, "edit": "deny", "bash": "deny"}` in gen-oc-config.sh output. Model is forced to use `icebreaker_fs.write` for writes (routes through mcpd → tier gate → COW → HITL if tier ≥ 2 → INV-8 via oc_audit_bridge). Live-verified on guest: with `edit: deny` alone (bash allow), Gemini called `icebreaker_fs_write` for a "create file X with content Y" prompt — mcpd audit recorded the tool call. With bash denied too, users can't ask opencode to `run ls` — they use gnome-terminal (already open running icebreaker-oc). AI Terminal's role in OC edition = NL → mcpd, not shell. **Variance note:** Gemini sometimes gives up silently in headless `opencode run` when native tools are denied (empty response, no tool call). Interactive TUI users get opencode's approval prompt for `ask` entries + can approve. If strict policy proves too restrictive in real use, revisit as F-100.1 <br/>**regression lock (R14):** smoke-gate L3 assert `permission.edit == "deny"` AND `permission.bash == "deny"` in shipped qb_oc.json. F-51 marker `F-100-mcp-only:incremental/build/gen-oc-config.sh:F-100`. Live-verify test post-rebuild: menu-click TUI, ask "create /tmp/opencode/probe.txt with content X" — must see either `icebreaker_fs.write` tool call fire (with ask prompt for tier-3 paths) OR opencode refusing native tools. Must NOT see `⚙ Write` in the response |
| F-101 | 2026-07-27 | v6.13_OC arm64 UTM live-guest observation (user prompt "what are my open windows" + "take a screenshot"): Gemini in opencode replied "I can only interact with the file system and execute shell commands" — GUI + RPA capabilities entirely unreachable in OC edition. Real regression from current edition where the controller's `mcpd_dispatcher_node` (Fix I+/F-88) intercepts gui.*/rpa.* intents before mcpd and dispatches to an isolated `gui_worker.py` subprocess. In OC edition opencode talks to mcpd directly via MCP protocol; the Python controller isn't in the tool-call path; and mcpd (Rust) has ZERO gui.* or rpa.* tools of its own | Fix P's original OC-edition design shipped ONE MCP server (`icebreaker` = mcpd) — that server exposes 23 tools from `fs/network/package/process/service/system` categories, no gui, no rpa. The 8 gui.* + 4 rpa.* capabilities that make current-edition users' automation flows work never had a transport into opencode's tool space. Fix Q added the audit bridge for what mcpd DID emit, but the bridge doesn't create tools — it only enriches audit rows for tool calls that already happened | Ship a SECOND MCP server named `iceui` (short, no dash-munging risk vs `icebreaker-gui`) alongside `icebreaker`. New module `dual-brain/controller/mcp_gui_server.py` (~270 LOC) hand-rolls the MCP JSON-RPC 2.0 protocol (no `mcp` Python package in the venv per recon — mcpd's own Rust does the equivalent in ~300 LOC of server.rs) with 4 handlers: `initialize`, `notifications/initialized`, `tools/list`, `tools/call`. Tool catalog assembled verbatim from `gui_agent.protocol._PARAM_SCHEMAS` + `rpa_bridge.protocol._PARAM_SCHEMAS` (no schema duplication). `tools/call` delegates to the existing `agent_graph_nodes._dispatch_in_subprocess` helper so pyatspi's GLib mainloop reentrancy is handled the same way current-edition does it (Fix I+/F-88 subprocess isolation pattern). Wrapper `/usr/libexec/icebreaker/gui-mcp-for-oc.sh` mirrors `mcpd-for-oc.sh` (sources locations.env, F-97 GEMINI→GOOGLE alias, execs `python -m controller.mcp_gui_server`). `qb_oc.json.template` gets a second `mcp` entry keyed `iceui`. `gen-oc-config.sh` extended to import both protocol schemas and emit 12 additional `iceui_*` permission entries (gui.ping/gui.get_window_list/rpa.ping/rpa.list_workflows → allow; rest → ask). New udev rule `/etc/udev/rules.d/10-uinput.rules` grants group `input` access to `/dev/uinput` so RPA keyboard/mouse synthesis works; build-iso.sh adds `usermod -aG input icebreaker`. `packages-desktop.txt` adds `python3-pyatspi`, `python3-pil`, `python3-numpy`, `python3-robotframework`, `python3-libseccomp` (one-time ~60min base-cache rebuild). Two prereq bug fixes ship first: (Prereq 1) `RpaBridge.__init__` accepts `config=` kwarg — gui_worker's dispatch line would TypeError otherwise; (Prereq 2) `gui_worker.py` redirects sys.stdout during dispatch — RpaBridge's `_emit_notification` writes JSON-RPC notifications direct to stdout mid-execute_workflow, which would corrupt the single-envelope contract the parent parses <br/>**regression lock (R14):** `test_mcp_gui_server.py` (13 tests: initialize + tools/list + tools/call dispatch + error paths + self-test) + smoke-gate L3 assertions (check_exec gui-mcp-for-oc.sh, udev rule file present, `python -m controller.mcp_gui_server --self-test` returns "self-test OK", `iceui_*` entries in qb_oc.json permission map with ≥12 entries) + F-51 markers `F-101-mcp-server:dual-brain/controller/mcp_gui_server.py:F-101` + `F-101-wrapper:cx-distro/distro/gui-mcp-for-oc.sh:F-101` + `F-101-udev:cx-distro/distro/10-uinput.rules:F-101`. **Rule: any capability the current-edition Python controller intercepts before mcpd MUST have an OC-edition MCP server exposing it, or an explicit v6.x_OC IMPLEMENTATION_PLAN note that the capability is deliberately unavailable in OC. Silent absence is the failure mode this rule kills.** |
| F-103 | 2026-08-02 | Fix V (v6.15): AT-SPI-only iceui MCP server (v6.14) silently returned `element_not_found` for Electron apps (Slack/Discord/VSCode), Chrome/Firefox web content, LibreOffice Impress canvas, most games, and cross-arch emulated apps. Real usage bounded to gnome-calculator + gnome-text-editor demos — users saw the AI refuse to click things it could clearly see, and gave up | Whitepaper §5 promised "natural language → any GUI action" but v6.14 shipped "natural language → AT-SPI-visible action". a11y-tree lookup is the wrong primitive for the app categories users hit most; needed a vision-grounded fallback that reads pixels not accessibility metadata | 12 new iceui MCP tools (parse_screen + click/type/drag/scroll/hover/press_key/key_sequence + 4 grounded_* NL wrappers) via `dual-brain/gui_agent/vision.py` (VisionGrounder — LiteLLM Gemini 2.5 Flash primary + Claude Haiku 4.5 fallback + 2s cache + BP-10 cost ceiling) + `input_synth.py` (xdotool wrappers with allowlist) + `agent.py` `_handle_grounded_*` orchestrators (parse → LLM element-pick → xdotool actuation). Landlock ruleset widened to include `/usr/bin` READ_EXEC (enables xdotool spawn). Smoke gate assertions verify 24-tool count + xdotool present + vision self-test <br/>**regression lock (R14):** 15 test_vision.py cases (cache, retry, fallback, cost ceiling, disabled mode) + 37 test_input_synth.py cases (button allowlist, coord bounds, HiDPI translate, shell-injection rejects) + 19 test_agent_v_grounded.py cases (end-to-end orchestration + failure taxonomy) + smoke-gate assertion `iceui --self-test | grep "24 tools"` + F-51 markers `F-103-vision:dual-brain/gui_agent/vision.py:F-103` + `F-103-input-synth:dual-brain/gui_agent/input_synth.py:F-103` |
| F-104 | 2026-08-02 | Fix V (v6.15): opencode's "ask" prompt for tier-2 write actions is plain-text only — user has NO visual sense of what the AI is about to click, so grounded_click looked like a leap of faith. Without a preview, "click the Send button" could equally well click Cancel or Delete and user wouldn't catch the mistake until after the fact | opencode 1.18.4 permission dialog is a TUI text prompt (verified via strings dump + `bwrap` inspection); no MCP protocol path to attach an inline image to the ask itself. But Linux desktop notifications (notify-send) accept a `--icon=<png>` flag that renders alongside the notification body, and users see them regardless of TUI focus | `dual-brain/gui_agent/annotate.py` renders annotated preview PNGs with kind-colored borders (iOS palette — button red, input blue), numbered ID chips, target arrow, 40% dim on non-target regions, low-confidence dashed border, corner watermark. `_handle_parse_screen` (V.4c) + `_maybe_repreview` (V.4d) fire `notify-send --icon=<png>` so the user sees the annotated view on their desktop ~500ms BEFORE any subsequent grounded_click ask prompt lands. Best-effort — if notify-send is missing or PIL blows up, tool still returns element list; notify_sent=False in the result <br/>**regression lock (R14):** 20 test_annotate.py cases (renders + retention + a11y label + edge cases) + `_try_notify_send` unit tests (subprocess success/fail/timeout paths) + F-51 marker `F-104-annotate:dual-brain/gui_agent/annotate.py:F-104` |
| F-105 | 2026-08-02 | Fix V (v6.15): opencode's "ask on every click" made grounded automation unusable UX — even a simple "open Firefox + click search box + type query" would fire 3 modals in a row. Without a trust affordance, users would either disable HITL entirely (defeating the safety model) or give up on grounded automation. Also: nothing prevented users from ACCIDENTALLY approving auto-click for terminals or sudo dialogs, which would be catastrophic | iOS 14+ tri-state permissions ("Allow Once" / "Allow While Using" / "Don't Allow") + 1Password domain-scoped autofill trust are the proven UX pattern for "approve once, work naturally". Icebreaker had no such affordance | `dual-brain/gui_agent/trust_store.py` (~340 LOC): four-tier grants (`once` / `session` / `persistent` / `deny_always`), narrow wildcards (`*` and `prefix*`), session-id binding for session-tier, TTL for persistent, never-silent expiry (matching-but-expired grants return denied with explicit "trust expired at X — ask again"). Built-in `_HARD_DENY` frozenset overrides EVERYTHING for gnome-terminal / xterm / konsole / terminator / sudo — defense in depth so a corrupt defaults file can't open them back up. Append-only JSONL with fcntl.LOCK_EX + fsync per write. Shipped defaults in `cx-distro/distro/gui_trust_defaults.jsonl` (24h auto-approve for 10 read-only tools; calculator pre-approved; 7 terminals + 4 secret-handlers hard-denied). `scripts/ib_trust.py` CLI: discoverable no-args help, fuzzy match (`click` → `gui.grounded_click`), --dry-run, undo via 16-deep JSON ring, `why APP TOOL` explains + exit-code-scripts, export/import <br/>**regression lock (R14):** 33 test_trust_store.py cases (all 4 tiers + wildcards + hard-deny + expiry + validation) + 29 test_ib_trust.py cases (CLI subcommands + fuzzy + dry-run + undo + import bad-line skip) + smoke-gate assertions (`ib-trust list` runs + defaults JSONL present) + F-51 markers `F-105-trust:dual-brain/gui_agent/trust_store.py:F-105` + `F-105-cli:dual-brain/scripts/ib_trust.py:F-105` + `F-105-defaults:cx-distro/distro/gui_trust_defaults.jsonl:F-105` |
| F-106 | 2026-08-02 | Fix V (v6.15): the VLM (Gemini vision) returns coords in the screenshot's PHYSICAL pixel space (a 3840x2160 image on a Retina-2x display). Xdotool operates in the X server's LOGICAL coord space (1920x1080 on the same display). Without translation every VLM-provided click would land at the wrong pixel — the "just click Send" promise falls apart on Apple Silicon UTM (user's primary test env) before it starts | Neither xdotool nor Gemini has any concept of the other's coord space. The translation MUST happen in-between. Also multi-monitor: two 1x screens side-by-side means a click at logical (2000, 500) is on monitor 2, not off-screen | `dual-brain/gui_agent/geometry.py` (~200 LOC): `MonitorLayout.detect()` parses `xrandr --query` output into frozen `Monitor(name, origin_x, origin_y, width, height, scale)` records. Scale inferred by comparing the connected line's logical resolution to the active mode's physical resolution. `[gui.geometry] hidpi_scale_override` for VM guests where xrandr misreports. 30s cache so click-heavy loops don't respawn xrandr. `logical_to_physical` + `physical_to_logical` inverses. `find_monitor(x,y)` picks display; off-screen falls back to primary. Every mouse fn in `input_synth.py` takes optional MonitorLayout param + translates before xdotool. Fail-safe: xrandr missing or errors → single-monitor 1x fallback layout, never raises during detect() <br/>**regression lock (R14):** 10 test_geometry.py cases (parse single/multi/HiDPI/scale-override, fallback on xrandr missing/errors, translation roundtrip, cache dedup) + 37 test_input_synth.py cases assert `_translate` applied before every xdotool argv + F-51 marker `F-106-geometry:dual-brain/gui_agent/geometry.py:F-106` |
| F-107 | 2026-08-02 | Pre-existing security-critical bug discovered mid-V.6a: **every** `LANDLOCK_ACCESS_FS_*` bit constant in `dual-brain/gui_agent/sandbox.py` and `dual-brain/rpa_bridge/sandbox.py` was WRONG since day one. Code had `READ_FILE = 1<<0` but kernel UAPI (include/uapi/linux/landlock.h) says `EXECUTE = 1<<0`. Similarly WRITE_FILE=1<<5 (actually REMOVE_FILE), MAKE_DIR=1<<9 (actually MAKE_SOCK), etc — 6 of 7 constants mislabeled. Practical impact: `$HOME "read-only"` was actually granted EXECUTE + WRITE_FILE. `scratch dir "read-write"` also granted MAKE_SOCK + MAKE_BLOCK + MAKE_SYM (create device nodes not intended). `handled_access_fs` omitted bits 2, 3, 7 → READ_FILE, READ_DIR, MAKE_DIR were silently UNRESTRICTED globally. Sandbox reads/lists/mkdirs anywhere on the filesystem. Also grants EXECUTE on $HOME + /tmp/icebreaker-gui/ (opposite of intent) | Wrong bit values were probably copy-pasted from an early Landlock draft or ABI proposal that never landed upstream. Bug survived because the wrong values HAPPENED to permit whatever operations the Python subprocess had already loaded pre-Landlock (Python + libc + libdbus were mmap'd before landlock_restrict_self ran). Sandbox never actually blocked anything meaningful in normal operation. The pre-existing seccomp `execve` DENY masked what would otherwise have been a live exec surface | (a) Bit values corrected to kernel UAPI canonical (`EXECUTE=1<<0`, `WRITE_FILE=1<<1`, `READ_FILE=1<<2`, `READ_DIR=1<<3`, `REMOVE_DIR=1<<4`, `REMOVE_FILE=1<<5`, `MAKE_CHAR=1<<6`, `MAKE_DIR=1<<7`, `MAKE_REG=1<<8`, `MAKE_SOCK=1<<9`, `MAKE_FIFO=1<<10`, `MAKE_BLOCK=1<<11`, `MAKE_SYM=1<<12`). Verified via `gh api repos/torvalds/linux/contents/include/uapi/linux/landlock.h`. Composite masks rewritten: `_LANDLOCK_READ_ONLY = READ_FILE\|READ_DIR` (no exec, no write); `_LANDLOCK_READ_EXEC = +EXECUTE` (for /usr/bin allowlist); `_LANDLOCK_READ_WRITE = READ+WRITE+MAKE_REG+MAKE_DIR+REMOVE_FILE+REMOVE_DIR` (deliberately no EXECUTE — scratch dir can't run drops); `_LANDLOCK_HANDLED = every ABI v1 bit` (no silent unrestrictions). (b) Path rules re-authored: $HOME + scratch + XDG_RUNTIME_DIR keep intent (no exec now); /usr + /lib + /lib64 + /opt/icebreaker get READ_EXEC (input_synth spawns); /etc + /var/lib/icebreaker + /proc + /sys + /dev + /run + /tmp get READ_ONLY (were unrestricted). (c) Seccomp: execve flipped DENY→ALLOW (Landlock EXECUTE now gates by path); execveat REMAINS DENIED (exec-by-fd escape). rpa_bridge/sandbox.py gets bit-value fix ONLY — path rules + seccomp unchanged to avoid accidentally unblocking Selenium's geckodriver spawn <br/>**regression lock (R14):** 26 parametrized bit-value tests in `gui_agent/tests/test_sandbox_landlock_bits.py` assert every constant equals kernel UAPI canonical + composite-mask sanity (READ_ONLY excludes exec, READ_EXEC includes exec, READ_WRITE excludes exec, HANDLED covers every ABI v1 bit). Belt-and-braces `test_no_bare_wrong_shifts_in_landlock_context` greps source files for the 6 pre-fix wrong lines. Smoke-gate L3 assertion: chroot venv Python asserts `_LANDLOCK_ACCESS_FS_EXECUTE == 1`. **Regression lesson: security-critical files with kernel-header-derived constants MUST have per-constant tests against a canonical table, not just "sandbox module imports cleanly". CLAUDE.md security-critical files should NEVER get constants copy-pasted from anywhere except the current kernel UAPI header.** F-51 marker `F-107-landlock-bits:dual-brain/gui_agent/sandbox.py:F-107` (V.6e added — was missing from V.6a). |
| F-109 | 2026-08-06 | Whitepaper §5's "COW imagination layer" flagship safety feature was a stub across v0-v6.15: mcpd returned a `requires_cow_approval` ticket carrying only `path` + `size` fields (no real diff), no overlay ever mounted, and no commit RPC existed. Both HITL surfaces (streaming path's Step 4 + AgentGraph's `hitl_gate_node`) fired with `cow_summary=None`. The audit's Tier-A #1 (`docs/2026-08-03_whitepaper_vs_reality_audit.md` row C-1) tagged this as the flagship departure — "The AI cannot make irreversible mistakes" was a bare promise. Also closed: audit row B-2 (dry-run only in second post-mcpd modal) and B-3 (AgentGraph had no COW branch at all) | User's original design choice (2026-08-03 via AskUserQuestion) selected **simulation over literal overlayfs** — real overlay would need `SYS_mount`/`SYS_umount2`/`SYS_unshare` moved from seccomp DENY→ALLOW plus Landlock rule additions for upperdir/workdir/dpkg-state/apt-cache. Same user-visible outcome ("3.2 GB will be freed. 847 files will be deleted.") achievable via targeted introspection: `walkdir + du` for fs.delete, `stat` for fs.write outside home, `apt-get -s -qq` for package.*. Zero new syscalls in the allowlist. Whitepaper §5 text update deferred to M7.6b to acknowledge the implementation approach | **v6.16 M7.0.1** — sub-commits `c3ac412` (M7.0.1a: `src/mcpd/src/tools/cow.rs` with `DryRunDiff` + 3 simulators + `IntentStore` with 5-min TTL + register/take/gc + 25 unit tests), `2568391` (M7.0.1b: `fs.rs` + `package.rs` ticket envelopes grow `preview.diff` sub-object; 8 integration tests), `6484ca6` (M7.0.1c: new `cow.commit` RPC in `server.rs::dispatch` + schema JSON + `commit_delete/write/install/remove/upgrade` real-execution handlers + 8 integration tests for op-swap/subject-swap/replay/expiry guards), `99a8c71` (M7.0.1d: `dual-brain/controller/cow_summary.py::format_diff` produces whitepaper §8.2 + terraform-plan compact shape; `ToolResult.dry_run_diff` + `McpdClient.commit_cow`; 32 new tests), `7e1c195` (M7.0.1e: streaming path Step 3.5 pre-pass + Step 10 rewrite in both streaming AND non-streaming `main.py` twins — populates `cow_summary` at INITIAL HITL gate, commits via `cow.commit` after approve, keeps old second-modal as fallback for pre-v6.16-mcpd), `ab991f6` (M7.0.1f: `agent_graph_nodes.py::cow_preview_node` runs between verifier and hitl_gate — LangGraph re-execution gotcha handled by node-split not idempotency-guard per 2026 production research; `hitl_gate_node` payload enriched with `cow_diff_json`; `mcpd_dispatcher_node` calls `commit_cow` when state carries pre-approved intent_id; `agent_graph_state.py` grows 2 fields checkpointed by SqliteSaver across interrupt), `58ddfea` (M7.0.1g: smoke-gate + § 10 disposition update landing this row), and `<pending>` (M7.0.1h: post-ct-scan Defect #1 fix — AgentGraph dispatcher fails loudly on COW-eligible action without pending intent_id + Defect #2 fix — replace dead `self._logger` guard with `_log_exception(self._system_logger, ...)` + stderr warn for non-streaming visibility). Zero seccomp/Landlock changes — `denylist_covers_high_impact_syscalls` at `src/mcpd/src/sandbox/seccomp.rs:425` stays green. mcpd `strings` check for `cow.commit` + `apt-get -s` presence added to `incremental/build/smoke-gate.sh` L6 <br/>**regression lock (R14):** `src/mcpd/tests/integration.rs::cow_commit_round_trip_for_fs_delete` + `cow_commit_round_trip_for_fs_write_outside_home` + `cow_commit_rejects_op_swap` + `cow_commit_rejects_subject_swap` + `cow_commit_replay_after_success_returns_not_found` + `cow_commit_bad_uuid_returns_err_not_protocol_error` + `cow_commit_bad_operation_enum_rejected_by_schema` + `cow_commit_in_tools_list_catalogue` + `src/mcpd/src/tools/cow.rs` inline unit suite (25 tests) + `dual-brain/controller/tests/test_cow_summary_formatter.py` (28 tests) + `test_mcpd_client_unit.py::test_dry_run_diff_*` + `test_commit_cow_round_trip_via_fake` + `test_agent_graph_run.py::test_tier2_pauses_at_hitl_then_resumes_on_approve` (updated to assert preview-then-dispatch, not never-called) + `test_tier2_deny_short_circuits_no_dispatch` + `test_e2e.py::test_tier3_hitl_deny_skips_dispatch` (updated with same B-2 semantic acknowledgement) + smoke-gate L6 assertions for `cow.commit` strings match + `apt-get` presence. **Lesson: whitepaper §5 promises "the AI cannot make irreversible mistakes" — that's the user-outcome invariant, not "we ship a specific kernel primitive." A dry-run that shows the exact real diff + a gated commit that fires the exact real op = same user guarantee. Simulation approach removes the sandbox weakening real overlayfs would demand.** |
| F-110 | 2026-08-06 | Whitepaper §6 promised "MCP tool schemas compiled into FSM... the model literally CANNOT generate invalid output — it is mathematically impossible." Reality across v0-v6.15: **PB was grammarless end-to-end.** Controller-side `_build_pb` at `dual-brain/controller/__main__.py:167-178` constructed `BackendConfig` without `grammar_path`, so `LlamaCppLocalBackend._grammar` stayed `None` and the JSON-RPC `payload["grammar"]` was never populated. Server-side `dual-brain/scripts/start_pb.sh:60-70` silently blanked `GRAMMAR_ARGS` if the file was missing (no visible warning); distro `dual-brain/scripts/start-pbd:63-69` didn't wire `--grammar-file` at all and comment-documented *"PB uses no grammar — relies on post-hoc tool-call validation."* Measured impact: PB emits schema-invalid JSON at ~5% rate; existing `verifier.py` catches these post-hoc and surfaces as `Outcome.QB_VERIFIER_REJECTED` — one visible failure per ~20 turns. Also: `mcp_tool_call.gbnf` never shipped to `/var/lib/icebreaker/grammars/` (the path `start-pbd` reads from), so even after fixing controller-side wiring the runtime file would be absent. Also closed: audit rows B-4 (§6 GBNF constraint promise) + C-4 (Tier A #2 — grammar-constrained decoding on PB) | User's design refinement (2026-08-06): fold GBNF wire-up AND bounded retry loop AND QB-consult rescue into ONE deliverable. Enforcing grammar alone risks breaking edge cases where PB genuinely can't produce a valid tool call; retry loop with QB coaching adds resilience while preserving the "no schema-invalid output survives" invariant. Grammar-file drift at runtime = visible-warn (not crash-loop) so operator recovery paths stay open; smoke-gate refuses ISO build if grammar file missing (belt-and-braces). Per-tool GBNF tightening (enumerate 25 tool names + per-tool params shape) deferred to v6.17 — generic `{"tool": <str>, "params": <obj>}` grammar sufficient to close whitepaper §6 shape promise for M7.0.2 | **v6.16 M7.0.2** — sub-commits `977bdea` (M7.0.2a: `RunConfig.pb_grammar_path` + `_build_pb` grammar_path threading + JSON schema + toml.example documentation), `18bb84f` (M7.0.2b: louder `start_pb.sh` visible-warn on missing grammar + `start-pbd` gains `--grammar-file` argument entirely), M7.0.2c collapsed as no-op (cost accumulator bug only manifests if retry loop is inlined; M7.0.2d encapsulation makes `pb_cost = pb_result.total_cost_usd` with `=` stay correct), `84fe6aa` (M7.0.2d: new `dual-brain/controller/pb_retry.py::PbRetryLoop` — bounded attempts, cost ceiling per BP-10, `consult_qb_after_attempt` gates QB rescue + 16 unit tests), `fdad67d` (M7.0.2e: `qb_pb_repair.txt` prompt + `PromptsConfig.qb_pb_repair` slot + `Controller._qb_repair` mirroring `_qb_summarise` shape; INV-1-safe — QB receives only schema-validated intent fields + PB output + verifier reason + 13 unit tests), `6a86b27` (M7.0.2f: `PbRetryLoop` wired into all 3 execution paths — streaming Steps 6-8 + non-streaming twin + `agent_graph_nodes.py::executor_node`; 3 `Controller._make_*_fn` adapters extracted to keep loop framework-agnostic; new `Outcome.PB_RETRY_EXHAUSTED` + `_map_pb_failure_outcome` helper that preserves single-attempt backward-compat semantics; +9 tests including INV-1 guards + config round-trip + AgentGraph exhaustion), and `<pending>` (M7.0.2g: v6.manifest `install -Dm644 mcp_tool_call.gbnf → /var/lib/icebreaker/grammars/` + smoke-gate L6 `check_file` + non-empty size sanity + first-boot `phase_result "pb_grammar"` warn-not-red on missing + § 10 disposition flip for B-4 + C-4 + this row). Bonus semantic win: non-streaming path pre-M7.0.2 had ZERO verifier retry (only streaming had `_should_retry_verifier`); PbRetryLoop now gives both paths identical retry semantics <br/>**regression lock (R14):** `dual-brain/controller/tests/test_pb_retry_loop.py` (16 tests: happy path, verifier rescue at attempt 2, exhaustion, cost ceiling, provider errors, retry_mode=off, QB-repair failure tolerance) + `test_qb_repair.py` (13 tests including INV-1 guard `test_qb_repair_user_msg_excludes_raw_text_fields` + PKG-1 discipline `test_qb_repair_prompt_file_exists` + prompt-content INV-1 meta-check) + `test_streaming.py::test_pb_retry_exhausted_outcome_emitted` + `test_pb_retry_two_attempt_success` + `test_pb_retry_disabled_via_max_attempts_1_single_shot_semantics` (backward-compat outcome mapping) + `test_e2e.py::test_pb_retry_exhausted_outcome_emitted_non_streaming` + `test_agent_graph_nodes.py::test_executor_node_pb_retry_exhausted_marks_failed` + `test_config.py::test_pb_retry_config_{defaults_when_section_missing,overrides_from_toml,rollback_knobs_load,is_frozen}` (4 tests) + `test_audit.py::test_all_outcome_enum_values_present` (locks new PB_RETRY_EXHAUSTED entry) + smoke-gate L6 `check_file /var/lib/icebreaker/grammars/mcp_tool_call.gbnf` + non-empty size sanity + first-boot `phase_result "pb_grammar"`. **Lesson: whitepaper §6 promised "mathematically impossible to generate invalid output" but never asserted at build-time that the grammar file actually ships. Any invariant that depends on a file being present at a specific runtime path MUST have a smoke-gate `check_file` AND a first-boot `phase_result` — two independent gates, one build-time one runtime, per BP-9 defense in depth.** |
| F-111 | 2026-08-11 | Whitepaper INV-1 promised "QB has zero direct access to system execution tools" (§5 line 107) + "Controller passes only an opaque reference ID to the PB" (§5 line 152) + "PB is completely blind to the outside world" (§5 line 117) — the Tier A #3 flagship departure. Reality in OC edition (v6.13_OC..v6.16): opencode (spawned as the user's AI Terminal) reads user text → asks Gemini → Gemini invokes mcpd + iceui MCP tools DIRECTLY. No Intent Object, no reference ID, no PB, no schema validation, no risk classifier, no HITL COW dry-run, no PB grammar-constrained tool call, no verifier vote. Audit rows written by `oc_audit_bridge` (Fix R, v6.13_OC) hard-stamp `session_id="opencode-oc"` + `turn_index=-1` + `model=""` + `tokens=0` + `cost=0.0` — enough for INV-8 hash-chain retention but zero forensic value (rows A-6/A-7). Also, since Fix M (v6.14) OC edition uses `iceui_*` gui/rpa tools which have NO AuditLog path at all — silent execution of GUI automation. Net: 8 whitepaper claims flatly false in OC mode (audit rows A-1..A-8 in `docs/2026-08-03_whitepaper_vs_reality_audit.md`); v6.16 M7.0.1 (COW) + M7.0.2 (grammar) shipped their Controller-side safety machinery, but the OC code path bypassed it entirely — user built v6.16 for a code path they don't use | User's frustration surfaced the mismatch (2026-08-10): "I care only about the OC version along with COW dry-run at HITL, PB GBNF grammar mandate, bounded retry loop with QB-consult rescue." Design (2026-08-10 approved plan): route the OC edition through the Controller pipeline via a single-tool MCP surface. opencode gets ONE tool (`iceui_submit_intent`); Gemini translates user text into a validated Intent Object; a new daemon RPC (`intent.run`) receives it; a new Controller entry point (`run_turn_from_intent`) drives Steps 2-11 unchanged. Everything from v6.16 (COW simulation dry-run, HITL, PB grammar, bounded retry, QB-consult rescue) fires as normal — the ONLY change is Step 1 is skipped (opencode already translated). INV-1 preserved: Controller re-validates every intent field via schema, re-classifies risk, re-verifies via qb_verifier. Single-real-Gemini in OC mode chosen over dual-backend (recon-verified: no OC code path touches `self._qb` outside from_intent flow). Python-only audit deconfliction via IntentStore UUID ring (zero mcpd Rust change) chosen over adding `caller_pid` to mcpd audit line | **v6.17 M7.6a-1** — sub-commits `79185ff` (M7.6a-1a: `mcp_gui_server.py::submit_intent` MCP tool + minimal one-shot AF_UNIX daemon-socket client; 11 tests including INV-1 forwarding lock + happy path + socket-connect/timeout/close failure modes; ICEBREAKER_CONTROLLER_SOCK env override for tests + dev), `b670317` (M7.6a-1b: new `intent.run` daemon RPC in `daemon.py` + `_start_pipeline_worker` refactor that both `turn.run` + `intent.run` share; params validation with graceful INTERNAL_ERROR fallback when Controller.run_turn_from_intent isn't shipped yet; 11 tests including turn-lock contention + INV-1 forwarding lock), `495b280` (M7.6a-1c: `Controller.run_turn_from_intent` thin wrapper + `from_intent` kwarg on `run_turn_streaming` that (a) bypasses F-92 OC short-circuit for from_intent path only + (b) skips Step 1 qb_intent + synthesizes zero-cost qb_response + (c) deep-copies intent to preserve caller's dict + (d) records intent as assistant message for verifier/summariser context; 12 tests including `test_run_turn_streaming_without_from_intent_still_short_circuits_in_oc_mode` invariant lock), `c5a507c` (M7.6a-1d: `__main__.py::_try_build_oc_native_qb` builds a real GeminiBackend from GEMINI_API_KEY when cfg.qb.name==opencode_oc; graceful NoOp fallback on missing key OR ctor error — daemon never crash-loops; new optional `OpencodeOcConfig.native_qb` sub-config for model/max_tokens/timeout overrides; 15 tests covering 14 pre-flight failure modes documented in the plan file), `7be0883` (M7.6a-1e: `gen-oc-config.sh` OC_MODE env var; `submit_intent_only` mode flips every MCP tool to `deny` + only `iceui_submit_intent: allow`; `legacy_direct` preserves v6.13_OC..v6.16 shape as rollback; edit + bash stay `deny` in both modes (F-100 discipline); 7 tests via subprocess-invoked fake mcpd stub), `173f885` (M7.6a-1f: `oc_audit_bridge._NATIVE_RING` module-level deque(500) with 10min TTL + `mark_native_dispatch` + `is_native_dispatch` consume-on-match helpers; McpdClient.call unconditionally marks — bridge only reads in OC mode where it's meaningful; reset-on-bridge-init prevents cross-lifecycle contamination; 15 tests covering ring contract + TTL + cap + bridge integration + McpdClient hook resilience), `9ebe5fd` (M7.6a-1g: `cx-distro/distro/opencode_prompt_submit_intent.txt` — full intent-object schema + tool-selection rules + 5 worked examples; gen-oc-config.sh injects `instructions: [runtime path]` in submit_intent_only mode; build-iso.sh installs prompt to `/etc/icebreaker/`; 3 additional tests locking source path + instructions wiring), `0b8f482` (M7.6a-1h: smoke-gate L3 tool count bumped 24→25, prompt-file check + non-empty sanity, OC_MODE lockdown consistency check — instructions present ⇔ every non-submit_intent tool = deny + iceui_submit_intent = allow; first-boot Step 1c `oc_submit_intent` phase — `ok`/`warn`/`skipped` matching pb_grammar's degrade-not-crash pattern), and `<pending>` (M7.6a-1i: this row + § 10 flips + tracker rows + audit doc banner). Cost profile per OC turn using submit_intent: opencode's Gemini call unchanged (~0.001 USD; already paid); native Gemini adds ~0.001-0.002 USD for verifier + summariser + optional QB-consult repair. Real-user 200 turns/day → ~$0.30/day added. Bounded by v6.16 M7.0.2 PbRetryConfig.cost_ceiling_usd_per_turn=0.005 <br/>**regression lock (R14):** `test_mcp_gui_server_submit_intent.py` (11 tests: schema shape, missing/empty session_id/intent, socket connect failure, happy path, JSON-RPC error envelope, notifications-before-response, connection-close, INV-1 forwarding, tools/call dispatch routing) + `test_intent_run_daemon_rpc.py` (11 tests: dispatch registration, params validation × 5, missing-Controller-method graceful error, happy path, streaming notifications, turn-lock contention, INV-1 forwarding) + `test_run_turn_from_intent.py` (12 tests: method exists, wrapper contract, Step-1 SKIPPED (QB.complete call count ≤ 2), downstream steps still fire, OC short-circuit invariant lock, intent immutability, server-owned field injection, session history recording, malformed intent rejection, cost accounting) + `test_backend_registry_oc.py` (15 tests: PF-1 missing-key fallback × 3, PF-9 ctor-error fallback, happy path with correct model/SecretRef, native_qb TOML overrides × 3, PF-10 backward compat × 2, _build_qb integration × 2, non-OC not affected) + `test_gen_oc_config_oc_mode.py` (10 tests: legacy_direct default + explicit, submit_intent_only lockdown, edit/bash deny preservation, rollback via mode toggle, unknown value fallback, iceui-import-failure resilience, instructions field wired in submit_intent_only, absent in legacy_direct, prompt source-path + keyword-content lock) + `test_oc_audit_bridge_filter.py` (15 tests: mark/is_native basic contract, method/target mismatch, consume-on-match, multiple marks, TTL expiry, ring cap 500, bridge integration skip/write/consume, McpdClient.call marks, mark-hook failure never breaks call, cross-lifecycle isolation via reset-on-bridge-init) + smoke-gate L3: iceui tool count = 25, prompt file present + ≥500 bytes, OC_MODE lockdown consistency (instructions ⇔ deny-everything-else) + first-boot phase `oc_submit_intent` = ok/warn/skipped. **Lesson: an audit-row disposition is only real when the failing user flow actually reaches the fix. v6.16 shipped the Controller-side safety machinery but the OC path bypassed it — 15 sub-commits of value the user couldn't use. The user's frustration at the v6.16 arm64 UTM-ready moment forced the pivot that produced this milestone; without the frustration, we would have shipped v6.17 M7.0.3 (iceui audit sink) as planned + still had OC users on unchanged v6.13_OC-shaped tool routing. Rule: for every audit row we claim closed, someone MUST have actually used the fixed path — not just have the machinery present.** |
| F-101.2 | 2026-07-31 | v6.14 OC arm64 build failed at smoke gate on the ONE F-101 iceui self-test assertion (`[FAIL] F-101: iceui MCP server self-test failed — handshake or tools/list broken`). Every other L0–L6 check (76+) passed. Manual reproduction inside the chroot: `chroot $CHROOT /opt/icebreaker/venv/bin/python3 -m controller.mcp_gui_server --self-test` → `self-test OK: initialize + tools/list (12 tools)`. Self-test itself was fine — the assertion was masking a true PASS as a FAIL. R6 (no ISO from a broken tree) correctly refused to produce an ISO; wasted ~1h of build time before the failure surfaced | The assertion used `in_chroot "$VENV_PY -m controller.mcp_gui_server --self-test 2>&1" \| grep -q "self-test OK"`. The `in_chroot` helper is defined as `chroot "$CHROOT" bash -c "$1" >/dev/null 2>&1` — it discards ALL output including stdout, so the pipe to `grep -q` read from an empty pipe and grep always returned non-zero. The `\| ... \|\| fail` short-circuit therefore ALWAYS fired the fail branch regardless of self-test outcome. False-negative — indistinguishable from a genuine handshake failure. F-101 shipped an assertion that could never PASS | Replace the one-liner with an explicit `if chroot "$CHROOT" bash -c "..." \| grep -q "self-test OK"; then pass; else fail; fi` that runs chroot directly and preserves output. Do NOT modify `in_chroot` itself — other call sites rely on its silence for exit-code-only checks (e.g. `in_chroot "id icebreaker \| grep -q icebreaker-users"` where the pipe lives inside the string and only the exit code matters). Only the F-101 assertion needed captured output. Regression guard: the fix IS the guard — any regression to `in_chroot ... \| grep` would recreate the false-negative and get caught the moment someone actually intended to fail (currently masked). Adding a self-test-of-the-self-test would be over-engineering; the assertion is now shape-obvious. **Lesson: `A \| B && C \|\| D` with A's stdout discarded is a silent bug factory. Any smoke-gate assertion that pipes chroot output must use `chroot ... bash -c "..."` directly, not `in_chroot`.** |
| PKG-1 | 2026-06 | ImportError/FileNotFound for schemas, prompts, catalogue.toml, styles.tcss | pip doesn't ship package data | Explicit post-install copy; smoke-gate asserts presence |
| PKG-3 | 2026-06 | Model EACCES under systemd | `ProtectHome=yes` breaks symlinks into /home | COPY models to `/var/lib/icebreaker/models/`, never symlink |
| PKG-4 | 2026-06 | GUI "Not connected to daemon" | socket perms 0660 root:icebreaker-users; user not in group | User in `icebreaker-users` at creation (in base); smoke gate checks |
| PKG-8 | 2026-06 | Fixes missing from built ISO | `git archive HEAD` excluded uncommitted changes | R7: commit before building |
| F-112 | 2026-08-17 | **v6.17 OC amd64 QEMU gate failed (R2/R11 blocker):** guest booted and passed graphical target, GDM, no-failed-units, diagnostics, OC-terminal, and shell-PTY assertions, but `icebreaker-controller` remained active without creating `/run/icebreaker/controller.sock` within the 90-second TCG allowance. Socket mode/group, `daemon.status`, and `turn.run` therefore failed. Live evidence: service started at 16:20:20; the OC bridge logged at 16:21:48; LiteLLM contacted `raw.githubusercontent.com` at 16:22:26; the process was still CPU-bound before daemon binding. Restart reached systemd active in 16 seconds but repeated the pre-bind initialization. | The no-key OC path eagerly imported the LiteLLM-backed Gemini module before `Daemon.start()` bound the controller socket. Under no-KVM QEMU TCG, provider metadata initialization is expensive and network-backed. | **CLOSED — both QEMU repair gates green (2026-08-17).** `6f3ca5e` deferred the OC backend import; `85f29fc` completed the fix by returning the existing lightweight `opencode_oc` fallback from `_build_qb` before any provider import when no key/fallback is configured. `test_missing_oc_key_does_not_import_litellm_before_daemon_socket` runs `_build_qb` in a fresh process and asserts `litellm` is absent (16 registry tests green). Exact `v6.17a_OC` QEMU L2 re-runs: amd64 socket in **6s**; arm64 socket after **10s**; both had correct `0660 root:icebreaker-users` permissions, `daemon.status` RPC, `turn.run`, restart, and full L0–L6 gates including their native in-guest seccomp harvests. R1 UTM GUI validation remains a separate V6.17 release gate. |
| F-113 | 2026-08-17 | The canonical wrapper accepted `LABEL=v6.17r1`, spent the full uncached Docker pipeline, then failed in `build-iso.sh` because its documented label grammar permits only a semantic version plus one optional letter suffix. No incremental ISO was assembled from that invocation. | `cx-distro/rebuild/rebuild-v67.sh` delegated label validation until after Docker work instead of enforcing the same grammar at its own boundary. | `c68e273`: wrapper now rejects invalid labels before preflight/Docker; `cx-distro/tests/test_rebuild_label_validation.sh` syntax-checks the wrapper and locks the exact grammar/failure marker. Repair build uses legal `v6.17a`. |
| F-114 | 2026-08-19 | Clean v1 multi-arch build compiled `mcpd-amd64` but failed linking `mcpd-arm64`: host `cc` invoked `rust-lld`, which rejected ARM ELF object files as incompatible with `elf64-x86-64`. | The build loop hardcoded only `RUST_TRIPLE`; it did not configure Cargo's target linker even though the arm64 cross compiler was installed. | Source `RUST_TRIPLE` and `RUST_LINKER` from `config/archs/*.conf`; export the target-specific Cargo linker only for that target and fail clearly if it is absent. **Regression lock:** Stage 1 `file` architecture assertion must pass for both `mcpd-amd64` and `mcpd-arm64` in every clean v1 rebuild. |
| F-115 | 2026-08-19 | The clean v1 binary-only VM build reached Stage 2, cloned the pinned llama.cpp commit, then failed only when `cmake` was invoked. | `cx-distro/Dockerfile.build` declares `cmake`, but the `--force` preflight verified repository state and model integrity without verifying tools required by the selected stages. | Preflight derives required executables from `--skip-to`/`--stop-after` and refuses before compiling if any are absent. **Regression lock:** a direct binary-only build on an unprovisioned host fails in Stage 0 with `required build tool not found`, never after cloning/compiling. |
| F-116 | 2026-08-19 | v1 amd64 ISO assembly ran the seccomp harvest successfully but printed `pytest not on VM PATH — corpus gate skipped`; the builder would otherwise have continued to package a candidate without the F-35 regression gate. | `build-iso.sh` treated a missing host test runner as a warning, while `cx-distro/Dockerfile.build` did not install `python3-pytest`. | Make pytest mandatory for V≥5 corpus builds and install `python3-pytest` in the canonical builder image. **Regression lock:** missing pytest is now a fatal corpus-gate error; a successful ISO build proves the corpus executed. |
| F-117 | 2026-08-19 | v1 amd64 assembly reached the smoke gate and was correctly rejected on four stale assertions: it expected a fixed `icebreaker` user and API-key placeholders in public `locations.env`, although v1 deliberately uses a locked live-only `ubuntu` account, installer-created user accounts, and root-only `credentials.env`. | The v1 installer/secret migration changed runtime ownership and credential storage without updating the cumulative v6 smoke checks. | Smoke gate now asserts the locked live user, shared socket group, root-only credential file, absence of API-key variables from public `locations.env`, and absence of a baked key in `credentials.env`. **Regression lock:** a v1 smoke pass proves the replacement account and BP-8 secret model, rather than the obsolete `icebreaker`/0640 contract. |

---

## 8. Design decisions

- **D-1 Squashfs compression: zstd.** Prior v5 shipped uncompressed (8 GB+ ISO for GNOME, painful 20-min downloads per iteration). `mksquashfs -comp zstd -Xcompression-level 3` is nearly as fast to build, halves+ the size, and decompresses faster than xz at boot. If a boot failure is ever suspected to be compression-related, `build-iso.sh --no-compress` reproduces the old behavior (log it in § 7 if that ever matters).
- **D-2 GNOME base built ONCE.** `build-base.sh` output is keyed on sha256(packages-desktop.txt + UBUNTU_BASE). Changing the package list = new base build (~60 min). Everything else reuses the cache (~15 min ISO turnaround).
- **D-3 User + autologin live in the base**, since they never change per version. Version overlays that need user-home changes must patch `/home/icebreaker/` explicitly (F-6).
- **D-4 Future manifests are stubs on purpose** (R2). `version_overlay()` for Vn is implemented only when Vn-1 is GREEN. Implementing all nine up front would recreate the big-bang integration this project exists to kill.
- **D-5 `toram` boot param for desktop profile** (same as proven v5 config); requires VM RAM ≥ squashfs size + working set → give UTM/QEMU 8 GB.
- **D-7 No echo backend; V2 uses the unconfigured-gemini path.** An echo QB would require widening the `qb.backend` enum in `schemas/controller_config.json` (+ config-loader branches + registry import) — security-adjacent edits for a test-only pathway, and a fabricated-intent backend left configured in a later version could reach mcpd. Instead V2 ships `backend = "gemini"` with no key: the daemon starts via the already-tested `_UnconfiguredBackend` fallback (F-5), `turn.run` returns an actionable error, and V5 activates QB by just adding the key. mcpd ships in V2 (not V6) because `_run_daemon` spawns it unconditionally — a missing binary is a fatal crash-loop.
- **D-6 We modify Ubuntu; we do not build an OS.** The base is assembled by `debootstrap` from **official signed packages at archive.ubuntu.com** plus Canonical's `ubuntu-desktop`/`ubuntu-standard` metapackages and the stock `linux-generic` kernel. Nothing is compiled from source; no custom kernel/libc/GNOME. The only hand-assembled piece is the live-boot wrapper (squashfs + isolinux/GRUB), copied verbatim from the v4/v5-proven `cx-distro/build.sh` chain. **Fallback (only if V0 fails its boot gate):** remaster Canonical's official desktop ISO — unpack `ubuntu-24.04-desktop-amd64.iso`, inject our overlay, repack. Not the default because 24.04's layered casper squashfs adds new unknowns while our current chain is already proven on UTM.

---

## 9. Phase 6 Exit Criteria

**Rule.** Every box below is checked [x] BEFORE `v1.0-rc1` gets built (see Scope I).
Cross-reference: `docs/IMPLEMENTATION_PLAN.md § Go/No-Go Gate Checklist (Phase 6 → Phase 7)`
for the strategic approval gates that layer on top of this operational checklist.

**Legend.**
- `[x]` — done and merged to `main`
- `[ ]` — pending
- `[deferred → I]` — infrastructure ready; final verification requires a fresh `v1.0-rc1` ISO
  (this is the "no ISO until the plan is done" rule — some live checks by definition run
  against the artifact we're gating on)

### Scope A — Kill every silent exception swallow (46 sites)
- [x] A.P1 (13 sites): all F-53'd + `_log_exception` surfaced
- [x] A.P2 (18 sites): TUI + GUI + shutdown surface
- [x] A.P3 (15 sites): seccomp + probes + AT-SPI
- [x] `test_no_silent_swallow.py` baseline + CI enforcement
- [x] F-51 markers for every P1 site (`incremental/versions/v2.manifest`)
- **Merged:** PR #27

### Scope B — Every knob is a config field; every field surfaces in the GUI
- [x] B1: 6 hardcoded knobs migrated to `controller/schemas/controller_config.json`
- [x] B2: 3 declared-but-unsurfaced fields exposed in Control Center (verifier voting + Advanced PB)
- [x] B3: custom-model / backend-swap race fix in `models_page.py`
- [x] B4: no-new-knobs AST checker (`tools/lint/no_new_knobs.py`) + G23 CI gate
- [x] Backward-compat: pre-Scope-B TOML loads with every default resolved (`test_config_backward_compat.py`)
- **Merged:** PR #27

### Scope C — Model presets verified against real APIs
- [x] C1: every preset live-verified against provider SDK (Gemini / Anthropic / OpenAI)
- [x] C2: preset registry moved to `catalogue.toml` schema v2
- [x] C3: XDG state cache with 7-day TTL for verification (`preset_verification_cache.py`)
- [x] C4: models page loads from catalogue + red-badge failing presets
- **Merged:** PR #28

### Scope D — Live verification of every Control Center page
- [x] D1: `manual_verification.md` edge-case matrix (not happy path)
- [x] D2: per-page smoke tests — all 8 pages, tmp_path save round-trip
- [x] D4: `pkexec ib-setup-key --dry-run` polkit test
- [x] D-instrumentation: debug mode module + GUI toggle (`controller/debug_log.py`)
- [ ] D3 [deferred → I]: live click-through sweep against fresh `v1.0-rc1` guest
- **Merged:** PR #29

### Scope E — Live end-to-end fallback chain verification
- [x] E1: fallback chain offline edge-case exhaustion (`test_backend_fallback_edge_cases.py`)
- [x] E2: streaming + cancellation edge cases
- [x] E3: `ICEBREAKER_LIVE` gate + `test_fallback_live.py` (uses operator-supplied bad keys)
- [ ] E-live [deferred → I]: guest sweep against fresh `v1.0-rc1` daemon
- **Merged:** PR #29

### Scope F — Regression coverage for every fixed bug (F-41 → F-56)
- [x] F1: 4 behavioral test modules (F-43/F-47b, F-48, F-52, F-53) — 56 assertions
- [x] F2: 7 broad-OS corpus rows + `_meta.deferred` block for shipped-only actions
- [x] F3: G24 live gate in `ci.sh` (uses `DaemonClient`, not `ib_debug`)
- [x] F4: R14 rule added to § 2 + 10 `regression lock (R14):` annotations in § 7
- [x] F5: 5 new F-51 markers (F43-stream, F47b-inner, F48-fix, F52-wire, F53-verifier)
- [x] F6: G24 live-verified against `icebreaker@192.168.64.27` 2026-07-11 —
      runner unwrap works, sweep completed 41 rows in ~10 min, caught 28× F-43/F-52
      + 2× F-56 + 1× F-53 on the deployed v6.65 venv (proving G24's value)
- [x] F-57 [logged 2026-07-11]: F-55 + F-56 added to `§ 7 Failure Log` from G24 findings
- [ ] F-live-clean [deferred → I]: G24 sweep against fresh `v1.0-rc1` produces 0 F-4x/F-5x errors
- **Merged:** PR #30

### Scope G — mcpd rebuild for both arches + F-55/F-56 + broader harvest coverage
- [x] G1: arm64 seccomp allowlist extended with `faccessat` (48) + `faccessat2` (439) — fixes F-55. Landed `src/mcpd/src/sandbox/seccomp.rs:220-233`, unconditional (both arches use these via libc). `cargo check` green for `aarch64-unknown-linux-gnu` + `x86_64-unknown-linux-gnu`. Commit `4910ef5`.
- [x] G2: F-56 fix — local `from .turn_events import ResultEvent` in `_emit_unsupported`, `dual-brain/controller/main.py:2470-2482`. Regression test `test_f56_result_event_scope.py` (4 tests). Commit `4910ef5`.
- [x] G3: Cross-compile infra — `cx-distro/Dockerfile.build` adds `aarch64-unknown-linux-gnu` target; `cx-distro/build.sh` Stage 1 loops over both arches → `mcpd-amd64` + `mcpd-arm64`; `file(1)` cross-check per binary; `models/checksums.sha256` auto-refreshed. `mcpd` symlink preserved for V0-V6.51 legacy.
- [x] G4: R15 rule proposed (§ 2 addition); `incremental/build/mcpd-harvest.sh` extended from 10 → 15 exercises (adds `process.inspect`, `service.logs`, `network.status`, `system.cpu`, `system.memory`). Response-count assertions + F-35 landing id renumbered 10 → 15.
- [x] G5: In-guest harvest runner `incremental/scripts/mcpd-harvest-guest.sh` (NEW), installed by `v2.manifest` at `/usr/local/bin/mcpd-harvest-guest.sh`, invoked from `incremental/tests/qemu-gate.sh` L6 via `_ssh`. TCG timeout scaling (F-13 rule). `smoke-gate.sh` L6 adds presence check + `strings` check for `faccessat` in the shipped mcpd binary.
- [x] G6: F-51 marker `F56-emit-import` added to `v2.manifest` for the F-56 fix. F-55 + G5 are covered by `smoke-gate.sh` L6 checks (mcpd binary + chroot script presence — v2.manifest markers only cover venv-installed files).
- [ ] G-live [deferred → I]: install fresh `mcpd-arm64` on UTM guest via `ib-update.sh`, re-run G24 sweep, verify no journalctl SIGSYS + no F-56 NameError.
- [ ] G-R15-lgtm: user LGTM the R15 wording in § 2 before PR #32 merge.

### Scope H — Docs, phase status, release notes (this scope)
- [ ] H1: `§ 9 Phase 6 Exit Criteria` in GROUND_TRUTH.md (this section — self-referential, marks [x] on merge)
- [ ] H2: `CLAUDE.md` Phase 6 row downgraded to "In progress"
- [ ] H3: `AI_Native_OS_Whitepaper.md § 10.5` audit acknowledgment footnote + **cross-link to § 10 Whitepaper Departures Ledger** (added 2026-08-03): v1.0-rc1 exit requires every § 10 row to have a resolved disposition or a live milestone link (R17 discipline)
- [ ] H4: `docs/RELEASE_NOTES_v1.0-rc1.md` template drafted (build-time blanks marked)

### Scope I — Build ONE ISO labelled `v1.0-rc1` (both arches)
- [ ] I1: every checkbox in Scopes A–H above is `[x]`
- [ ] I2: `make -C incremental all ARCH=amd64 && make -C incremental all ARCH=arm64` — both build clean
- [ ] I3: QEMU gate on both arches (boot < 90s, corpus 0 failures)
- [ ] I4: UTM Virtualize sweep on arm64 (15-query broad-OS demo, 0 failures)
- [ ] I5: amd64 verification in QEMU + VirtualBox EFI
- [ ] I6: G24 clean against fresh `v1.0-rc1` daemon (0 F-4x/F-5x errors)
- [ ] I7: F-55 fix verified — `service.logs` invokes journalctl without SIGSYS
- [ ] I8: F-56 verified fixed on fresh venv OR reproduced with a `test_f56_*` module added
- [ ] I9: Release-notes template blanks filled: ISO SHA-256 (both arches), component versions,
      release date, GPG key ID

### Scope J — Phase 6 formal exit
- [ ] J1: this § 9 fully green (every `[x]` above and every deferred converted to `[x]`)
- [ ] J2: `CLAUDE.md` Phase 6 restored to `**Complete**` with ISO hashes recorded
- [ ] J3: `git tag -s v1.0-rc1` with amd64 + arm64 hashes in the annotation
- [ ] J4: PR #27 merge acknowledgment (task #109) — signed off after J3 tag

### Cross-references
- `docs/IMPLEMENTATION_PLAN.md § Go/No-Go Gate Checklist (Phase 6 → Phase 7)` —
  strategic gates that layer on this operational checklist
- `AI_Native_OS_Whitepaper.md § 10.5 Audit acknowledgment (2026-07-10)` —
  documentation-honesty note explaining what "Phase 6 complete" historically meant
  vs what it means from `v1.0-rc1` onward
- `CLAUDE.md § Phase Status` — the row that tracks the current state; see H2 for the
  in-progress downgrade

### How to declare a scope complete
1. Every sub-checkbox in the scope is `[x]`.
2. The PR that carried it is merged to `main`.
3. Test suite is green on `main` at merge time (currently 2329 passed / 40 skipped / 2 pre-existing).
4. Any deferred sub-item under the scope is either converted to `[x]` OR moved to
   the appropriate follow-up scope (e.g. Scope D's D3 live-sweep sits under Scope I).

---

## 10. Whitepaper Departures Ledger

Seeded 2026-08-03 from `docs/2026-08-03_whitepaper_vs_reality_audit.md`. Governed by **R17** (§ 2).

Every whitepaper claim that ships differently is one row. Disposition must resolve to either an F-xx (fix that closes it), a milestone in `docs/2026-08-03_phase7_convergence_tracker.md` (planned closure), or an explicit **deliberate** rationale (documented deviation). No orphans.

Legend for `Disposition`:
- `→ Mx.y` — closed by the named milestone in the convergence tracker
- `→ F-xx` — already closed by the named failure-log fix (cross-link only, no new work)
- `deliberate (D-x)` — documented deliberate departure, see § 8 Design decisions
- `deferred → Phase 8` — post-v1.0 backlog, not a v1.0 blocker
- `whitepaper edit → M7.6b` — code is correct, whitepaper text needs update

| ID | Whitepaper § | Claim | Reality | Evidence | Disposition |
|----|--------------|-------|---------|----------|-------------|
| A-1 | §5 line 107 (INV-1) | "QB has zero direct access to system execution tools" | OC edition: QB (Gemini via opencode) invokes mcpd + iceui tools directly | `cx-distro/distro/qb_oc.json.template:6-18`; `mcpd-for-oc.sh:45` | → F-111 (closed by v6.17 M7.6a-1a-i; opencode → submit_intent → Controller pipeline; opencode's permission map locks every non-submit_intent tool to `deny`) |
| A-2 | §5 line 152 | "Controller passes only an opaque reference ID to the PB" | OC edition: no Intent Object, no reference ID, no PB | `dual-brain/controller/main.py:544-545` (OC short-circuit as UNSUPPORTED) | → F-111 (closed by v6.17 M7.6a-1c; `Controller.run_turn_from_intent` receives the intent, `IntentStore.put` mints an opaque UUID, PB receives ONLY the UUID via F-41 rich envelope — same discipline as current edition) |
| A-3 | §5 line 117 | "PB is completely blind to the outside world" | OC edition: PB is not on the call path; systemd unit boots but nothing routes there | `cx-distro/config/hooks/live/0100-icebreaker-setup.hook.chroot:7`; no OC opener of `/run/icebreaker/pbd.sock` | → F-111 (closed by v6.17 M7.6a-1d; PB was always real in OC mode — v.manifest edition-agnostic — but `Controller.run_turn_from_intent` finally routes to it) |
| A-4 | §5 lines 172-176 | "Controller — schema validation, risk classification, HITL gate, audit logging" | OC edition: schema/risk/HITL bypassed on OC tool path; only audit partially preserved | `dual-brain/controller/mcp_gui_server.py:257-263` calls `_dispatch_in_subprocess` directly | → F-111 (closed by v6.17 M7.6a-1a+c; submit_intent forwards to daemon which drives full 11-step pipeline — schema, risk, HITL, audit all fire) |
| A-5 | §8 lines 421-443 | Tier 2 LLM classifier + Tier 3 blocking modal with dry-run + risk badge | OC edition: flattened to opencode's `allow`/`ask`/`deny`; no dry-run, no risk badge, no tier label | `incremental/build/gen-oc-config.sh:107-109,181`; `qb_oc.json.template:26-29` | → F-111 (closed by v6.17 M7.6a-1a-i; submit_intent inherits the v6.16 M7.0.1 real COW dry-run at HITL + risk_classifier tier assignment + M7.0.2 PB grammar mandate + PbRetryLoop QB-consult) |
| A-6 | INV-8 | Audit "captures every intent including rejected" | `iceui_*` tool calls (24 tools) have ZERO audit write path | `dual-brain/controller/mcp_gui_server.py` has 0 `AuditLog` references; `oc_audit_bridge.py:71-74` only tails mcpd's log | → F-111 (partial — submit_intent turns now audit natively via Controller._audit; oc_audit_bridge deduplicates via M7.6a-1f UUID ring. Non-submit_intent iceui direct calls (legacy_direct rollback path only) still ride mcpd log tail — full parity is M7.0.3 v6.18 scope) |
| A-7 | §5 | Audit fields include model, tokens, cost, session, turn | OC audit rows hard-stamp `session_id="opencode-oc"`, `turn_index=-1`, `model=""`, `tokens_in=0`, `tokens_out=0`, `cost=0.0` | `dual-brain/controller/oc_audit_bridge.py:22-27,158-175` | → F-111 (closed by v6.17 M7.6a-1a-i; native audit writes carry real session_id + turn_index + model=`gemini/gemini-2.5-flash` + tokens + cost via M7.6a-1d real Gemini backend + M7.6a-1c pipeline aggregation) |
| A-8 | §3 lines 152-153 | PB "resolves the reference, evaluates the action against its policy rules, and either executes or escalates" | OC edition: no PB policy pass — Gemini emits args, mcpd executes verbatim after opencode's ask | Same as A-1/A-2 | → F-111 (closed by v6.17 M7.6a-1c+d; Step 6 PB grammar-constrained tool call fires + Step 8 verifier vote — full INV-1 discipline restored on OC submit_intent turns) |
| A-9 | §5 line 130 | Raw screenshot bytes never touch the QB | `VisionGrounder` sends screenshot to `gemini-2.5-flash` — same provider as QB; whitepaper has no accommodation for in-QB vision second call | `dual-brain/gui_agent/vision.py:5-6,28-30`; `controller/config.py:366` | whitepaper edit → M7.6b (document as deliberate; INV-1 covers execution isolation, not provider isolation) |
| B-1 | §3/§6 (INV-1) | "PB receives ONLY opaque reference IDs" | PB payload carries `intent_id + target + expected_content + pb_hint` since F-27/F-41 (rich envelope) | `dual-brain/controller/session.py:297-335` | whitepaper edit → M7.6b (also → F-27 + F-41, already-closed as *fixes*; departure is undocumented) |
| B-2 | §8.2 | HITL prompt shows dry-run diff | Tier-3 gate carries `cow_summary=None`; dry-run only in a second post-mcpd gate on streaming path | `dual-brain/controller/main.py:840-845` (initial gate); `main.py:1544-1578` (post-mcpd gate) | → F-109 (closed by v6.16 M7.0.1e; streaming Step 3.5 + AgentGraph cow_preview_node populate cow_summary before hitl_gate fires) |
| B-3 | §5 + §8 | COW dry-run present at every destructive-op approval | AgentGraph `mcpd_dispatcher_node` has NO COW-approval branch; only streaming path has it | `dual-brain/controller/agent_graph_nodes.py:742-879` | → F-109 (closed by v6.16 M7.0.1f; cow_preview_node + mcpd_dispatcher_node commit_cow branch land the AgentGraph half) |
| B-4 | §6 | Constrained-decoded via GBNF — "the model literally CANNOT generate invalid output" | Controller-side `LlamaCppLocalBackend` omits `grammar_path`; server-side `start_pb.sh` silently degrades to unconstrained if file missing | `dual-brain/controller/backends/llama_local_backend.py:196-197`; `dual-brain/controller/__main__.py:170-178`; `dual-brain/scripts/start_pb.sh:62-65` | → F-110 (closed by v6.16 M7.0.2a-g; wire-up + louder degrade-warn + bounded retry with QB-consult rescue when grammar still misses per-tool tightening) |
| B-5 | §4/§6 | GUI/RPA "User → QB → Controller → PB → mcpd" | AgentGraph `mcpd_dispatcher_node` dispatches `gui.*`/`rpa.*` to `controller.gui_worker` subprocess directly — mcpd bypassed | `dual-brain/controller/agent_graph_nodes.py:798-841` | whitepaper edit → M7.6b (isolation invariant preserved via subprocess + sandbox; whitepaper diagram is over-specific about transport) |
| C-1 | §5 | "The AI cannot make irreversible mistakes — COW imagination layer" | `src/mcpd/src/sandbox/cow.rs` did not exist pre-v6.16; ticket-only stub | Original evidence: `src/mcpd/src/tools/fs.rs:215,238,258-278`. Now: `src/mcpd/src/tools/cow.rs` (M7.0.1a) + IntentStore + cow.commit (M7.0.1c) + controller two-phase (M7.0.1d-f) | → F-109 (closed by v6.16 M7.0.1a-g; simulation-based per user-approved design 2026-08-03, not literal overlayfs mount — whitepaper §5 text edit deferred to M7.6b) |
| C-2 | §5 | Total security overhead <10ms — benchmarked | No benchmark; only mcpd tools/list p95 in `ci.sh` G9 | `src/mcpd/ci.sh:133-181` | → M7.6 (p95 profile suite) |
| C-3 | §6 (F2 mitigation) | Speculative decoding "must be implemented from day one" | PB has no draft model shipped; PB launched without `--draft-model`; QB opt-in but no `[pb]` section in example config | `dual-brain/scripts/start-pbd:65-69`; no draft GGUF in tree | deferred → Phase 8 (needs fine-tuned draft) |
| C-4 | §6 | Grammar-constrained decoding on PB | PB launched WITHOUT `--grammar-file` from controller side; silent-degrades server-side | `dual-brain/scripts/start-pbd:63` ("PB uses no grammar — relies on post-hoc tool-call validation") | → F-110 (closed by v6.16 M7.0.2a-g; controller-side + server-side wired + bounded PB retry loop with QB-consult rescue when single-attempt output survives grammar but fails per-tool schema — v6.17 per-tool GBNF tightening still deferred) |
| C-5 | §6 | ~2 GB RAM footprint — measured | Prose only, no measurement | `docs/ARCHITECTURE.md:66` | → M7.7 (release-notes template requires measurement) |
| C-6 | INV-7 | SHA-256 receipts for all shipped GGUFs | Only 1 file listed (`run7_cot_q4km.gguf`); no draft, no QB entries (QB is cloud so N/A but not documented) | `models/checksums.sha256` | → M7.7 (release notes clarify scope) |
| C-7 | §9 | Debian live-build | debootstrap + mksquashfs + xorriso (live-build broken on Noble) | `cx-distro/build.sh:549-550` (documented) | deliberate (D-6) |
| C-8 | §9 | Boot <60s — gated | Timer exists (`first-boot:42,279-289`) but no pass/fail gate | `cx-distro/distro/first-boot:42,279-289` | → M7.7 (release-notes wire the gate) |
| C-9 | §9 + §10 M7.7 | GPG-signed ISO | Not wired anywhere | `incremental/GROUND_TRUTH.md § 1 v1.0 row` (marked PENDING) | → M7.7 |
| C-10 | §10 Phase 7 M7.2–M7.7 | Playwright + MS Graph + Google Workspace MCPs, analytics.chart, GPG, hardening | Not started; only M7.1 pre-work (trust field + empty allowlist) | `cx-distro/distro/mcp_allowlist.toml` empty | → M7.1 (finish) + M7.2 + M7.3 + M7.4 + M7.5 + M7.6 + M7.7 (locked sequence) |
| C-11 | §10 Phase 8 | Goal/plan_steps/verifier/sub-agents | Not started; `agent_graph_bridge` is single-turn plumbing | `dual-brain/controller/agent_graph_bridge.py` | deferred → Phase 8 |
| C-12 | §13 KPI 1 | End-to-end p95 latency suite | Only mcpd tools/list benchmark | `src/mcpd/ci.sh:133-181` | → M7.6 |
| C-13 | §13 KPI 5 | Tier-3/day telemetry aggregator | None | — | → M7.6 (partial) + deferred (fleet observability → Phase 8 M8.5) |
| C-14 | §14 | sandlock / CX Linux / cortexd as primary refs | Not integrated; direct `landlock` crate used | `src/mcpd/src/sandbox/landlock.rs:27-33` | deliberate (whitepaper §14 = inspiration only) |
| C-15 | §14 Datasets | The Stack + man pages in training set | NL2SH / NL2Bash / CodeAlpaca only | `privileged-brain/data/raw/*` | deferred (training-data hygiene sprint post-v1.0) |
| C-16 | BP-3 | Universal ANSI/C0 scrub on model stream | Applied on GUI audit fields only | `dual-brain/controller/audit.py:224,227`; `main.py:1280,1322,1429` | → M7.6 (hardening pass extends to stream) |
| C-17 | BP-13 / R14 | Every F-xx has named regression lock | 48/94 F-xx rows have `regression lock` (~51%) | `incremental/GROUND_TRUTH.md § 7` | → M7.6b (backfill sprint) |

### How to add a row to § 10
1. New whitepaper departure surfaces (audit, PR review, user question).
2. Author the row with `ID` (extend the letter series — D-1, D-2, … for future audits), `Whitepaper §`, `Claim`, `Reality`, `Evidence`, and provisional `Disposition`.
3. If disposition is `→ Mx.y`, the tracker file (`docs/2026-08-03_phase7_convergence_tracker.md`) gains a corresponding row.
4. If disposition is `whitepaper edit → M7.6b`, note it as a M7.6b task.
5. Same-commit PR: touching an INV-\* invariant or a whitepaper section already in § 10 requires this row to update in the same commit (R17).
