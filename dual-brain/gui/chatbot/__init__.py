"""Chatbot window — NL conversation UI with CoT and HITL (M6UI.2).

Sub-modules:
  window         ChatbotWindow (AdwApplicationWindow)
  message_list   Scrollable conversation history
  message_row    User / assistant / system message bubbles
  input_bar      Text entry + send button
  cot_expander   Expandable chain-of-thought card
"""
