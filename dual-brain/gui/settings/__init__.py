"""Settings Panel — visual controller.toml editor (M6UI.1).

Sub-modules:
  window         AdwPreferencesWindow shell
  backend_page   QB backend selector + per-backend config
  model_page     Model manager with checksum verification
  keymap_page    Visual keymap editor
  session_page   Session and UX preferences
  daemon_page    Daemon, HITL, and system config
"""
