# Dual-Brain Controller — Phase 2 Python dependencies
#
# Install on the GCP VM after extracting the dual-brain tarball:
#   python3 -m venv ~/dual-brain-venv
#   source ~/dual-brain-venv/bin/activate
#   pip install -r dual-brain/requirements.txt
#
# All version pins are floor-only (>=) to allow VM-side patch updates.
# Hard upper bounds added where a breaking change is known.

# Schema validation (M2.2) — Intent Object JSON Schema enforcement
jsonschema>=4.20,<5
jsonschema[format-nongpl]>=4.20,<5
# attrs is a hard runtime dep of jsonschema>=4.0 — not guaranteed present in
# the minimal debootstrap chroot, so pin it explicitly.
attrs>=23.1

# Property-based / fuzz testing (M2.2, M2.5, M2.13)
hypothesis>=6.100

# HTTP client for llama-server (M2.5 LlamaCppLocalBackend)
requests>=2.31

# Test runner
pytest>=7.4
pytest-timeout>=2.2
# Async TUI tests (terminal/tests/test_tui.py uses @pytest.mark.asyncio)
pytest-asyncio>=0.23

# Brain backends (M2.6, M2.7)
# Floor versions chosen so native JSON output APIs (Anthropic
# output_config.format, Gemini response_schema) are stable. The
# Anthropic native JSON output requires anthropic >= 0.40 in principle,
# but Claude Haiku 4.5 (the model that supports output_config.format)
# only loads against newer SDKs; runtime install at deploy should be
# the latest stable.
anthropic>=0.40
google-generativeai>=0.8

# Multi-turn REPL line editing + history (M2.11)
prompt-toolkit>=3.0

# AI Terminal TUI (Phase 6T, PR #23)
textual>=0.80
rich>=13

# TOML config parsing (M2.4)
# tomllib is stdlib in Python 3.11+; tomli is the back-port for 3.10.
tomli>=2.0; python_version < "3.11"

# TOML round-trip serialization (settings window — preserves nested tables)
tomlkit>=0.12

# TOML writer for the preset verification cache (save_cache silently skips
# writes without it — pyproject declares it; keep this file in lockstep)
tomli-w>=1.0

# Agent graph runtime (v6.8 Tasks #146/#147) — core deps in pyproject.toml;
# listed here too so the VM deploy path (pip install -r requirements.txt)
# doesn't ship without them.
langgraph>=1.2,<2.0
langgraph-checkpoint-sqlite>=2.0
